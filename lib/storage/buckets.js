var buckets = {};

buckets.list = function(callback) {
  // TODO(jbd): Implement
};

buckets.get = function(name, opt_callback) {
 // TODO(jbd): Implement
};

buckets.save = function(name, opts, opt_callback) {
 // TODO(jbd): Implement
};

buckets.del = function(name, opt_callback) {
 // TODO(jbd): Implement
};

module.exports = buckets;
