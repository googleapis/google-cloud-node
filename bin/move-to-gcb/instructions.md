Use the scripts in this folder to set up a check in the CI pipeline that
will run system tests in GCB for a particular client library.

For example, run the following script to set up system tests in GCB for
bigquery-storage:

./generate_yaml_and_create_trigger.sh -l bigquery-storage
