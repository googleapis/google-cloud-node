// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import {describe, it} from 'mocha';
import * as productreviewsserviceModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = protobuf.Root.fromJSON(
  require('../protos/protos.json')
).resolveAll();

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName: string, fields: string[]) {
  let type = root.lookupType(typeName) as protobuf.Type;
  for (const field of fields.slice(0, -1)) {
    type = type.fields[field]?.resolvedType as protobuf.Type;
  }
  return type.fields[fields[fields.length - 1]]?.defaultValue;
}

function generateSampleMessage<T extends object>(instance: T) {
  const filledObject = (
    instance.constructor as typeof protobuf.Message
  ).toObject(instance as protobuf.Message<T>, {defaults: true});
  return (instance.constructor as typeof protobuf.Message).fromObject(
    filledObject
  ) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
  return error
    ? sinon.stub().rejects(error)
    : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(
  response?: ResponseType,
  error?: Error
) {
  return error
    ? sinon.stub().callsArgWith(2, error)
    : sinon.stub().callsArgWith(2, null, response);
}

function stubPageStreamingCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error
) {
  const pagingStub = sinon.stub();
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
    }
  }
  const transformStub = error
    ? sinon.stub().callsArgWith(2, error)
    : pagingStub;
  const mockStream = new PassThrough({
    objectMode: true,
    transform: transformStub,
  });
  // trigger as many responses as needed
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      setImmediate(() => {
        mockStream.write({});
      });
    }
    setImmediate(() => {
      mockStream.end();
    });
  } else {
    setImmediate(() => {
      mockStream.write({});
    });
    setImmediate(() => {
      mockStream.end();
    });
  }
  return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error
) {
  let counter = 0;
  const asyncIterable = {
    [Symbol.asyncIterator]() {
      return {
        async next() {
          if (error) {
            return Promise.reject(error);
          }
          if (counter >= responses!.length) {
            return Promise.resolve({done: true, value: undefined});
          }
          return Promise.resolve({done: false, value: responses![counter++]});
        },
      };
    },
  };
  return sinon.stub().returns(asyncIterable);
}

describe('v1beta.ProductReviewsServiceClient', () => {
  describe('Common methods', () => {
    it('has apiEndpoint', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient();
      const apiEndpoint = client.apiEndpoint;
      assert.strictEqual(apiEndpoint, 'merchantapi.googleapis.com');
    });

    it('has universeDomain', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient();
      const universeDomain = client.universeDomain;
      assert.strictEqual(universeDomain, 'googleapis.com');
    });

    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      it('throws DeprecationWarning if static servicePath is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const servicePath =
          productreviewsserviceModule.v1beta.ProductReviewsServiceClient
            .servicePath;
        assert.strictEqual(servicePath, 'merchantapi.googleapis.com');
        assert(stub.called);
        stub.restore();
      });

      it('throws DeprecationWarning if static apiEndpoint is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const apiEndpoint =
          productreviewsserviceModule.v1beta.ProductReviewsServiceClient
            .apiEndpoint;
        assert.strictEqual(apiEndpoint, 'merchantapi.googleapis.com');
        assert(stub.called);
        stub.restore();
      });
    }
    it('sets apiEndpoint according to universe domain camelCase', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          universeDomain: 'example.com',
        });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'merchantapi.example.com');
    });

    it('sets apiEndpoint according to universe domain snakeCase', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          universe_domain: 'example.com',
        });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'merchantapi.example.com');
    });

    if (typeof process === 'object' && 'env' in process) {
      describe('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
        it('sets apiEndpoint from environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client =
            new productreviewsserviceModule.v1beta.ProductReviewsServiceClient();
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'merchantapi.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });

        it('value configured in code has priority over environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client =
            new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
              universeDomain: 'configured.example.com',
            });
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'merchantapi.configured.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });
      });
    }
    it('does not allow setting both universeDomain and universe_domain', () => {
      assert.throws(() => {
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          universe_domain: 'example.com',
          universeDomain: 'example.net',
        });
      });
    });

    it('has port', () => {
      const port =
        productreviewsserviceModule.v1beta.ProductReviewsServiceClient.port;
      assert(port);
      assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient();
      assert(client);
    });

    it('should create a client with gRPC fallback', () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          fallback: true,
        });
      assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      assert.strictEqual(client.productReviewsServiceStub, undefined);
      await client.initialize();
      assert(client.productReviewsServiceStub);
    });

    it('has close method for the initialized client', done => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      client.initialize().catch(err => {
        throw err;
      });
      assert(client.productReviewsServiceStub);
      client
        .close()
        .then(() => {
          done();
        })
        .catch(err => {
          throw err;
        });
    });

    it('has close method for the non-initialized client', done => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      assert.strictEqual(client.productReviewsServiceStub, undefined);
      client
        .close()
        .then(() => {
          done();
        })
        .catch(err => {
          throw err;
        });
    });

    it('has getProjectId method', async () => {
      const fakeProjectId = 'fake-project-id';
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
      const result = await client.getProjectId();
      assert.strictEqual(result, fakeProjectId);
      assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
      const fakeProjectId = 'fake-project-id';
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      client.auth.getProjectId = sinon
        .stub()
        .callsArgWith(0, null, fakeProjectId);
      const promise = new Promise((resolve, reject) => {
        client.getProjectId((err?: Error | null, projectId?: string | null) => {
          if (err) {
            reject(err);
          } else {
            resolve(projectId);
          }
        });
      });
      const result = await promise;
      assert.strictEqual(result, fakeProjectId);
    });
  });

  describe('getProductReview', () => {
    it('invokes getProductReview without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
      );
      client.innerApiCalls.getProductReview = stubSimpleCall(expectedResponse);
      const [response] = await client.getProductReview(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getProductReview without error using callback', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
      );
      client.innerApiCalls.getProductReview =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getProductReview(
          request,
          (
            err?: Error | null,
            result?: protos.google.shopping.merchant.reviews.v1beta.IProductReview | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getProductReview with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getProductReview = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.getProductReview(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getProductReview with closed client', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.GetProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch(err => {
        throw err;
      });
      await assert.rejects(client.getProductReview(request), expectedError);
    });
  });

  describe('insertProductReview', () => {
    it('invokes insertProductReview without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
      );
      client.innerApiCalls.insertProductReview =
        stubSimpleCall(expectedResponse);
      const [response] = await client.insertProductReview(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes insertProductReview without error using callback', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
      );
      client.innerApiCalls.insertProductReview =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.insertProductReview(
          request,
          (
            err?: Error | null,
            result?: protos.google.shopping.merchant.reviews.v1beta.IProductReview | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes insertProductReview with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.insertProductReview = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.insertProductReview(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.insertProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes insertProductReview with closed client', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.InsertProductReviewRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch(err => {
        throw err;
      });
      await assert.rejects(client.insertProductReview(request), expectedError);
    });
  });

  describe('deleteProductReview', () => {
    it('invokes deleteProductReview without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty()
      );
      client.innerApiCalls.deleteProductReview =
        stubSimpleCall(expectedResponse);
      const [response] = await client.deleteProductReview(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteProductReview without error using callback', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty()
      );
      client.innerApiCalls.deleteProductReview =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.deleteProductReview(
          request,
          (
            err?: Error | null,
            result?: protos.google.protobuf.IEmpty | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteProductReview with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.deleteProductReview = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.deleteProductReview(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteProductReview as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteProductReview with closed client', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.DeleteProductReviewRequest',
        ['name']
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch(err => {
        throw err;
      });
      await assert.rejects(client.deleteProductReview(request), expectedError);
    });
  });

  describe('listProductReviews', () => {
    it('invokes listProductReviews without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
      ];
      client.innerApiCalls.listProductReviews =
        stubSimpleCall(expectedResponse);
      const [response] = await client.listProductReviews(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listProductReviews without error using callback', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
      ];
      client.innerApiCalls.listProductReviews =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listProductReviews(
          request,
          (
            err?: Error | null,
            result?:
              | protos.google.shopping.merchant.reviews.v1beta.IProductReview[]
              | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listProductReviews with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listProductReviews = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.listProductReviews(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listProductReviews as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listProductReviewsStream without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
      ];
      client.descriptors.page.listProductReviews.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listProductReviewsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.shopping.merchant.reviews.v1beta.ProductReview[] =
          [];
        stream.on(
          'data',
          (
            response: protos.google.shopping.merchant.reviews.v1beta.ProductReview
          ) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listProductReviews.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listProductReviews, request)
      );
      assert(
        (client.descriptors.page.listProductReviews.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams)
      );
    });

    it('invokes listProductReviewsStream with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listProductReviews.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.listProductReviewsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.shopping.merchant.reviews.v1beta.ProductReview[] =
          [];
        stream.on(
          'data',
          (
            response: protos.google.shopping.merchant.reviews.v1beta.ProductReview
          ) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listProductReviews.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listProductReviews, request)
      );
      assert(
        (client.descriptors.page.listProductReviews.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams)
      );
    });

    it('uses async iteration with listProductReviews without error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
        generateSampleMessage(
          new protos.google.shopping.merchant.reviews.v1beta.ProductReview()
        ),
      ];
      client.descriptors.page.listProductReviews.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.shopping.merchant.reviews.v1beta.IProductReview[] =
        [];
      const iterable = client.listProductReviewsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.listProductReviews.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
      assert(
        (client.descriptors.page.listProductReviews.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams)
      );
    });

    it('uses async iteration with listProductReviews with error', async () => {
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest()
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.shopping.merchant.reviews.v1beta.ListProductReviewsRequest',
        ['parent']
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listProductReviews.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listProductReviewsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.shopping.merchant.reviews.v1beta.IProductReview[] =
          [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.listProductReviews.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
      assert(
        (client.descriptors.page.listProductReviews.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams)
      );
    });
  });

  describe('Path templates', () => {
    describe('account', async () => {
      const fakePath = '/rendered/path/account';
      const expectedParameters = {
        account: 'accountValue',
      };
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      client.pathTemplates.accountPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.accountPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('accountPath', () => {
        const result = client.accountPath('accountValue');
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.accountPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters)
        );
      });

      it('matchAccountFromAccountName', () => {
        const result = client.matchAccountFromAccountName(fakePath);
        assert.strictEqual(result, 'accountValue');
        assert(
          (client.pathTemplates.accountPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath)
        );
      });
    });

    describe('merchantReview', async () => {
      const fakePath = '/rendered/path/merchantReview';
      const expectedParameters = {
        account: 'accountValue',
        name: 'nameValue',
      };
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      client.pathTemplates.merchantReviewPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.merchantReviewPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('merchantReviewPath', () => {
        const result = client.merchantReviewPath('accountValue', 'nameValue');
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.merchantReviewPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters)
        );
      });

      it('matchAccountFromMerchantReviewName', () => {
        const result = client.matchAccountFromMerchantReviewName(fakePath);
        assert.strictEqual(result, 'accountValue');
        assert(
          (client.pathTemplates.merchantReviewPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath)
        );
      });

      it('matchNameFromMerchantReviewName', () => {
        const result = client.matchNameFromMerchantReviewName(fakePath);
        assert.strictEqual(result, 'nameValue');
        assert(
          (client.pathTemplates.merchantReviewPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath)
        );
      });
    });

    describe('productReview', async () => {
      const fakePath = '/rendered/path/productReview';
      const expectedParameters = {
        account: 'accountValue',
        productreview: 'productreviewValue',
      };
      const client =
        new productreviewsserviceModule.v1beta.ProductReviewsServiceClient({
          credentials: {client_email: 'bogus', private_key: 'bogus'},
          projectId: 'bogus',
        });
      await client.initialize();
      client.pathTemplates.productReviewPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.productReviewPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('productReviewPath', () => {
        const result = client.productReviewPath(
          'accountValue',
          'productreviewValue'
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.productReviewPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters)
        );
      });

      it('matchAccountFromProductReviewName', () => {
        const result = client.matchAccountFromProductReviewName(fakePath);
        assert.strictEqual(result, 'accountValue');
        assert(
          (client.pathTemplates.productReviewPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath)
        );
      });

      it('matchProductreviewFromProductReviewName', () => {
        const result = client.matchProductreviewFromProductReviewName(fakePath);
        assert.strictEqual(result, 'productreviewValue');
        assert(
          (client.pathTemplates.productReviewPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath)
        );
      });
    });
  });
});
