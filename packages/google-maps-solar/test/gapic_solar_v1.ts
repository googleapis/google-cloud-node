// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import {describe, it} from 'mocha';
import * as solarModule from '../src';

import {protobuf} from 'google-gax';

// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = protobuf.Root.fromJSON(
  require('../protos/protos.json')
).resolveAll();

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName: string, fields: string[]) {
  let type = root.lookupType(typeName) as protobuf.Type;
  for (const field of fields.slice(0, -1)) {
    type = type.fields[field]?.resolvedType as protobuf.Type;
  }
  return type.fields[fields[fields.length - 1]]?.defaultValue;
}

function generateSampleMessage<T extends object>(instance: T) {
  const filledObject = (
    instance.constructor as typeof protobuf.Message
  ).toObject(instance as protobuf.Message<T>, {defaults: true});
  return (instance.constructor as typeof protobuf.Message).fromObject(
    filledObject
  ) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
  return error
    ? sinon.stub().rejects(error)
    : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(
  response?: ResponseType,
  error?: Error
) {
  return error
    ? sinon.stub().callsArgWith(2, error)
    : sinon.stub().callsArgWith(2, null, response);
}

describe('v1.SolarClient', () => {
  describe('Common methods', () => {
    it('has apiEndpoint', () => {
      const client = new solarModule.v1.SolarClient();
      const apiEndpoint = client.apiEndpoint;
      assert.strictEqual(apiEndpoint, 'solar.googleapis.com');
    });

    it('has universeDomain', () => {
      const client = new solarModule.v1.SolarClient();
      const universeDomain = client.universeDomain;
      assert.strictEqual(universeDomain, 'googleapis.com');
    });

    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      it('throws DeprecationWarning if static servicePath is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const servicePath = solarModule.v1.SolarClient.servicePath;
        assert.strictEqual(servicePath, 'solar.googleapis.com');
        assert(stub.called);
        stub.restore();
      });

      it('throws DeprecationWarning if static apiEndpoint is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const apiEndpoint = solarModule.v1.SolarClient.apiEndpoint;
        assert.strictEqual(apiEndpoint, 'solar.googleapis.com');
        assert(stub.called);
        stub.restore();
      });
    }
    it('sets apiEndpoint according to universe domain camelCase', () => {
      const client = new solarModule.v1.SolarClient({
        universeDomain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'solar.example.com');
    });

    it('sets apiEndpoint according to universe domain snakeCase', () => {
      const client = new solarModule.v1.SolarClient({
        universe_domain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'solar.example.com');
    });

    if (typeof process === 'object' && 'env' in process) {
      describe('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
        it('sets apiEndpoint from environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new solarModule.v1.SolarClient();
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'solar.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });

        it('value configured in code has priority over environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new solarModule.v1.SolarClient({
            universeDomain: 'configured.example.com',
          });
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'solar.configured.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });
      });
    }
    it('does not allow setting both universeDomain and universe_domain', () => {
      assert.throws(() => {
        new solarModule.v1.SolarClient({
          universe_domain: 'example.com',
          universeDomain: 'example.net',
        });
      });
    });

    it('has port', () => {
      const port = solarModule.v1.SolarClient.port;
      assert(port);
      assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
      const client = new solarModule.v1.SolarClient();
      assert(client);
    });

    it('should create a client with gRPC fallback', () => {
      const client = new solarModule.v1.SolarClient({
        fallback: true,
      });
      assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.solarStub, undefined);
      await client.initialize();
      assert(client.solarStub);
    });

    it('has close method for the initialized client', done => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      assert(client.solarStub);
      client.close().then(() => {
        done();
      });
    });

    it('has close method for the non-initialized client', done => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.solarStub, undefined);
      client.close().then(() => {
        done();
      });
    });

    it('has getProjectId method', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
      const result = await client.getProjectId();
      assert.strictEqual(result, fakeProjectId);
      assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon
        .stub()
        .callsArgWith(0, null, fakeProjectId);
      const promise = new Promise((resolve, reject) => {
        client.getProjectId((err?: Error | null, projectId?: string | null) => {
          if (err) {
            reject(err);
          } else {
            resolve(projectId);
          }
        });
      });
      const result = await promise;
      assert.strictEqual(result, fakeProjectId);
    });
  });

  describe('findClosestBuildingInsights', () => {
    it('invokes findClosestBuildingInsights without error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.FindClosestBuildingInsightsRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.solar.v1.BuildingInsights()
      );
      client.innerApiCalls.findClosestBuildingInsights =
        stubSimpleCall(expectedResponse);
      const [response] = await client.findClosestBuildingInsights(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes findClosestBuildingInsights without error using callback', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.FindClosestBuildingInsightsRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.solar.v1.BuildingInsights()
      );
      client.innerApiCalls.findClosestBuildingInsights =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.findClosestBuildingInsights(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.solar.v1.IBuildingInsights | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes findClosestBuildingInsights with error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.FindClosestBuildingInsightsRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.findClosestBuildingInsights = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(
        client.findClosestBuildingInsights(request),
        expectedError
      );
    });

    it('invokes findClosestBuildingInsights with closed client', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.FindClosestBuildingInsightsRequest()
      );
      const expectedError = new Error('The client has already been closed.');
      client.close();
      await assert.rejects(
        client.findClosestBuildingInsights(request),
        expectedError
      );
    });
  });

  describe('getDataLayers', () => {
    it('invokes getDataLayers without error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetDataLayersRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.solar.v1.DataLayers()
      );
      client.innerApiCalls.getDataLayers = stubSimpleCall(expectedResponse);
      const [response] = await client.getDataLayers(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes getDataLayers without error using callback', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetDataLayersRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.solar.v1.DataLayers()
      );
      client.innerApiCalls.getDataLayers =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getDataLayers(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.solar.v1.IDataLayers | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes getDataLayers with error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetDataLayersRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.getDataLayers = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.getDataLayers(request), expectedError);
    });

    it('invokes getDataLayers with closed client', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetDataLayersRequest()
      );
      const expectedError = new Error('The client has already been closed.');
      client.close();
      await assert.rejects(client.getDataLayers(request), expectedError);
    });
  });

  describe('getGeoTiff', () => {
    it('invokes getGeoTiff without error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetGeoTiffRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.api.HttpBody()
      );
      client.innerApiCalls.getGeoTiff = stubSimpleCall(expectedResponse);
      const [response] = await client.getGeoTiff(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes getGeoTiff without error using callback', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetGeoTiffRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.api.HttpBody()
      );
      client.innerApiCalls.getGeoTiff =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getGeoTiff(
          request,
          (err?: Error | null, result?: protos.google.api.IHttpBody | null) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes getGeoTiff with error', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetGeoTiffRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.getGeoTiff = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.getGeoTiff(request), expectedError);
    });

    it('invokes getGeoTiff with closed client', async () => {
      const client = new solarModule.v1.SolarClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.solar.v1.GetGeoTiffRequest()
      );
      const expectedError = new Error('The client has already been closed.');
      client.close();
      await assert.rejects(client.getGeoTiff(request), expectedError);
    });
  });
});
