// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import type * as gax from 'google-gax';
import type {
  Callback,
  CallOptions,
  Descriptors,
  ClientOptions,
  PaginationCallback,
  GaxCall,
} from 'google-gax';
import {Transform} from 'stream';
import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');

/**
 * Client JSON configuration object, loaded from
 * `src/v1/weather_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './weather_client_config.json';
const version = require('../../../package.json').version;

/**
 *  Service definition for the Weather API.
 * @class
 * @memberof v1
 */
export class WeatherClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _providedCustomServicePath: boolean;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: {[method: string]: gax.CallSettings};
  private _universeDomain: string;
  private _servicePath: string;
  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  warn: (code: string, message: string, warnType?: string) => void;
  innerApiCalls: {[name: string]: Function};
  weatherStub?: Promise<{[name: string]: Function}>;

  /**
   * Construct an instance of WeatherClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://developers.google.com/identity/protocols/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP/1.1 REST mode.
   *     For more information, please check the
   *     {@link https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#http11-rest-api-mode documentation}.
   * @param {gax} [gaxInstance]: loaded instance of `google-gax`. Useful if you
   *     need to avoid loading the default gRPC version and want to use the fallback
   *     HTTP implementation. Load only fallback version and pass it to the constructor:
   *     ```
   *     const gax = require('google-gax/build/src/fallback'); // avoids loading google-gax with gRPC
   *     const client = new WeatherClient({fallback: true}, gax);
   *     ```
   */
  constructor(
    opts?: ClientOptions,
    gaxInstance?: typeof gax | typeof gax.fallback
  ) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof WeatherClient;
    if (
      opts?.universe_domain &&
      opts?.universeDomain &&
      opts?.universe_domain !== opts?.universeDomain
    ) {
      throw new Error(
        'Please set either universe_domain or universeDomain, but not both.'
      );
    }
    const universeDomainEnvVar =
      typeof process === 'object' && typeof process.env === 'object'
        ? process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN']
        : undefined;
    this._universeDomain =
      opts?.universeDomain ??
      opts?.universe_domain ??
      universeDomainEnvVar ??
      'googleapis.com';
    this._servicePath = 'weather.' + this._universeDomain;
    const servicePath =
      opts?.servicePath || opts?.apiEndpoint || this._servicePath;
    this._providedCustomServicePath = !!(
      opts?.servicePath || opts?.apiEndpoint
    );
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback =
      opts?.fallback ??
      (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({servicePath, port, clientConfig, fallback}, opts);

    // Request numeric enum values if REST transport is used.
    opts.numericEnums = true;

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== this._servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Load google-gax module synchronously if needed
    if (!gaxInstance) {
      gaxInstance = require('google-gax') as typeof gax;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gaxInstance.fallback : gaxInstance;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = this._gaxGrpc.auth as gax.GoogleAuth;

    // Set useJWTAccessWithScope on the auth object.
    this.auth.useJWTAccessWithScope = true;

    // Set defaultServicePath on the auth object.
    this.auth.defaultServicePath = this._servicePath;

    // Set the default scopes in auth client if needed.
    if (servicePath === this._servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }

    // Determine the client header string.
    const clientHeader = [`gax/${this._gaxModule.version}`, `gapic/${version}`];
    if (typeof process === 'object' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      lookupForecastHours: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'forecastHours'
      ),
      lookupForecastDays: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'forecastDays'
      ),
      lookupHistoryHours: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'historyHours'
      ),
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
      'google.maps.weather.v1.Weather',
      gapicConfig as gax.ClientConfig,
      opts.clientConfig || {},
      {'x-goog-api-client': clientHeader.join(' ')}
    );

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};

    // Add a warn function to the client constructor so it can be easily tested.
    this.warn = this._gaxModule.warn;
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.weatherStub) {
      return this.weatherStub;
    }

    // Put together the "service stub" for
    // google.maps.weather.v1.Weather.
    this.weatherStub = this._gaxGrpc.createStub(
      this._opts.fallback
        ? (this._protos as protobuf.Root).lookupService(
            'google.maps.weather.v1.Weather'
          )
        : // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.maps.weather.v1.Weather,
      this._opts,
      this._providedCustomServicePath
    ) as Promise<{[method: string]: Function}>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const weatherStubMethods = [
      'lookupCurrentConditions',
      'lookupForecastHours',
      'lookupForecastDays',
      'lookupHistoryHours',
    ];
    for (const methodName of weatherStubMethods) {
      const callPromise = this.weatherStub.then(
        stub =>
          (...args: Array<{}>) => {
            if (this._terminated) {
              return Promise.reject('The client has already been closed.');
            }
            const func = stub[methodName];
            return func.apply(stub, args);
          },
        (err: Error | null | undefined) => () => {
          throw err;
        }
      );

      const descriptor = this.descriptors.page[methodName] || undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor,
        this._opts.fallback
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.weatherStub;
  }

  /**
   * The DNS address for this API service.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static servicePath is deprecated, please use the instance method instead.',
        'DeprecationWarning'
      );
    }
    return 'weather.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static apiEndpoint is deprecated, please use the instance method instead.',
        'DeprecationWarning'
      );
    }
    return 'weather.googleapis.com';
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  get apiEndpoint() {
    return this._servicePath;
  }

  get universeDomain() {
    return this._universeDomain;
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return ['https://www.googleapis.com/auth/cloud-platform'];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(
    callback?: Callback<string, undefined, undefined>
  ): Promise<string> | void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  /**
   * Returns the current weather conditions at a given location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the current weather conditions for.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.maps.weather.v1.LookupCurrentConditionsResponse|LookupCurrentConditionsResponse}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/weather.lookup_current_conditions.js</caption>
   * region_tag:weather_v1_generated_Weather_LookupCurrentConditions_async
   */
  lookupCurrentConditions(
    request?: protos.google.maps.weather.v1.ILookupCurrentConditionsRequest,
    options?: CallOptions
  ): Promise<
    [
      protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
      protos.google.maps.weather.v1.ILookupCurrentConditionsRequest | undefined,
      {} | undefined,
    ]
  >;
  lookupCurrentConditions(
    request: protos.google.maps.weather.v1.ILookupCurrentConditionsRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
      | protos.google.maps.weather.v1.ILookupCurrentConditionsRequest
      | null
      | undefined,
      {} | null | undefined
    >
  ): void;
  lookupCurrentConditions(
    request: protos.google.maps.weather.v1.ILookupCurrentConditionsRequest,
    callback: Callback<
      protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
      | protos.google.maps.weather.v1.ILookupCurrentConditionsRequest
      | null
      | undefined,
      {} | null | undefined
    >
  ): void;
  lookupCurrentConditions(
    request?: protos.google.maps.weather.v1.ILookupCurrentConditionsRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
          | protos.google.maps.weather.v1.ILookupCurrentConditionsRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
      | protos.google.maps.weather.v1.ILookupCurrentConditionsRequest
      | null
      | undefined,
      {} | null | undefined
    >
  ): Promise<
    [
      protos.google.maps.weather.v1.ILookupCurrentConditionsResponse,
      protos.google.maps.weather.v1.ILookupCurrentConditionsRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize();
    return this.innerApiCalls.lookupCurrentConditions(
      request,
      options,
      callback
    );
  }

  /**
   * Returns up to 240 hours of hourly forecasts at a given location, starting
   * from the current hour.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly forecast for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the
   *   current hour - a value from 1 to 240 (inclusive). The default is the
   *   maximum allowed value of 240.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly forecast records to return per page
   *   - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.maps.weather.v1.ForecastHour|ForecastHour}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `lookupForecastHoursAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupForecastHours(
    request?: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    options?: CallOptions
  ): Promise<
    [
      protos.google.maps.weather.v1.IForecastHour[],
      protos.google.maps.weather.v1.ILookupForecastHoursRequest | null,
      protos.google.maps.weather.v1.ILookupForecastHoursResponse,
    ]
  >;
  lookupForecastHours(
    request: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastHoursRequest,
      | protos.google.maps.weather.v1.ILookupForecastHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastHour
    >
  ): void;
  lookupForecastHours(
    request: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastHoursRequest,
      | protos.google.maps.weather.v1.ILookupForecastHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastHour
    >
  ): void;
  lookupForecastHours(
    request?: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.maps.weather.v1.ILookupForecastHoursRequest,
          | protos.google.maps.weather.v1.ILookupForecastHoursResponse
          | null
          | undefined,
          protos.google.maps.weather.v1.IForecastHour
        >,
    callback?: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastHoursRequest,
      | protos.google.maps.weather.v1.ILookupForecastHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastHour
    >
  ): Promise<
    [
      protos.google.maps.weather.v1.IForecastHour[],
      protos.google.maps.weather.v1.ILookupForecastHoursRequest | null,
      protos.google.maps.weather.v1.ILookupForecastHoursResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize();
    return this.innerApiCalls.lookupForecastHours(request, options, callback);
  }

  /**
   * Equivalent to `lookupForecastHours`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly forecast for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the
   *   current hour - a value from 1 to 240 (inclusive). The default is the
   *   maximum allowed value of 240.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly forecast records to return per page
   *   - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.maps.weather.v1.ForecastHour|ForecastHour} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `lookupForecastHoursAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupForecastHoursStream(
    request?: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    options?: CallOptions
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupForecastHours'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupForecastHours.createStream(
      this.innerApiCalls.lookupForecastHours as GaxCall,
      request,
      callSettings
    );
  }

  /**
   * Equivalent to `lookupForecastHours`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly forecast for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the
   *   current hour - a value from 1 to 240 (inclusive). The default is the
   *   maximum allowed value of 240.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly forecast records to return per page
   *   - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.maps.weather.v1.ForecastHour|ForecastHour}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/weather.lookup_forecast_hours.js</caption>
   * region_tag:weather_v1_generated_Weather_LookupForecastHours_async
   */
  lookupForecastHoursAsync(
    request?: protos.google.maps.weather.v1.ILookupForecastHoursRequest,
    options?: CallOptions
  ): AsyncIterable<protos.google.maps.weather.v1.IForecastHour> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupForecastHours'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupForecastHours.asyncIterate(
      this.innerApiCalls['lookupForecastHours'] as GaxCall,
      request as {},
      callSettings
    ) as AsyncIterable<protos.google.maps.weather.v1.IForecastHour>;
  }
  /**
   * Returns up to 10 days of daily forecasts at a given location, starting from
   * the current day.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the daily forecast for.
   * @param {number} [request.days]
   *   Optional. Limits the amount of total days to fetch starting from the
   *   current day - a value from 1 to 10 (inclusive). The default value is the
   *   maximum allowed value of 10.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of daily forecast records to return per page -
   *   a value from 1 to 10 (inclusive). The default value is 5.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.maps.weather.v1.ForecastDay|ForecastDay}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `lookupForecastDaysAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupForecastDays(
    request?: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    options?: CallOptions
  ): Promise<
    [
      protos.google.maps.weather.v1.IForecastDay[],
      protos.google.maps.weather.v1.ILookupForecastDaysRequest | null,
      protos.google.maps.weather.v1.ILookupForecastDaysResponse,
    ]
  >;
  lookupForecastDays(
    request: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastDaysRequest,
      | protos.google.maps.weather.v1.ILookupForecastDaysResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastDay
    >
  ): void;
  lookupForecastDays(
    request: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastDaysRequest,
      | protos.google.maps.weather.v1.ILookupForecastDaysResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastDay
    >
  ): void;
  lookupForecastDays(
    request?: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.maps.weather.v1.ILookupForecastDaysRequest,
          | protos.google.maps.weather.v1.ILookupForecastDaysResponse
          | null
          | undefined,
          protos.google.maps.weather.v1.IForecastDay
        >,
    callback?: PaginationCallback<
      protos.google.maps.weather.v1.ILookupForecastDaysRequest,
      | protos.google.maps.weather.v1.ILookupForecastDaysResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IForecastDay
    >
  ): Promise<
    [
      protos.google.maps.weather.v1.IForecastDay[],
      protos.google.maps.weather.v1.ILookupForecastDaysRequest | null,
      protos.google.maps.weather.v1.ILookupForecastDaysResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize();
    return this.innerApiCalls.lookupForecastDays(request, options, callback);
  }

  /**
   * Equivalent to `lookupForecastDays`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the daily forecast for.
   * @param {number} [request.days]
   *   Optional. Limits the amount of total days to fetch starting from the
   *   current day - a value from 1 to 10 (inclusive). The default value is the
   *   maximum allowed value of 10.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of daily forecast records to return per page -
   *   a value from 1 to 10 (inclusive). The default value is 5.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.maps.weather.v1.ForecastDay|ForecastDay} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `lookupForecastDaysAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupForecastDaysStream(
    request?: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    options?: CallOptions
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupForecastDays'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupForecastDays.createStream(
      this.innerApiCalls.lookupForecastDays as GaxCall,
      request,
      callSettings
    );
  }

  /**
   * Equivalent to `lookupForecastDays`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the daily forecast for.
   * @param {number} [request.days]
   *   Optional. Limits the amount of total days to fetch starting from the
   *   current day - a value from 1 to 10 (inclusive). The default value is the
   *   maximum allowed value of 10.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of daily forecast records to return per page -
   *   a value from 1 to 10 (inclusive). The default value is 5.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.maps.weather.v1.ForecastDay|ForecastDay}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/weather.lookup_forecast_days.js</caption>
   * region_tag:weather_v1_generated_Weather_LookupForecastDays_async
   */
  lookupForecastDaysAsync(
    request?: protos.google.maps.weather.v1.ILookupForecastDaysRequest,
    options?: CallOptions
  ): AsyncIterable<protos.google.maps.weather.v1.IForecastDay> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupForecastDays'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupForecastDays.asyncIterate(
      this.innerApiCalls['lookupForecastDays'] as GaxCall,
      request as {},
      callSettings
    ) as AsyncIterable<protos.google.maps.weather.v1.IForecastDay>;
  }
  /**
   * Returns up to 24 hours of hourly historical weather data at a given
   * location, starting from the last hour.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly historical data for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the last
   *   hour - a from 1 to 24 (inclusive). The default is the maximum allowed value
   *   of 24.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly historical records to return per
   *   page - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.maps.weather.v1.HistoryHour|HistoryHour}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `lookupHistoryHoursAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupHistoryHours(
    request?: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    options?: CallOptions
  ): Promise<
    [
      protos.google.maps.weather.v1.IHistoryHour[],
      protos.google.maps.weather.v1.ILookupHistoryHoursRequest | null,
      protos.google.maps.weather.v1.ILookupHistoryHoursResponse,
    ]
  >;
  lookupHistoryHours(
    request: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
      | protos.google.maps.weather.v1.ILookupHistoryHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IHistoryHour
    >
  ): void;
  lookupHistoryHours(
    request: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    callback: PaginationCallback<
      protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
      | protos.google.maps.weather.v1.ILookupHistoryHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IHistoryHour
    >
  ): void;
  lookupHistoryHours(
    request?: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
          | protos.google.maps.weather.v1.ILookupHistoryHoursResponse
          | null
          | undefined,
          protos.google.maps.weather.v1.IHistoryHour
        >,
    callback?: PaginationCallback<
      protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
      | protos.google.maps.weather.v1.ILookupHistoryHoursResponse
      | null
      | undefined,
      protos.google.maps.weather.v1.IHistoryHour
    >
  ): Promise<
    [
      protos.google.maps.weather.v1.IHistoryHour[],
      protos.google.maps.weather.v1.ILookupHistoryHoursRequest | null,
      protos.google.maps.weather.v1.ILookupHistoryHoursResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize();
    return this.innerApiCalls.lookupHistoryHours(request, options, callback);
  }

  /**
   * Equivalent to `lookupHistoryHours`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly historical data for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the last
   *   hour - a from 1 to 24 (inclusive). The default is the maximum allowed value
   *   of 24.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly historical records to return per
   *   page - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.maps.weather.v1.HistoryHour|HistoryHour} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `lookupHistoryHoursAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  lookupHistoryHoursStream(
    request?: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    options?: CallOptions
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupHistoryHours'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupHistoryHours.createStream(
      this.innerApiCalls.lookupHistoryHours as GaxCall,
      request,
      callSettings
    );
  }

  /**
   * Equivalent to `lookupHistoryHours`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.type.LatLng} request.location
   *   Required. The location to get the hourly historical data for.
   * @param {number} [request.hours]
   *   Optional. Limits the amount of total hours to fetch starting from the last
   *   hour - a from 1 to 24 (inclusive). The default is the maximum allowed value
   *   of 24.
   * @param {google.maps.weather.v1.UnitsSystem} [request.unitsSystem]
   *   Optional. The units system to use for the returned weather conditions. If
   *   not provided, the returned weather conditions will be in the metric system
   *   (default = METRIC).
   * @param {string} [request.languageCode]
   *   Optional. Allows the client to choose the language for the response. If
   *   data cannot be provided for that language, the API uses the closest match.
   *   Allowed values rely on the IETF BCP-47 standard. The default value is "en".
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of hourly historical records to return per
   *   page - a value from 1 to 24 (inclusive). The default is the maximum allowed
   *   value of 24.
   * @param {string} [request.pageToken]
   *   Optional. A page token received from a previous request. It is used to
   *   retrieve the subsequent page.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.maps.weather.v1.HistoryHour|HistoryHour}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/weather.lookup_history_hours.js</caption>
   * region_tag:weather_v1_generated_Weather_LookupHistoryHours_async
   */
  lookupHistoryHoursAsync(
    request?: protos.google.maps.weather.v1.ILookupHistoryHoursRequest,
    options?: CallOptions
  ): AsyncIterable<protos.google.maps.weather.v1.IHistoryHour> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['lookupHistoryHours'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize();
    return this.descriptors.page.lookupHistoryHours.asyncIterate(
      this.innerApiCalls['lookupHistoryHours'] as GaxCall,
      request as {},
      callSettings
    ) as AsyncIterable<protos.google.maps.weather.v1.IHistoryHour>;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    if (this.weatherStub && !this._terminated) {
      return this.weatherStub.then(stub => {
        this._terminated = true;
        stub.close();
      });
    }
    return Promise.resolve();
  }
}
