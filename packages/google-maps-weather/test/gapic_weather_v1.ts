// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import {describe, it} from 'mocha';
import * as weatherModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = protobuf.Root.fromJSON(
  require('../protos/protos.json')
).resolveAll();

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName: string, fields: string[]) {
  let type = root.lookupType(typeName) as protobuf.Type;
  for (const field of fields.slice(0, -1)) {
    type = type.fields[field]?.resolvedType as protobuf.Type;
  }
  return type.fields[fields[fields.length - 1]]?.defaultValue;
}

function generateSampleMessage<T extends object>(instance: T) {
  const filledObject = (
    instance.constructor as typeof protobuf.Message
  ).toObject(instance as protobuf.Message<T>, {defaults: true});
  return (instance.constructor as typeof protobuf.Message).fromObject(
    filledObject
  ) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
  return error
    ? sinon.stub().rejects(error)
    : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(
  response?: ResponseType,
  error?: Error
) {
  return error
    ? sinon.stub().callsArgWith(2, error)
    : sinon.stub().callsArgWith(2, null, response);
}

function stubPageStreamingCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error
) {
  const pagingStub = sinon.stub();
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
    }
  }
  const transformStub = error
    ? sinon.stub().callsArgWith(2, error)
    : pagingStub;
  const mockStream = new PassThrough({
    objectMode: true,
    transform: transformStub,
  });
  // trigger as many responses as needed
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      setImmediate(() => {
        mockStream.write({});
      });
    }
    setImmediate(() => {
      mockStream.end();
    });
  } else {
    setImmediate(() => {
      mockStream.write({});
    });
    setImmediate(() => {
      mockStream.end();
    });
  }
  return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error
) {
  let counter = 0;
  const asyncIterable = {
    [Symbol.asyncIterator]() {
      return {
        async next() {
          if (error) {
            return Promise.reject(error);
          }
          if (counter >= responses!.length) {
            return Promise.resolve({done: true, value: undefined});
          }
          return Promise.resolve({done: false, value: responses![counter++]});
        },
      };
    },
  };
  return sinon.stub().returns(asyncIterable);
}

describe('v1.WeatherClient', () => {
  describe('Common methods', () => {
    it('has apiEndpoint', () => {
      const client = new weatherModule.v1.WeatherClient();
      const apiEndpoint = client.apiEndpoint;
      assert.strictEqual(apiEndpoint, 'weather.googleapis.com');
    });

    it('has universeDomain', () => {
      const client = new weatherModule.v1.WeatherClient();
      const universeDomain = client.universeDomain;
      assert.strictEqual(universeDomain, 'googleapis.com');
    });

    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      it('throws DeprecationWarning if static servicePath is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const servicePath = weatherModule.v1.WeatherClient.servicePath;
        assert.strictEqual(servicePath, 'weather.googleapis.com');
        assert(stub.called);
        stub.restore();
      });

      it('throws DeprecationWarning if static apiEndpoint is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const apiEndpoint = weatherModule.v1.WeatherClient.apiEndpoint;
        assert.strictEqual(apiEndpoint, 'weather.googleapis.com');
        assert(stub.called);
        stub.restore();
      });
    }
    it('sets apiEndpoint according to universe domain camelCase', () => {
      const client = new weatherModule.v1.WeatherClient({
        universeDomain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'weather.example.com');
    });

    it('sets apiEndpoint according to universe domain snakeCase', () => {
      const client = new weatherModule.v1.WeatherClient({
        universe_domain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'weather.example.com');
    });

    if (typeof process === 'object' && 'env' in process) {
      describe('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
        it('sets apiEndpoint from environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new weatherModule.v1.WeatherClient();
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'weather.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });

        it('value configured in code has priority over environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new weatherModule.v1.WeatherClient({
            universeDomain: 'configured.example.com',
          });
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'weather.configured.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });
      });
    }
    it('does not allow setting both universeDomain and universe_domain', () => {
      assert.throws(() => {
        new weatherModule.v1.WeatherClient({
          universe_domain: 'example.com',
          universeDomain: 'example.net',
        });
      });
    });

    it('has port', () => {
      const port = weatherModule.v1.WeatherClient.port;
      assert(port);
      assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
      const client = new weatherModule.v1.WeatherClient();
      assert(client);
    });

    it('should create a client with gRPC fallback', () => {
      const client = new weatherModule.v1.WeatherClient({
        fallback: true,
      });
      assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.weatherStub, undefined);
      await client.initialize();
      assert(client.weatherStub);
    });

    it('has close method for the initialized client', done => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      assert(client.weatherStub);
      client.close().then(() => {
        done();
      });
    });

    it('has close method for the non-initialized client', done => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.weatherStub, undefined);
      client.close().then(() => {
        done();
      });
    });

    it('has getProjectId method', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
      const result = await client.getProjectId();
      assert.strictEqual(result, fakeProjectId);
      assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon
        .stub()
        .callsArgWith(0, null, fakeProjectId);
      const promise = new Promise((resolve, reject) => {
        client.getProjectId((err?: Error | null, projectId?: string | null) => {
          if (err) {
            reject(err);
          } else {
            resolve(projectId);
          }
        });
      });
      const result = await promise;
      assert.strictEqual(result, fakeProjectId);
    });
  });

  describe('lookupCurrentConditions', () => {
    it('invokes lookupCurrentConditions without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsResponse()
      );
      client.innerApiCalls.lookupCurrentConditions =
        stubSimpleCall(expectedResponse);
      const [response] = await client.lookupCurrentConditions(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupCurrentConditions without error using callback', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsRequest()
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsResponse()
      );
      client.innerApiCalls.lookupCurrentConditions =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.lookupCurrentConditions(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.weather.v1.ILookupCurrentConditionsResponse | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupCurrentConditions with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.lookupCurrentConditions = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(
        client.lookupCurrentConditions(request),
        expectedError
      );
    });

    it('invokes lookupCurrentConditions with closed client', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupCurrentConditionsRequest()
      );
      const expectedError = new Error('The client has already been closed.');
      client.close();
      await assert.rejects(
        client.lookupCurrentConditions(request),
        expectedError
      );
    });
  });

  describe('lookupForecastHours', () => {
    it('invokes lookupForecastHours without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
      ];
      client.innerApiCalls.lookupForecastHours =
        stubSimpleCall(expectedResponse);
      const [response] = await client.lookupForecastHours(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupForecastHours without error using callback', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
      ];
      client.innerApiCalls.lookupForecastHours =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.lookupForecastHours(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.weather.v1.IForecastHour[] | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupForecastHours with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.lookupForecastHours = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.lookupForecastHours(request), expectedError);
    });

    it('invokes lookupForecastHoursStream without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
      ];
      client.descriptors.page.lookupForecastHours.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.lookupForecastHoursStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.ForecastHour[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.ForecastHour) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.lookupForecastHours.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupForecastHours, request)
      );
    });

    it('invokes lookupForecastHoursStream with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupForecastHours.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.lookupForecastHoursStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.ForecastHour[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.ForecastHour) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.lookupForecastHours.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupForecastHours, request)
      );
    });

    it('uses async iteration with lookupForecastHours without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastHour()),
      ];
      client.descriptors.page.lookupForecastHours.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.maps.weather.v1.IForecastHour[] = [];
      const iterable = client.lookupForecastHoursAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupForecastHours.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });

    it('uses async iteration with lookupForecastHours with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastHoursRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupForecastHours.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.lookupForecastHoursAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.maps.weather.v1.IForecastHour[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupForecastHours.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });
  });

  describe('lookupForecastDays', () => {
    it('invokes lookupForecastDays without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
      ];
      client.innerApiCalls.lookupForecastDays =
        stubSimpleCall(expectedResponse);
      const [response] = await client.lookupForecastDays(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupForecastDays without error using callback', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
      ];
      client.innerApiCalls.lookupForecastDays =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.lookupForecastDays(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.weather.v1.IForecastDay[] | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupForecastDays with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.lookupForecastDays = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.lookupForecastDays(request), expectedError);
    });

    it('invokes lookupForecastDaysStream without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
      ];
      client.descriptors.page.lookupForecastDays.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.lookupForecastDaysStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.ForecastDay[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.ForecastDay) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.lookupForecastDays.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupForecastDays, request)
      );
    });

    it('invokes lookupForecastDaysStream with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupForecastDays.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.lookupForecastDaysStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.ForecastDay[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.ForecastDay) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.lookupForecastDays.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupForecastDays, request)
      );
    });

    it('uses async iteration with lookupForecastDays without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
        generateSampleMessage(new protos.google.maps.weather.v1.ForecastDay()),
      ];
      client.descriptors.page.lookupForecastDays.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.maps.weather.v1.IForecastDay[] = [];
      const iterable = client.lookupForecastDaysAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupForecastDays.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });

    it('uses async iteration with lookupForecastDays with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupForecastDaysRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupForecastDays.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.lookupForecastDaysAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.maps.weather.v1.IForecastDay[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupForecastDays.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });
  });

  describe('lookupHistoryHours', () => {
    it('invokes lookupHistoryHours without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
      ];
      client.innerApiCalls.lookupHistoryHours =
        stubSimpleCall(expectedResponse);
      const [response] = await client.lookupHistoryHours(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupHistoryHours without error using callback', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
      ];
      client.innerApiCalls.lookupHistoryHours =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.lookupHistoryHours(
          request,
          (
            err?: Error | null,
            result?: protos.google.maps.weather.v1.IHistoryHour[] | null
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          }
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes lookupHistoryHours with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.lookupHistoryHours = stubSimpleCall(
        undefined,
        expectedError
      );
      await assert.rejects(client.lookupHistoryHours(request), expectedError);
    });

    it('invokes lookupHistoryHoursStream without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
      ];
      client.descriptors.page.lookupHistoryHours.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.lookupHistoryHoursStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.HistoryHour[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.HistoryHour) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.lookupHistoryHours.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupHistoryHours, request)
      );
    });

    it('invokes lookupHistoryHoursStream with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupHistoryHours.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.lookupHistoryHoursStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.maps.weather.v1.HistoryHour[] = [];
        stream.on(
          'data',
          (response: protos.google.maps.weather.v1.HistoryHour) => {
            responses.push(response);
          }
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.lookupHistoryHours.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.lookupHistoryHours, request)
      );
    });

    it('uses async iteration with lookupHistoryHours without error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedResponse = [
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
        generateSampleMessage(new protos.google.maps.weather.v1.HistoryHour()),
      ];
      client.descriptors.page.lookupHistoryHours.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.maps.weather.v1.IHistoryHour[] = [];
      const iterable = client.lookupHistoryHoursAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupHistoryHours.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });

    it('uses async iteration with lookupHistoryHours with error', async () => {
      const client = new weatherModule.v1.WeatherClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize();
      const request = generateSampleMessage(
        new protos.google.maps.weather.v1.LookupHistoryHoursRequest()
      );
      const expectedError = new Error('expected');
      client.descriptors.page.lookupHistoryHours.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.lookupHistoryHoursAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.maps.weather.v1.IHistoryHour[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.lookupHistoryHours.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request
      );
    });
  });
});
