"use strict";
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **
Object.defineProperty(exports, "__esModule", { value: true });
const protos = require("../protos/protos");
const assert = require("assert");
const sinon = require("sinon");
const mocha_1 = require("mocha");
const spannerexecutorproxyModule = require("../src");
const stream_1 = require("stream");
const google_gax_1 = require("google-gax");
// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = google_gax_1.protobuf.Root.fromJSON(require('../protos/protos.json')).resolveAll();
// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName, fields) {
    let type = root.lookupType(typeName);
    for (const field of fields.slice(0, -1)) {
        type = type.fields[field]?.resolvedType;
    }
    return type.fields[fields[fields.length - 1]]?.defaultValue;
}
function generateSampleMessage(instance) {
    const filledObject = instance.constructor.toObject(instance, { defaults: true });
    return instance.constructor.fromObject(filledObject);
}
function stubBidiStreamingCall(response, error) {
    const transformStub = error
        ? sinon.stub().callsArgWith(2, error)
        : sinon.stub().callsArgWith(2, null, response);
    const mockStream = new stream_1.PassThrough({
        objectMode: true,
        transform: transformStub,
    });
    return sinon.stub().returns(mockStream);
}
(0, mocha_1.describe)('v1.SpannerExecutorProxyClient', () => {
    (0, mocha_1.describe)('Common methods', () => {
        (0, mocha_1.it)('has apiEndpoint', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient();
            const apiEndpoint = client.apiEndpoint;
            assert.strictEqual(apiEndpoint, 'spanner-cloud-executor.googleapis.com');
        });
        (0, mocha_1.it)('has universeDomain', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient();
            const universeDomain = client.universeDomain;
            assert.strictEqual(universeDomain, 'googleapis.com');
        });
        if (typeof process === 'object' &&
            typeof process.emitWarning === 'function') {
            (0, mocha_1.it)('throws DeprecationWarning if static servicePath is used', () => {
                const stub = sinon.stub(process, 'emitWarning');
                const servicePath = spannerexecutorproxyModule.v1.SpannerExecutorProxyClient.servicePath;
                assert.strictEqual(servicePath, 'spanner-cloud-executor.googleapis.com');
                assert(stub.called);
                stub.restore();
            });
            (0, mocha_1.it)('throws DeprecationWarning if static apiEndpoint is used', () => {
                const stub = sinon.stub(process, 'emitWarning');
                const apiEndpoint = spannerexecutorproxyModule.v1.SpannerExecutorProxyClient.apiEndpoint;
                assert.strictEqual(apiEndpoint, 'spanner-cloud-executor.googleapis.com');
                assert(stub.called);
                stub.restore();
            });
        }
        (0, mocha_1.it)('sets apiEndpoint according to universe domain camelCase', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                universeDomain: 'example.com',
            });
            const servicePath = client.apiEndpoint;
            assert.strictEqual(servicePath, 'spanner-cloud-executor.example.com');
        });
        (0, mocha_1.it)('sets apiEndpoint according to universe domain snakeCase', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                universe_domain: 'example.com',
            });
            const servicePath = client.apiEndpoint;
            assert.strictEqual(servicePath, 'spanner-cloud-executor.example.com');
        });
        if (typeof process === 'object' && 'env' in process) {
            (0, mocha_1.describe)('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
                (0, mocha_1.it)('sets apiEndpoint from environment variable', () => {
                    const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
                    process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
                    const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient();
                    const servicePath = client.apiEndpoint;
                    assert.strictEqual(servicePath, 'spanner-cloud-executor.example.com');
                    if (saved) {
                        process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
                    }
                    else {
                        delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
                    }
                });
                (0, mocha_1.it)('value configured in code has priority over environment variable', () => {
                    const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
                    process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
                    const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                        universeDomain: 'configured.example.com',
                    });
                    const servicePath = client.apiEndpoint;
                    assert.strictEqual(servicePath, 'spanner-cloud-executor.configured.example.com');
                    if (saved) {
                        process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
                    }
                    else {
                        delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
                    }
                });
            });
        }
        (0, mocha_1.it)('does not allow setting both universeDomain and universe_domain', () => {
            assert.throws(() => {
                new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                    universe_domain: 'example.com',
                    universeDomain: 'example.net',
                });
            });
        });
        (0, mocha_1.it)('has port', () => {
            const port = spannerexecutorproxyModule.v1.SpannerExecutorProxyClient.port;
            assert(port);
            assert(typeof port === 'number');
        });
        (0, mocha_1.it)('should create a client with no option', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient();
            assert(client);
        });
        (0, mocha_1.it)('should create a client with gRPC fallback', () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                fallback: true,
            });
            assert(client);
        });
        (0, mocha_1.it)('has initialize method and supports deferred initialization', async () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            assert.strictEqual(client.spannerExecutorProxyStub, undefined);
            await client.initialize();
            assert(client.spannerExecutorProxyStub);
        });
        (0, mocha_1.it)('has close method for the initialized client', done => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            client.initialize().catch(err => {
                throw err;
            });
            assert(client.spannerExecutorProxyStub);
            client
                .close()
                .then(() => {
                done();
            })
                .catch(err => {
                throw err;
            });
        });
        (0, mocha_1.it)('has close method for the non-initialized client', done => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            assert.strictEqual(client.spannerExecutorProxyStub, undefined);
            client
                .close()
                .then(() => {
                done();
            })
                .catch(err => {
                throw err;
            });
        });
        (0, mocha_1.it)('has getProjectId method', async () => {
            const fakeProjectId = 'fake-project-id';
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
            const result = await client.getProjectId();
            assert.strictEqual(result, fakeProjectId);
            assert(client.auth.getProjectId.calledWithExactly());
        });
        (0, mocha_1.it)('has getProjectId method with callback', async () => {
            const fakeProjectId = 'fake-project-id';
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            client.auth.getProjectId = sinon
                .stub()
                .callsArgWith(0, null, fakeProjectId);
            const promise = new Promise((resolve, reject) => {
                client.getProjectId((err, projectId) => {
                    if (err) {
                        reject(err);
                    }
                    else {
                        resolve(projectId);
                    }
                });
            });
            const result = await promise;
            assert.strictEqual(result, fakeProjectId);
        });
    });
    (0, mocha_1.describe)('executeActionAsync', () => {
        (0, mocha_1.it)('invokes executeActionAsync without error', async () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            const request = generateSampleMessage(new protos.google.spanner.executor.v1.SpannerAsyncActionRequest());
            const expectedResponse = generateSampleMessage(new protos.google.spanner.executor.v1.SpannerAsyncActionResponse());
            client.innerApiCalls.executeActionAsync =
                stubBidiStreamingCall(expectedResponse);
            const stream = client.executeActionAsync();
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response) => {
                    resolve(response);
                });
                stream.on('error', (err) => {
                    reject(err);
                });
                stream.write(request);
                stream.end();
            });
            const response = await promise;
            assert.deepStrictEqual(response, expectedResponse);
            assert(client.innerApiCalls.executeActionAsync
                .getCall(0)
                .calledWith(null));
            assert.deepStrictEqual(stream._transform.getCall(0)
                .args[0], request);
        });
        (0, mocha_1.it)('invokes executeActionAsync with error', async () => {
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            const request = generateSampleMessage(new protos.google.spanner.executor.v1.SpannerAsyncActionRequest());
            const expectedError = new Error('expected');
            client.innerApiCalls.executeActionAsync = stubBidiStreamingCall(undefined, expectedError);
            const stream = client.executeActionAsync();
            const promise = new Promise((resolve, reject) => {
                stream.on('data', (response) => {
                    resolve(response);
                });
                stream.on('error', (err) => {
                    reject(err);
                });
                stream.write(request);
                stream.end();
            });
            await assert.rejects(promise, expectedError);
            assert(client.innerApiCalls.executeActionAsync
                .getCall(0)
                .calledWith(null));
            assert.deepStrictEqual(stream._transform.getCall(0)
                .args[0], request);
        });
    });
    (0, mocha_1.describe)('Path templates', () => {
        (0, mocha_1.describe)('backup', async () => {
            const fakePath = '/rendered/path/backup';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                backup: 'backupValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.backupPathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.backupPathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('backupPath', () => {
                const result = client.backupPath('projectValue', 'instanceValue', 'backupValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.backupPathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromBackupName', () => {
                const result = client.matchProjectFromBackupName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.backupPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromBackupName', () => {
                const result = client.matchInstanceFromBackupName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.backupPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchBackupFromBackupName', () => {
                const result = client.matchBackupFromBackupName(fakePath);
                assert.strictEqual(result, 'backupValue');
                assert(client.pathTemplates.backupPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('backupSchedule', async () => {
            const fakePath = '/rendered/path/backupSchedule';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                database: 'databaseValue',
                schedule: 'scheduleValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.backupSchedulePathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.backupSchedulePathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('backupSchedulePath', () => {
                const result = client.backupSchedulePath('projectValue', 'instanceValue', 'databaseValue', 'scheduleValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.backupSchedulePathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromBackupScheduleName', () => {
                const result = client.matchProjectFromBackupScheduleName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.backupSchedulePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromBackupScheduleName', () => {
                const result = client.matchInstanceFromBackupScheduleName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.backupSchedulePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchDatabaseFromBackupScheduleName', () => {
                const result = client.matchDatabaseFromBackupScheduleName(fakePath);
                assert.strictEqual(result, 'databaseValue');
                assert(client.pathTemplates.backupSchedulePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchScheduleFromBackupScheduleName', () => {
                const result = client.matchScheduleFromBackupScheduleName(fakePath);
                assert.strictEqual(result, 'scheduleValue');
                assert(client.pathTemplates.backupSchedulePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('database', async () => {
            const fakePath = '/rendered/path/database';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                database: 'databaseValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.databasePathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.databasePathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('databasePath', () => {
                const result = client.databasePath('projectValue', 'instanceValue', 'databaseValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.databasePathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromDatabaseName', () => {
                const result = client.matchProjectFromDatabaseName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.databasePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromDatabaseName', () => {
                const result = client.matchInstanceFromDatabaseName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.databasePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchDatabaseFromDatabaseName', () => {
                const result = client.matchDatabaseFromDatabaseName(fakePath);
                assert.strictEqual(result, 'databaseValue');
                assert(client.pathTemplates.databasePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('databaseRole', async () => {
            const fakePath = '/rendered/path/databaseRole';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                database: 'databaseValue',
                role: 'roleValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.databaseRolePathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.databaseRolePathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('databaseRolePath', () => {
                const result = client.databaseRolePath('projectValue', 'instanceValue', 'databaseValue', 'roleValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.databaseRolePathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromDatabaseRoleName', () => {
                const result = client.matchProjectFromDatabaseRoleName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.databaseRolePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromDatabaseRoleName', () => {
                const result = client.matchInstanceFromDatabaseRoleName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.databaseRolePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchDatabaseFromDatabaseRoleName', () => {
                const result = client.matchDatabaseFromDatabaseRoleName(fakePath);
                assert.strictEqual(result, 'databaseValue');
                assert(client.pathTemplates.databaseRolePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchRoleFromDatabaseRoleName', () => {
                const result = client.matchRoleFromDatabaseRoleName(fakePath);
                assert.strictEqual(result, 'roleValue');
                assert(client.pathTemplates.databaseRolePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('instance', async () => {
            const fakePath = '/rendered/path/instance';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.instancePathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.instancePathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('instancePath', () => {
                const result = client.instancePath('projectValue', 'instanceValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.instancePathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromInstanceName', () => {
                const result = client.matchProjectFromInstanceName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.instancePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromInstanceName', () => {
                const result = client.matchInstanceFromInstanceName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.instancePathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('instanceConfig', async () => {
            const fakePath = '/rendered/path/instanceConfig';
            const expectedParameters = {
                project: 'projectValue',
                instance_config: 'instanceConfigValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.instanceConfigPathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.instanceConfigPathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('instanceConfigPath', () => {
                const result = client.instanceConfigPath('projectValue', 'instanceConfigValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.instanceConfigPathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromInstanceConfigName', () => {
                const result = client.matchProjectFromInstanceConfigName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.instanceConfigPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceConfigFromInstanceConfigName', () => {
                const result = client.matchInstanceConfigFromInstanceConfigName(fakePath);
                assert.strictEqual(result, 'instanceConfigValue');
                assert(client.pathTemplates.instanceConfigPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('instancePartition', async () => {
            const fakePath = '/rendered/path/instancePartition';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                instance_partition: 'instancePartitionValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.instancePartitionPathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.instancePartitionPathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('instancePartitionPath', () => {
                const result = client.instancePartitionPath('projectValue', 'instanceValue', 'instancePartitionValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.instancePartitionPathTemplate
                    .render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromInstancePartitionName', () => {
                const result = client.matchProjectFromInstancePartitionName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.instancePartitionPathTemplate
                    .match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromInstancePartitionName', () => {
                const result = client.matchInstanceFromInstancePartitionName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.instancePartitionPathTemplate
                    .match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstancePartitionFromInstancePartitionName', () => {
                const result = client.matchInstancePartitionFromInstancePartitionName(fakePath);
                assert.strictEqual(result, 'instancePartitionValue');
                assert(client.pathTemplates.instancePartitionPathTemplate
                    .match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
        (0, mocha_1.describe)('session', async () => {
            const fakePath = '/rendered/path/session';
            const expectedParameters = {
                project: 'projectValue',
                instance: 'instanceValue',
                database: 'databaseValue',
                session: 'sessionValue',
            };
            const client = new spannerexecutorproxyModule.v1.SpannerExecutorProxyClient({
                credentials: { client_email: 'bogus', private_key: 'bogus' },
                projectId: 'bogus',
            });
            await client.initialize();
            client.pathTemplates.sessionPathTemplate.render = sinon
                .stub()
                .returns(fakePath);
            client.pathTemplates.sessionPathTemplate.match = sinon
                .stub()
                .returns(expectedParameters);
            (0, mocha_1.it)('sessionPath', () => {
                const result = client.sessionPath('projectValue', 'instanceValue', 'databaseValue', 'sessionValue');
                assert.strictEqual(result, fakePath);
                assert(client.pathTemplates.sessionPathTemplate.render
                    .getCall(-1)
                    .calledWith(expectedParameters));
            });
            (0, mocha_1.it)('matchProjectFromSessionName', () => {
                const result = client.matchProjectFromSessionName(fakePath);
                assert.strictEqual(result, 'projectValue');
                assert(client.pathTemplates.sessionPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchInstanceFromSessionName', () => {
                const result = client.matchInstanceFromSessionName(fakePath);
                assert.strictEqual(result, 'instanceValue');
                assert(client.pathTemplates.sessionPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchDatabaseFromSessionName', () => {
                const result = client.matchDatabaseFromSessionName(fakePath);
                assert.strictEqual(result, 'databaseValue');
                assert(client.pathTemplates.sessionPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
            (0, mocha_1.it)('matchSessionFromSessionName', () => {
                const result = client.matchSessionFromSessionName(fakePath);
                assert.strictEqual(result, 'sessionValue');
                assert(client.pathTemplates.sessionPathTemplate.match
                    .getCall(-1)
                    .calledWith(fakePath));
            });
        });
    });
});
//# sourceMappingURL=gapic_spanner_executor_proxy_v1.js.map