// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import type * as gax from 'google-gax';
import type {
  Callback,
  CallOptions,
  Descriptors,
  ClientOptions,
  PaginationCallback,
  GaxCall,
  LocationsClient,
  LocationProtos,
} from 'google-gax';
import { Transform } from 'stream';
import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');
import { loggingUtils as logging, decodeAnyProtosInArray } from 'google-gax';

/**
 * Client JSON configuration object, loaded from
 * `src/v1/app_topology_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './app_topology_client_config.json';
const version = require('../../../package.json').version;

/**
 *  Service describing handlers for resources
 * @class
 * @memberof v1
 */
export class AppTopologyClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _providedCustomServicePath: boolean;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: { [method: string]: gax.CallSettings };
  private _universeDomain: string;
  private _servicePath: string;
  private _log = logging.log('apptopology');

  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  warn: (code: string, message: string, warnType?: string) => void;
  innerApiCalls: { [name: string]: Function };
  locationsClient: LocationsClient;
  pathTemplates: { [name: string]: gax.PathTemplate };
  appTopologyStub?: Promise<{ [name: string]: Function }>;

  /**
   * Construct an instance of AppTopologyClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://cloud.google.com/docs/authentication/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP/1.1 REST mode.
   *     For more information, please check the
   *     {@link https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#http11-rest-api-mode documentation}.
   * @param {gax} [gaxInstance]: loaded instance of `google-gax`. Useful if you
   *     need to avoid loading the default gRPC version and want to use the fallback
   *     HTTP implementation. Load only fallback version and pass it to the constructor:
   *     ```
   *     const gax = require('google-gax/build/src/fallback'); // avoids loading google-gax with gRPC
   *     const client = new AppTopologyClient({fallback: true}, gax);
   *     ```
   */
  constructor(
    opts?: ClientOptions,
    gaxInstance?: typeof gax | typeof gax.fallback,
  ) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof AppTopologyClient;
    if (
      opts?.universe_domain &&
      opts?.universeDomain &&
      opts?.universe_domain !== opts?.universeDomain
    ) {
      throw new Error(
        'Please set either universe_domain or universeDomain, but not both.',
      );
    }
    const universeDomainEnvVar =
      typeof process === 'object' && typeof process.env === 'object'
        ? process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN']
        : undefined;
    this._universeDomain =
      opts?.universeDomain ??
      opts?.universe_domain ??
      universeDomainEnvVar ??
      'googleapis.com';
    this._servicePath = 'apptopology.' + this._universeDomain;
    const servicePath =
      opts?.servicePath || opts?.apiEndpoint || this._servicePath;
    this._providedCustomServicePath = !!(
      opts?.servicePath || opts?.apiEndpoint
    );
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback =
      opts?.fallback ??
      (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({ servicePath, port, clientConfig, fallback }, opts);

    // Request numeric enum values if REST transport is used.
    opts.numericEnums = true;

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== this._servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Load google-gax module synchronously if needed
    if (!gaxInstance) {
      gaxInstance = require('google-gax') as typeof gax;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gaxInstance.fallback : gaxInstance;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = this._gaxGrpc.auth as gax.GoogleAuth;

    // Set useJWTAccessWithScope on the auth object.
    this.auth.useJWTAccessWithScope = true;

    // Set defaultServicePath on the auth object.
    this.auth.defaultServicePath = this._servicePath;

    // Set the default scopes in auth client if needed.
    if (servicePath === this._servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }
    this.locationsClient = new this._gaxModule.LocationsClient(
      this._gaxGrpc,
      opts,
    );

    // Determine the client header string.
    const clientHeader = [`gax/${this._gaxModule.version}`, `gapic/${version}`];
    if (typeof process === 'object' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // This API contains "path templates"; forward-slash-separated
    // identifiers to uniquely identify resources within the API.
    // Create useful helper objects for these.
    this.pathTemplates = {
      discoveredResourcesTopologyPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/discoveredResourcesTopology',
      ),
      domainPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/domains/{domain}',
      ),
      locationPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}',
      ),
      projectPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}',
      ),
      schemaPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/domains/{domain}/schema',
      ),
    };

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      listDomains: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'domains',
      ),
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
      'google.cloud.apptopology.v1.AppTopology',
      gapicConfig as gax.ClientConfig,
      opts.clientConfig || {},
      { 'x-goog-api-client': clientHeader.join(' ') },
    );

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};

    // Add a warn function to the client constructor so it can be easily tested.
    this.warn = this._gaxModule.warn;
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.appTopologyStub) {
      return this.appTopologyStub;
    }

    // Put together the "service stub" for
    // google.cloud.apptopology.v1.AppTopology.
    this.appTopologyStub = this._gaxGrpc.createStub(
      this._opts.fallback
        ? (this._protos as protobuf.Root).lookupService(
            'google.cloud.apptopology.v1.AppTopology',
          )
        : // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.cloud.apptopology.v1.AppTopology,
      this._opts,
      this._providedCustomServicePath,
    ) as Promise<{ [method: string]: Function }>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const appTopologyStubMethods = [
      'generateDiscoveredResourcesTopology',
      'getSchema',
      'getDomain',
      'listDomains',
    ];
    for (const methodName of appTopologyStubMethods) {
      const callPromise = this.appTopologyStub.then(
        (stub) =>
          (...args: Array<{}>) => {
            if (this._terminated) {
              return Promise.reject('The client has already been closed.');
            }
            const func = stub[methodName];
            return func.apply(stub, args);
          },
        (err: Error | null | undefined) => () => {
          throw err;
        },
      );

      const descriptor = this.descriptors.page[methodName] || undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor,
        this._opts.fallback,
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.appTopologyStub;
  }

  /**
   * The DNS address for this API service.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static servicePath is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'apptopology.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static apiEndpoint is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'apptopology.googleapis.com';
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  get apiEndpoint() {
    return this._servicePath;
  }

  get universeDomain() {
    return this._universeDomain;
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return [
      'https://www.googleapis.com/auth/apptopology.read-only',
      'https://www.googleapis.com/auth/apptopology.read-write',
      'https://www.googleapis.com/auth/cloud-platform',
    ];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(
    callback?: Callback<string, undefined, undefined>,
  ): Promise<string> | void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  /**
   * Generate the topology for all resources in the given project. If the
   * project represents an
   * [app
   * boundary](https://cloud.google.com/app-hub/docs/reference/rest/v1/Boundary),
   * the topology is generated for all resources in the boundary.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.cloud.apptopology.v1.GraphPattern} request.filter
   *   Filters for the topology nodes and edges; Detail format see GraphPattern
   *   proto. A separate 'LookupSchema' method will be added that will return
   *   the necessary information to be able to construct these filters.
   * @param {string} request.name
   *   Required. The project to query discoverable resources on.
   *   Expected format:
   *   `projects/{project}/locations/{location}/discoveredResourcesTopology`.
   *   Only `global` location is supported.
   * @param {string[]} request.topologyDomains
   *   Required. The full resource name of the domain of the app topology.
   *   Format: `projects/{project}/locations/{location}/domains/{domain}`
   *   Caller must have apptopology.domains.get permission on each of the domains.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.apptopology.v1.GenerateDiscoveredResourcesTopologyResponse|GenerateDiscoveredResourcesTopologyResponse}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/app_topology.generate_discovered_resources_topology.js</caption>
   * region_tag:apptopology_v1_generated_AppTopology_GenerateDiscoveredResourcesTopology_async
   */
  generateDiscoveredResourcesTopology(
    request?: protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
      (
        | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
        | undefined
      ),
      {} | undefined,
    ]
  >;
  generateDiscoveredResourcesTopology(
    request: protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
      | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  generateDiscoveredResourcesTopology(
    request: protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest,
    callback: Callback<
      protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
      | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  generateDiscoveredResourcesTopology(
    request?: protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
          | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
      | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
      (
        | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
        | undefined
      ),
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('generateDiscoveredResourcesTopology request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
          | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info(
            'generateDiscoveredResourcesTopology response %j',
            response,
          );
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .generateDiscoveredResourcesTopology(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyResponse,
          (
            | protos.google.cloud.apptopology.v1.IGenerateDiscoveredResourcesTopologyRequest
            | undefined
          ),
          {} | undefined,
        ]) => {
          this._log.info(
            'generateDiscoveredResourcesTopology response %j',
            response,
          );
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Retrieves the schema for the specified topology domain. The schema
   * defines the NodeTypes and EdgeTypes that are supported in
   * GenerateDiscoveredResourcesTopology requests and responses for a given
   * domain.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the singleton domain schema resource.
   *   Format: `projects/{project}/locations/{location}/domains/{domain}/schema`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.apptopology.v1.Schema|Schema}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/app_topology.get_schema.js</caption>
   * region_tag:apptopology_v1_generated_AppTopology_GetSchema_async
   */
  getSchema(
    request?: protos.google.cloud.apptopology.v1.IGetSchemaRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.ISchema,
      protos.google.cloud.apptopology.v1.IGetSchemaRequest | undefined,
      {} | undefined,
    ]
  >;
  getSchema(
    request: protos.google.cloud.apptopology.v1.IGetSchemaRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.apptopology.v1.ISchema,
      protos.google.cloud.apptopology.v1.IGetSchemaRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getSchema(
    request: protos.google.cloud.apptopology.v1.IGetSchemaRequest,
    callback: Callback<
      protos.google.cloud.apptopology.v1.ISchema,
      protos.google.cloud.apptopology.v1.IGetSchemaRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getSchema(
    request?: protos.google.cloud.apptopology.v1.IGetSchemaRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.apptopology.v1.ISchema,
          | protos.google.cloud.apptopology.v1.IGetSchemaRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.apptopology.v1.ISchema,
      protos.google.cloud.apptopology.v1.IGetSchemaRequest | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.ISchema,
      protos.google.cloud.apptopology.v1.IGetSchemaRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getSchema request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.apptopology.v1.ISchema,
          | protos.google.cloud.apptopology.v1.IGetSchemaRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getSchema response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getSchema(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.apptopology.v1.ISchema,
          protos.google.cloud.apptopology.v1.IGetSchemaRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getSchema response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Retrieves the specified topology domain.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the domain to retrieve.
   *   Format: `projects/{project}/locations/{location}/domains/{domain}`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.apptopology.v1.Domain|Domain}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/app_topology.get_domain.js</caption>
   * region_tag:apptopology_v1_generated_AppTopology_GetDomain_async
   */
  getDomain(
    request?: protos.google.cloud.apptopology.v1.IGetDomainRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IDomain,
      protos.google.cloud.apptopology.v1.IGetDomainRequest | undefined,
      {} | undefined,
    ]
  >;
  getDomain(
    request: protos.google.cloud.apptopology.v1.IGetDomainRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.apptopology.v1.IDomain,
      protos.google.cloud.apptopology.v1.IGetDomainRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getDomain(
    request: protos.google.cloud.apptopology.v1.IGetDomainRequest,
    callback: Callback<
      protos.google.cloud.apptopology.v1.IDomain,
      protos.google.cloud.apptopology.v1.IGetDomainRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getDomain(
    request?: protos.google.cloud.apptopology.v1.IGetDomainRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.apptopology.v1.IDomain,
          | protos.google.cloud.apptopology.v1.IGetDomainRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.apptopology.v1.IDomain,
      protos.google.cloud.apptopology.v1.IGetDomainRequest | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IDomain,
      protos.google.cloud.apptopology.v1.IGetDomainRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getDomain request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.apptopology.v1.IDomain,
          | protos.google.cloud.apptopology.v1.IGetDomainRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getDomain response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getDomain(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.apptopology.v1.IDomain,
          protos.google.cloud.apptopology.v1.IGetDomainRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getDomain response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }

  /**
   * Lists the topology domains available in a specific location.
   * Only `global` location is supported.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The parent location to list domains for.
   *   Format: `projects/{project}/locations/{location}`
   *   Only `global` location is supported.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of domains to return. The service may return
   *   fewer than this value. If unspecified, at most 50 domains will be returned.
   *   The maximum value is 1000; values above 1000 will be coerced to 1000.
   * @param {string} [request.pageToken]
   *   Optional. A page token, received from a previous `ListDomains` call.
   *   Provide this to retrieve the subsequent page.
   *
   *   When paginating, all other parameters provided to `ListDomains` must match
   *   the call that provided the page token.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.apptopology.v1.Domain|Domain}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listDomainsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listDomains(
    request?: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IDomain[],
      protos.google.cloud.apptopology.v1.IListDomainsRequest | null,
      protos.google.cloud.apptopology.v1.IListDomainsResponse,
    ]
  >;
  listDomains(
    request: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.apptopology.v1.IListDomainsRequest,
      | protos.google.cloud.apptopology.v1.IListDomainsResponse
      | null
      | undefined,
      protos.google.cloud.apptopology.v1.IDomain
    >,
  ): void;
  listDomains(
    request: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    callback: PaginationCallback<
      protos.google.cloud.apptopology.v1.IListDomainsRequest,
      | protos.google.cloud.apptopology.v1.IListDomainsResponse
      | null
      | undefined,
      protos.google.cloud.apptopology.v1.IDomain
    >,
  ): void;
  listDomains(
    request?: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.apptopology.v1.IListDomainsRequest,
          | protos.google.cloud.apptopology.v1.IListDomainsResponse
          | null
          | undefined,
          protos.google.cloud.apptopology.v1.IDomain
        >,
    callback?: PaginationCallback<
      protos.google.cloud.apptopology.v1.IListDomainsRequest,
      | protos.google.cloud.apptopology.v1.IListDomainsResponse
      | null
      | undefined,
      protos.google.cloud.apptopology.v1.IDomain
    >,
  ): Promise<
    [
      protos.google.cloud.apptopology.v1.IDomain[],
      protos.google.cloud.apptopology.v1.IListDomainsRequest | null,
      protos.google.cloud.apptopology.v1.IListDomainsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.apptopology.v1.IListDomainsRequest,
          | protos.google.cloud.apptopology.v1.IListDomainsResponse
          | null
          | undefined,
          protos.google.cloud.apptopology.v1.IDomain
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listDomains values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listDomains request %j', request);
    return this.innerApiCalls
      .listDomains(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.apptopology.v1.IDomain[],
          protos.google.cloud.apptopology.v1.IListDomainsRequest | null,
          protos.google.cloud.apptopology.v1.IListDomainsResponse,
        ]) => {
          this._log.info('listDomains values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listDomains`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The parent location to list domains for.
   *   Format: `projects/{project}/locations/{location}`
   *   Only `global` location is supported.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of domains to return. The service may return
   *   fewer than this value. If unspecified, at most 50 domains will be returned.
   *   The maximum value is 1000; values above 1000 will be coerced to 1000.
   * @param {string} [request.pageToken]
   *   Optional. A page token, received from a previous `ListDomains` call.
   *   Provide this to retrieve the subsequent page.
   *
   *   When paginating, all other parameters provided to `ListDomains` must match
   *   the call that provided the page token.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.apptopology.v1.Domain|Domain} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listDomainsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listDomainsStream(
    request?: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listDomains'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listDomains stream %j', request);
    return this.descriptors.page.listDomains.createStream(
      this.innerApiCalls.listDomains as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listDomains`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The parent location to list domains for.
   *   Format: `projects/{project}/locations/{location}`
   *   Only `global` location is supported.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of domains to return. The service may return
   *   fewer than this value. If unspecified, at most 50 domains will be returned.
   *   The maximum value is 1000; values above 1000 will be coerced to 1000.
   * @param {string} [request.pageToken]
   *   Optional. A page token, received from a previous `ListDomains` call.
   *   Provide this to retrieve the subsequent page.
   *
   *   When paginating, all other parameters provided to `ListDomains` must match
   *   the call that provided the page token.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.apptopology.v1.Domain|Domain}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/app_topology.list_domains.js</caption>
   * region_tag:apptopology_v1_generated_AppTopology_ListDomains_async
   */
  listDomainsAsync(
    request?: protos.google.cloud.apptopology.v1.IListDomainsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.apptopology.v1.IDomain> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listDomains'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listDomains iterate %j', request);
    return this.descriptors.page.listDomains.asyncIterate(
      this.innerApiCalls['listDomains'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.apptopology.v1.IDomain>;
  }

  /**
   * Gets information about a location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Resource name for the location.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html | CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link google.cloud.location.Location | Location}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example
   * ```
   * const [response] = await client.getLocation(request);
   * ```
   */
  getLocation(
    request: LocationProtos.google.cloud.location.IGetLocationRequest,
    options?:
      | gax.CallOptions
      | Callback<
          LocationProtos.google.cloud.location.ILocation,
          | LocationProtos.google.cloud.location.IGetLocationRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LocationProtos.google.cloud.location.ILocation,
      | LocationProtos.google.cloud.location.IGetLocationRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<LocationProtos.google.cloud.location.ILocation> {
    return this.locationsClient.getLocation(request, options, callback);
  }
  /**
   * Lists information about the supported locations for this service. Returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   The resource that owns the locations collection, if applicable.
   * @param {string} request.filter
   *   The standard list filter.
   * @param {number} request.pageSize
   *   The standard list page size.
   * @param {string} request.pageToken
   *   The standard list page token.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link google.cloud.location.Location | Location}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example
   * ```
   * const iterable = client.listLocationsAsync(request);
   * for await (const response of iterable) {
   *   // process response
   * }
   * ```
   */
  listLocationsAsync(
    request: LocationProtos.google.cloud.location.IListLocationsRequest,
    options?: CallOptions,
  ): AsyncIterable<LocationProtos.google.cloud.location.ILocation> {
    return this.locationsClient.listLocationsAsync(request, options);
  }

  // --------------------
  // -- Path templates --
  // --------------------

  /**
   * Return a fully-qualified discoveredResourcesTopology resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @returns {string} Resource name string.
   */
  discoveredResourcesTopologyPath(project: string, location: string) {
    return this.pathTemplates.discoveredResourcesTopologyPathTemplate.render({
      project: project,
      location: location,
    });
  }

  /**
   * Parse the project from DiscoveredResourcesTopology resource.
   *
   * @param {string} discoveredResourcesTopologyName
   *   A fully-qualified path representing DiscoveredResourcesTopology resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromDiscoveredResourcesTopologyName(
    discoveredResourcesTopologyName: string,
  ) {
    return this.pathTemplates.discoveredResourcesTopologyPathTemplate.match(
      discoveredResourcesTopologyName,
    ).project;
  }

  /**
   * Parse the location from DiscoveredResourcesTopology resource.
   *
   * @param {string} discoveredResourcesTopologyName
   *   A fully-qualified path representing DiscoveredResourcesTopology resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromDiscoveredResourcesTopologyName(
    discoveredResourcesTopologyName: string,
  ) {
    return this.pathTemplates.discoveredResourcesTopologyPathTemplate.match(
      discoveredResourcesTopologyName,
    ).location;
  }

  /**
   * Return a fully-qualified domain resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} domain
   * @returns {string} Resource name string.
   */
  domainPath(project: string, location: string, domain: string) {
    return this.pathTemplates.domainPathTemplate.render({
      project: project,
      location: location,
      domain: domain,
    });
  }

  /**
   * Parse the project from Domain resource.
   *
   * @param {string} domainName
   *   A fully-qualified path representing Domain resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromDomainName(domainName: string) {
    return this.pathTemplates.domainPathTemplate.match(domainName).project;
  }

  /**
   * Parse the location from Domain resource.
   *
   * @param {string} domainName
   *   A fully-qualified path representing Domain resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromDomainName(domainName: string) {
    return this.pathTemplates.domainPathTemplate.match(domainName).location;
  }

  /**
   * Parse the domain from Domain resource.
   *
   * @param {string} domainName
   *   A fully-qualified path representing Domain resource.
   * @returns {string} A string representing the domain.
   */
  matchDomainFromDomainName(domainName: string) {
    return this.pathTemplates.domainPathTemplate.match(domainName).domain;
  }

  /**
   * Return a fully-qualified location resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @returns {string} Resource name string.
   */
  locationPath(project: string, location: string) {
    return this.pathTemplates.locationPathTemplate.render({
      project: project,
      location: location,
    });
  }

  /**
   * Parse the project from Location resource.
   *
   * @param {string} locationName
   *   A fully-qualified path representing Location resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromLocationName(locationName: string) {
    return this.pathTemplates.locationPathTemplate.match(locationName).project;
  }

  /**
   * Parse the location from Location resource.
   *
   * @param {string} locationName
   *   A fully-qualified path representing Location resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromLocationName(locationName: string) {
    return this.pathTemplates.locationPathTemplate.match(locationName).location;
  }

  /**
   * Return a fully-qualified project resource name string.
   *
   * @param {string} project
   * @returns {string} Resource name string.
   */
  projectPath(project: string) {
    return this.pathTemplates.projectPathTemplate.render({
      project: project,
    });
  }

  /**
   * Parse the project from Project resource.
   *
   * @param {string} projectName
   *   A fully-qualified path representing Project resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromProjectName(projectName: string) {
    return this.pathTemplates.projectPathTemplate.match(projectName).project;
  }

  /**
   * Return a fully-qualified schema resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} domain
   * @returns {string} Resource name string.
   */
  schemaPath(project: string, location: string, domain: string) {
    return this.pathTemplates.schemaPathTemplate.render({
      project: project,
      location: location,
      domain: domain,
    });
  }

  /**
   * Parse the project from Schema resource.
   *
   * @param {string} schemaName
   *   A fully-qualified path representing Schema resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromSchemaName(schemaName: string) {
    return this.pathTemplates.schemaPathTemplate.match(schemaName).project;
  }

  /**
   * Parse the location from Schema resource.
   *
   * @param {string} schemaName
   *   A fully-qualified path representing Schema resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromSchemaName(schemaName: string) {
    return this.pathTemplates.schemaPathTemplate.match(schemaName).location;
  }

  /**
   * Parse the domain from Schema resource.
   *
   * @param {string} schemaName
   *   A fully-qualified path representing Schema resource.
   * @returns {string} A string representing the domain.
   */
  matchDomainFromSchemaName(schemaName: string) {
    return this.pathTemplates.schemaPathTemplate.match(schemaName).domain;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    if (this.appTopologyStub && !this._terminated) {
      return this.appTopologyStub.then((stub) => {
        this._log.info('ending gRPC channel');
        this._terminated = true;
        stub.close();
        this.locationsClient.close().catch((err) => {
          throw err;
        });
      });
    }
    return Promise.resolve();
  }
}
