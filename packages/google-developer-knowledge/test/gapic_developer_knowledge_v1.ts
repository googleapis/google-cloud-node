// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';
import {describe, it} from 'mocha';
import * as developerknowledgeModule from '../src';

import {PassThrough} from 'stream';

import {protobuf} from 'google-gax';

// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = protobuf.Root.fromJSON(
  require('../protos/protos.json'),
).resolveAll();

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName: string, fields: string[]) {
  let type = root.lookupType(typeName) as protobuf.Type;
  for (const field of fields.slice(0, -1)) {
    type = type.fields[field]?.resolvedType as protobuf.Type;
  }
  return type.fields[fields[fields.length - 1]]?.defaultValue;
}

function generateSampleMessage<T extends object>(instance: T) {
  const filledObject = (
    instance.constructor as typeof protobuf.Message
  ).toObject(instance as protobuf.Message<T>, {defaults: true});
  return (instance.constructor as typeof protobuf.Message).fromObject(
    filledObject,
  ) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
  return error
    ? sinon.stub().rejects(error)
    : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(
  response?: ResponseType,
  error?: Error,
) {
  return error
    ? sinon.stub().callsArgWith(2, error)
    : sinon.stub().callsArgWith(2, null, response);
}

function stubPageStreamingCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error,
) {
  const pagingStub = sinon.stub();
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
    }
  }
  const transformStub = error
    ? sinon.stub().callsArgWith(2, error)
    : pagingStub;
  const mockStream = new PassThrough({
    objectMode: true,
    transform: transformStub,
  });
  // trigger as many responses as needed
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      setImmediate(() => {
        mockStream.write({});
      });
    }
    setImmediate(() => {
      mockStream.end();
    });
  } else {
    setImmediate(() => {
      mockStream.write({});
    });
    setImmediate(() => {
      mockStream.end();
    });
  }
  return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error,
) {
  let counter = 0;
  const asyncIterable = {
    [Symbol.asyncIterator]() {
      return {
        async next() {
          if (error) {
            return Promise.reject(error);
          }
          if (counter >= responses!.length) {
            return Promise.resolve({done: true, value: undefined});
          }
          return Promise.resolve({done: false, value: responses![counter++]});
        },
      };
    },
  };
  return sinon.stub().returns(asyncIterable);
}

describe('v1.DeveloperKnowledgeClient', () => {
  describe('Common methods', () => {
    it('has apiEndpoint', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient();
      const apiEndpoint = client.apiEndpoint;
      assert.strictEqual(apiEndpoint, 'developerknowledge.googleapis.com');
    });

    it('has universeDomain', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient();
      const universeDomain = client.universeDomain;
      assert.strictEqual(universeDomain, 'googleapis.com');
    });

    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      it('throws DeprecationWarning if static servicePath is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const servicePath =
          developerknowledgeModule.v1.DeveloperKnowledgeClient.servicePath;
        assert.strictEqual(servicePath, 'developerknowledge.googleapis.com');
        assert(stub.called);
        stub.restore();
      });

      it('throws DeprecationWarning if static apiEndpoint is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const apiEndpoint =
          developerknowledgeModule.v1.DeveloperKnowledgeClient.apiEndpoint;
        assert.strictEqual(apiEndpoint, 'developerknowledge.googleapis.com');
        assert(stub.called);
        stub.restore();
      });
    }
    it('sets apiEndpoint according to universe domain camelCase', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        universeDomain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'developerknowledge.example.com');
    });

    it('sets apiEndpoint according to universe domain snakeCase', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        universe_domain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'developerknowledge.example.com');
    });

    if (typeof process === 'object' && 'env' in process) {
      describe('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
        it('sets apiEndpoint from environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client =
            new developerknowledgeModule.v1.DeveloperKnowledgeClient();
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'developerknowledge.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });

        it('value configured in code has priority over environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client =
            new developerknowledgeModule.v1.DeveloperKnowledgeClient({
              universeDomain: 'configured.example.com',
            });
          const servicePath = client.apiEndpoint;
          assert.strictEqual(
            servicePath,
            'developerknowledge.configured.example.com',
          );
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });
      });
    }
    it('does not allow setting both universeDomain and universe_domain', () => {
      assert.throws(() => {
        new developerknowledgeModule.v1.DeveloperKnowledgeClient({
          universe_domain: 'example.com',
          universeDomain: 'example.net',
        });
      });
    });

    it('has port', () => {
      const port = developerknowledgeModule.v1.DeveloperKnowledgeClient.port;
      assert(port);
      assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient();
      assert(client);
    });

    it('should create a client with gRPC fallback', () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        fallback: true,
      });
      assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.developerKnowledgeStub, undefined);
      await client.initialize();
      assert(client.developerKnowledgeStub);
    });

    it('has close method for the initialized client', done => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.initialize().catch(err => {
        throw err;
      });
      assert(client.developerKnowledgeStub);
      client
        .close()
        .then(() => {
          done();
        })
        .catch(err => {
          throw err;
        });
    });

    it('has close method for the non-initialized client', done => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      assert.strictEqual(client.developerKnowledgeStub, undefined);
      client
        .close()
        .then(() => {
          done();
        })
        .catch(err => {
          throw err;
        });
    });

    it('has getProjectId method', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
      const result = await client.getProjectId();
      assert.strictEqual(result, fakeProjectId);
      assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon
        .stub()
        .callsArgWith(0, null, fakeProjectId);
      const promise = new Promise((resolve, reject) => {
        client.getProjectId((err?: Error | null, projectId?: string | null) => {
          if (err) {
            reject(err);
          } else {
            resolve(projectId);
          }
        });
      });
      const result = await promise;
      assert.strictEqual(result, fakeProjectId);
    });
  });

  describe('getDocument', () => {
    it('invokes getDocument without error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.GetDocumentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.developers.knowledge.v1.GetDocumentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.developers.knowledge.v1.Document(),
      );
      client.innerApiCalls.getDocument = stubSimpleCall(expectedResponse);
      const [response] = await client.getDocument(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getDocument without error using callback', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.GetDocumentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.developers.knowledge.v1.GetDocumentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.developers.knowledge.v1.Document(),
      );
      client.innerApiCalls.getDocument =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getDocument(
          request,
          (
            err?: Error | null,
            result?: protos.google.developers.knowledge.v1.IDocument | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getDocument with error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.GetDocumentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.developers.knowledge.v1.GetDocumentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getDocument = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.getDocument(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getDocument as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getDocument with closed client', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.GetDocumentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.developers.knowledge.v1.GetDocumentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch(err => {
        throw err;
      });
      await assert.rejects(client.getDocument(request), expectedError);
    });
  });

  describe('batchGetDocuments', () => {
    it('invokes batchGetDocuments without error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsResponse(),
      );
      client.innerApiCalls.batchGetDocuments = stubSimpleCall(expectedResponse);
      const [response] = await client.batchGetDocuments(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes batchGetDocuments without error using callback', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsResponse(),
      );
      client.innerApiCalls.batchGetDocuments =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.batchGetDocuments(
          request,
          (
            err?: Error | null,
            result?: protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes batchGetDocuments with error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsRequest(),
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.batchGetDocuments = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.batchGetDocuments(request), expectedError);
    });

    it('invokes batchGetDocuments with closed client', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.BatchGetDocumentsRequest(),
      );
      const expectedError = new Error('The client has already been closed.');
      client.close().catch(err => {
        throw err;
      });
      await assert.rejects(client.batchGetDocuments(request), expectedError);
    });
  });

  describe('searchDocumentChunks', () => {
    it('invokes searchDocumentChunks without error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
      ];
      client.innerApiCalls.searchDocumentChunks =
        stubSimpleCall(expectedResponse);
      const [response] = await client.searchDocumentChunks(request);
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes searchDocumentChunks without error using callback', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
      ];
      client.innerApiCalls.searchDocumentChunks =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.searchDocumentChunks(
          request,
          (
            err?: Error | null,
            result?:
              protos.google.developers.knowledge.v1.IDocumentChunk[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
    });

    it('invokes searchDocumentChunks with error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedError = new Error('expected');
      client.innerApiCalls.searchDocumentChunks = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.searchDocumentChunks(request), expectedError);
    });

    it('invokes searchDocumentChunksStream without error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
      ];
      client.descriptors.page.searchDocumentChunks.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.searchDocumentChunksStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.developers.knowledge.v1.DocumentChunk[] =
          [];
        stream.on(
          'data',
          (response: protos.google.developers.knowledge.v1.DocumentChunk) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.searchDocumentChunks.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchDocumentChunks, request),
      );
    });

    it('invokes searchDocumentChunksStream with error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedError = new Error('expected');
      client.descriptors.page.searchDocumentChunks.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.searchDocumentChunksStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.developers.knowledge.v1.DocumentChunk[] =
          [];
        stream.on(
          'data',
          (response: protos.google.developers.knowledge.v1.DocumentChunk) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.searchDocumentChunks.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchDocumentChunks, request),
      );
    });

    it('uses async iteration with searchDocumentChunks without error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
        generateSampleMessage(
          new protos.google.developers.knowledge.v1.DocumentChunk(),
        ),
      ];
      client.descriptors.page.searchDocumentChunks.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.developers.knowledge.v1.IDocumentChunk[] =
        [];
      const iterable = client.searchDocumentChunksAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchDocumentChunks.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
    });

    it('uses async iteration with searchDocumentChunks with error', async () => {
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.developers.knowledge.v1.SearchDocumentChunksRequest(),
      );
      const expectedError = new Error('expected');
      client.descriptors.page.searchDocumentChunks.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.searchDocumentChunksAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.developers.knowledge.v1.IDocumentChunk[] =
          [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchDocumentChunks.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
    });
  });

  describe('Path templates', () => {
    describe('document', async () => {
      const fakePath = '/rendered/path/document';
      const expectedParameters = {
        document: 'documentValue',
      };
      const client = new developerknowledgeModule.v1.DeveloperKnowledgeClient({
        credentials: {client_email: 'bogus', private_key: 'bogus'},
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.documentPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.documentPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('documentPath', () => {
        const result = client.documentPath('documentValue');
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.documentPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchDocumentFromDocumentName', () => {
        const result = client.matchDocumentFromDocumentName(fakePath);
        assert.strictEqual(result, 'documentValue');
        assert(
          (client.pathTemplates.documentPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });
  });
});
