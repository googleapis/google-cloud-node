// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import type * as gax from 'google-gax';
import type {
  Callback,
  CallOptions,
  Descriptors,
  ClientOptions,
  PaginationCallback,
  GaxCall,
} from 'google-gax';
import { Transform } from 'stream';
import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');
import { loggingUtils as logging, decodeAnyProtosInArray } from 'google-gax';

/**
 * Client JSON configuration object, loaded from
 * `src/v1/developer_knowledge_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './developer_knowledge_client_config.json';
const version = require('../../../package.json').version;

/**
 *  The Developer Knowledge API provides programmatic access to Google's public
 *  developer documentation, enabling you to integrate this knowledge base into
 *  your own applications and workflows.
 *
 *  The API is designed to be the canonical source for machine-readable access to
 *  Google's developer documentation.
 *
 *  A typical use case is to first use
 *  {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.SearchDocumentChunks|DeveloperKnowledge.SearchDocumentChunks}
 *  to find relevant page URIs based on a query, and then use
 *  {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.GetDocument|DeveloperKnowledge.GetDocument}
 *  or
 *  {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.BatchGetDocuments|DeveloperKnowledge.BatchGetDocuments}
 *  to fetch the full content of the top results.
 *
 *  All document content is provided in Markdown format.
 * @class
 * @memberof v1
 */
export class DeveloperKnowledgeClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _providedCustomServicePath: boolean;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: { [method: string]: gax.CallSettings };
  private _universeDomain: string;
  private _servicePath: string;
  private _log = logging.log('developer-knowledge');

  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  warn: (code: string, message: string, warnType?: string) => void;
  innerApiCalls: { [name: string]: Function };
  pathTemplates: { [name: string]: gax.PathTemplate };
  developerKnowledgeStub?: Promise<{ [name: string]: Function }>;

  /**
   * Construct an instance of DeveloperKnowledgeClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://cloud.google.com/docs/authentication/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP/1.1 REST mode.
   *     For more information, please check the
   *     {@link https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#http11-rest-api-mode documentation}.
   * @param {gax} [gaxInstance]: loaded instance of `google-gax`. Useful if you
   *     need to avoid loading the default gRPC version and want to use the fallback
   *     HTTP implementation. Load only fallback version and pass it to the constructor:
   *     ```
   *     const gax = require('google-gax/build/src/fallback'); // avoids loading google-gax with gRPC
   *     const client = new DeveloperKnowledgeClient({fallback: true}, gax);
   *     ```
   */
  constructor(
    opts?: ClientOptions,
    gaxInstance?: typeof gax | typeof gax.fallback,
  ) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof DeveloperKnowledgeClient;
    if (
      opts?.universe_domain &&
      opts?.universeDomain &&
      opts?.universe_domain !== opts?.universeDomain
    ) {
      throw new Error(
        'Please set either universe_domain or universeDomain, but not both.',
      );
    }
    const universeDomainEnvVar =
      typeof process === 'object' && typeof process.env === 'object'
        ? process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN']
        : undefined;
    this._universeDomain =
      opts?.universeDomain ??
      opts?.universe_domain ??
      universeDomainEnvVar ??
      'googleapis.com';
    this._servicePath = 'developerknowledge.' + this._universeDomain;
    const servicePath =
      opts?.servicePath || opts?.apiEndpoint || this._servicePath;
    this._providedCustomServicePath = !!(
      opts?.servicePath || opts?.apiEndpoint
    );
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback =
      opts?.fallback ??
      (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({ servicePath, port, clientConfig, fallback }, opts);

    // Request numeric enum values if REST transport is used.
    opts.numericEnums = true;

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== this._servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Load google-gax module synchronously if needed
    if (!gaxInstance) {
      gaxInstance = require('google-gax') as typeof gax;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gaxInstance.fallback : gaxInstance;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = this._gaxGrpc.auth as gax.GoogleAuth;

    // Set useJWTAccessWithScope on the auth object.
    this.auth.useJWTAccessWithScope = true;

    // Set defaultServicePath on the auth object.
    this.auth.defaultServicePath = this._servicePath;

    // Set the default scopes in auth client if needed.
    if (servicePath === this._servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }

    // Determine the client header string.
    const clientHeader = [`gax/${this._gaxModule.version}`, `gapic/${version}`];
    if (typeof process === 'object' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // This API contains "path templates"; forward-slash-separated
    // identifiers to uniquely identify resources within the API.
    // Create useful helper objects for these.
    this.pathTemplates = {
      documentPathTemplate: new this._gaxModule.PathTemplate(
        'documents/{document}',
      ),
    };

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      searchDocumentChunks: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'results',
      ),
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
      'google.developers.knowledge.v1.DeveloperKnowledge',
      gapicConfig as gax.ClientConfig,
      opts.clientConfig || {},
      { 'x-goog-api-client': clientHeader.join(' ') },
    );

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};

    // Add a warn function to the client constructor so it can be easily tested.
    this.warn = this._gaxModule.warn;
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.developerKnowledgeStub) {
      return this.developerKnowledgeStub;
    }

    // Put together the "service stub" for
    // google.developers.knowledge.v1.DeveloperKnowledge.
    this.developerKnowledgeStub = this._gaxGrpc.createStub(
      this._opts.fallback
        ? (this._protos as protobuf.Root).lookupService(
            'google.developers.knowledge.v1.DeveloperKnowledge',
          )
        : // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.developers.knowledge.v1
            .DeveloperKnowledge,
      this._opts,
      this._providedCustomServicePath,
    ) as Promise<{ [method: string]: Function }>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const developerKnowledgeStubMethods = [
      'searchDocumentChunks',
      'getDocument',
      'batchGetDocuments',
      'answerQuery',
    ];
    for (const methodName of developerKnowledgeStubMethods) {
      const callPromise = this.developerKnowledgeStub.then(
        (stub) =>
          (...args: Array<{}>) => {
            if (this._terminated) {
              return Promise.reject('The client has already been closed.');
            }
            const func = stub[methodName];
            return func.apply(stub, args);
          },
        (err: Error | null | undefined) => () => {
          throw err;
        },
      );

      const descriptor = this.descriptors.page[methodName] || undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor,
        this._opts.fallback,
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.developerKnowledgeStub;
  }

  /**
   * The DNS address for this API service.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static servicePath is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'developerknowledge.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static apiEndpoint is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'developerknowledge.googleapis.com';
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  get apiEndpoint() {
    return this._servicePath;
  }

  get universeDomain() {
    return this._universeDomain;
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return ['https://www.googleapis.com/auth/cloud-platform'];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(
    callback?: Callback<string, undefined, undefined>,
  ): Promise<string> | void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  /**
   * Retrieves a single document with its full Markdown content.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. Specifies the name of the document to retrieve.
   *   Format: `documents/{uri_without_scheme}`
   *   Example: `documents/docs.cloud.google.com/storage/docs/creating-buckets`
   * @param {google.developers.knowledge.v1.DocumentView} [request.view]
   *   Optional. Specifies the
   *   {@link protos.google.developers.knowledge.v1.DocumentView|DocumentView} of the
   *   document. If unspecified,
   *   {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.GetDocument|DeveloperKnowledge.GetDocument}
   *   defaults to `DOCUMENT_VIEW_CONTENT`.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.developers.knowledge.v1.Document|Document}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/developer_knowledge.get_document.js</caption>
   * region_tag:developerknowledge_v1_generated_DeveloperKnowledge_GetDocument_async
   */
  getDocument(
    request?: protos.google.developers.knowledge.v1.IGetDocumentRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IDocument,
      protos.google.developers.knowledge.v1.IGetDocumentRequest | undefined,
      {} | undefined,
    ]
  >;
  getDocument(
    request: protos.google.developers.knowledge.v1.IGetDocumentRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.developers.knowledge.v1.IDocument,
      | protos.google.developers.knowledge.v1.IGetDocumentRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getDocument(
    request: protos.google.developers.knowledge.v1.IGetDocumentRequest,
    callback: Callback<
      protos.google.developers.knowledge.v1.IDocument,
      | protos.google.developers.knowledge.v1.IGetDocumentRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getDocument(
    request?: protos.google.developers.knowledge.v1.IGetDocumentRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.developers.knowledge.v1.IDocument,
          | protos.google.developers.knowledge.v1.IGetDocumentRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.developers.knowledge.v1.IDocument,
      | protos.google.developers.knowledge.v1.IGetDocumentRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IDocument,
      protos.google.developers.knowledge.v1.IGetDocumentRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getDocument request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.developers.knowledge.v1.IDocument,
          | protos.google.developers.knowledge.v1.IGetDocumentRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getDocument response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getDocument(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.developers.knowledge.v1.IDocument,
          protos.google.developers.knowledge.v1.IGetDocumentRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getDocument response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Retrieves multiple documents, each with its full Markdown content.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string[]} request.names
   *   Required. Specifies the names of the documents to retrieve. A maximum of 20
   *   documents can be retrieved in a batch. The documents are returned in the
   *   same order as the `names` in the request.
   *
   *   Format: `documents/{uri_without_scheme}`
   *   Example: `documents/docs.cloud.google.com/storage/docs/creating-buckets`
   *
   * @param {google.developers.knowledge.v1.DocumentView} [request.view]
   *   Optional. Specifies the
   *   {@link protos.google.developers.knowledge.v1.DocumentView|DocumentView} of the
   *   document. If unspecified,
   *   {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.BatchGetDocuments|DeveloperKnowledge.BatchGetDocuments}
   *   defaults to `DOCUMENT_VIEW_CONTENT`.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.developers.knowledge.v1.BatchGetDocumentsResponse|BatchGetDocumentsResponse}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/developer_knowledge.batch_get_documents.js</caption>
   * region_tag:developerknowledge_v1_generated_DeveloperKnowledge_BatchGetDocuments_async
   */
  batchGetDocuments(
    request?: protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
      (
        | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
        | undefined
      ),
      {} | undefined,
    ]
  >;
  batchGetDocuments(
    request: protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
      | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  batchGetDocuments(
    request: protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest,
    callback: Callback<
      protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
      | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  batchGetDocuments(
    request?: protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
          | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
      | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
      (
        | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
        | undefined
      ),
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('batchGetDocuments request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
          | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('batchGetDocuments response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .batchGetDocuments(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.developers.knowledge.v1.IBatchGetDocumentsResponse,
          (
            | protos.google.developers.knowledge.v1.IBatchGetDocumentsRequest
            | undefined
          ),
          {} | undefined,
        ]) => {
          this._log.info('batchGetDocuments response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Answers a query using grounded generation.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.query
   *   Required. The query to answer.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.developers.knowledge.v1.AnswerQueryResponse|AnswerQueryResponse}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/developer_knowledge.answer_query.js</caption>
   * region_tag:developerknowledge_v1_generated_DeveloperKnowledge_AnswerQuery_async
   */
  answerQuery(
    request?: protos.google.developers.knowledge.v1.IAnswerQueryRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IAnswerQueryResponse,
      protos.google.developers.knowledge.v1.IAnswerQueryRequest | undefined,
      {} | undefined,
    ]
  >;
  answerQuery(
    request: protos.google.developers.knowledge.v1.IAnswerQueryRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.developers.knowledge.v1.IAnswerQueryResponse,
      | protos.google.developers.knowledge.v1.IAnswerQueryRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  answerQuery(
    request: protos.google.developers.knowledge.v1.IAnswerQueryRequest,
    callback: Callback<
      protos.google.developers.knowledge.v1.IAnswerQueryResponse,
      | protos.google.developers.knowledge.v1.IAnswerQueryRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  answerQuery(
    request?: protos.google.developers.knowledge.v1.IAnswerQueryRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.developers.knowledge.v1.IAnswerQueryResponse,
          | protos.google.developers.knowledge.v1.IAnswerQueryRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.developers.knowledge.v1.IAnswerQueryResponse,
      | protos.google.developers.knowledge.v1.IAnswerQueryRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IAnswerQueryResponse,
      protos.google.developers.knowledge.v1.IAnswerQueryRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('answerQuery request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.developers.knowledge.v1.IAnswerQueryResponse,
          | protos.google.developers.knowledge.v1.IAnswerQueryRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('answerQuery response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .answerQuery(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.developers.knowledge.v1.IAnswerQueryResponse,
          protos.google.developers.knowledge.v1.IAnswerQueryRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('answerQuery response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }

  /**
   * Searches for developer knowledge across Google's developer documentation.
   * Returns {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}s
   * based on the user's query. There may be many chunks from the same
   * {@link protos.google.developers.knowledge.v1.Document|Document}.  To retrieve full
   * documents, use
   * {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.GetDocument|DeveloperKnowledge.GetDocument}
   * or
   * {@link protos.google.developers.knowledge.v1.DeveloperKnowledge.BatchGetDocuments|DeveloperKnowledge.BatchGetDocuments}
   * with the
   * {@link protos.google.developers.knowledge.v1.DocumentChunk.parent|DocumentChunk.parent}
   * returned in the
   * {@link protos.google.developers.knowledge.v1.SearchDocumentChunksResponse.results|SearchDocumentChunksResponse.results}.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.query
   *   Required. Provides the raw query string provided by the user, such as "How
   *   to create a Cloud Storage bucket?".
   * @param {number} [request.pageSize]
   *   Optional. Specifies the maximum number of results to return. The service
   *   may return fewer than this value.
   *
   *   If unspecified, at most 5 results will be returned.
   *
   *   The maximum value is 100; values above 100 will be coerced to 100.
   * @param {string} [request.pageToken]
   *   Optional. Contains a page token, received from a previous
   *   `SearchDocumentChunks` call. Provide this to retrieve the subsequent page.
   * @param {string} [request.filter]
   *   Optional. Applies a strict filter to the search results. The expression
   *   supports a subset of the syntax described at https://google.aip.dev/160.
   *
   *   While `SearchDocumentChunks` returns
   *   {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}s, the filter
   *   is applied to `DocumentChunk.document` fields.
   *
   *   Supported fields for filtering:
   *
   *   * `data_source` (STRING): The source of the document, e.g.
   *     `docs.cloud.google.com`. See
   *     https://developers.google.com/knowledge/reference/corpus-reference for
   *     the complete list of data sources in the corpus.
   *   * `update_time` (TIMESTAMP): The timestamp of when the document was last
   *     meaningfully updated. A meaningful update is one that changes document's
   *     markdown content or metadata.
   *   * `uri` (STRING): The document URI, e.g.
   *     `https://docs.cloud.google.com/bigquery/docs/tables`.
   *
   *   STRING fields support `=` (equals) and `!=` (not equals) operators for
   *   **exact match** on the whole string. Partial match, prefix match, and
   *   regexp match are not supported.
   *
   *   TIMESTAMP fields support `=`, `<`, `<=`, `>`, and `>=` operators.
   *   Timestamps must be in RFC-3339 format, e.g., `"2025-01-01T00:00:00Z"`.
   *
   *   Note: Field names must be in `snake_case` (e.g., `data_source`). Values on
   *   the right-hand side of filtering expressions must be string literals
   *   enclosed in double quotes (e.g., `"docs.cloud.google.com"`).
   *
   *   You can combine expressions using `AND`, `OR`, and `NOT` (or `-`) logical
   *   operators. `OR` has higher precedence than `AND`. Use parentheses for
   *   explicit precedence grouping.
   *
   *   Examples:
   *
   *   * `data_source = "docs.cloud.google.com" OR data_source =
   *     "firebase.google.com"`
   *   * `data_source != "firebase.google.com"`
   *   * `update_time < "2024-01-01T00:00:00Z"`
   *   * `update_time >= "2025-01-22T00:00:00Z" AND (data_source =
   *     "developer.chrome.com" OR data_source = "web.dev")`
   *   * `uri = "https://docs.cloud.google.com/release-notes"`
   *
   *   The `filter` string must not exceed 500 characters; values longer than 500
   *   characters will result in an `INVALID_ARGUMENT` error.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `searchDocumentChunksAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchDocumentChunks(
    request?: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IDocumentChunk[],
      protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest | null,
      protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse,
    ]
  >;
  searchDocumentChunks(
    request: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
      | protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse
      | null
      | undefined,
      protos.google.developers.knowledge.v1.IDocumentChunk
    >,
  ): void;
  searchDocumentChunks(
    request: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    callback: PaginationCallback<
      protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
      | protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse
      | null
      | undefined,
      protos.google.developers.knowledge.v1.IDocumentChunk
    >,
  ): void;
  searchDocumentChunks(
    request?: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
          | protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse
          | null
          | undefined,
          protos.google.developers.knowledge.v1.IDocumentChunk
        >,
    callback?: PaginationCallback<
      protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
      | protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse
      | null
      | undefined,
      protos.google.developers.knowledge.v1.IDocumentChunk
    >,
  ): Promise<
    [
      protos.google.developers.knowledge.v1.IDocumentChunk[],
      protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest | null,
      protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
          | protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse
          | null
          | undefined,
          protos.google.developers.knowledge.v1.IDocumentChunk
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('searchDocumentChunks values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('searchDocumentChunks request %j', request);
    return this.innerApiCalls
      .searchDocumentChunks(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.developers.knowledge.v1.IDocumentChunk[],
          protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest | null,
          protos.google.developers.knowledge.v1.ISearchDocumentChunksResponse,
        ]) => {
          this._log.info('searchDocumentChunks values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `searchDocumentChunks`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.query
   *   Required. Provides the raw query string provided by the user, such as "How
   *   to create a Cloud Storage bucket?".
   * @param {number} [request.pageSize]
   *   Optional. Specifies the maximum number of results to return. The service
   *   may return fewer than this value.
   *
   *   If unspecified, at most 5 results will be returned.
   *
   *   The maximum value is 100; values above 100 will be coerced to 100.
   * @param {string} [request.pageToken]
   *   Optional. Contains a page token, received from a previous
   *   `SearchDocumentChunks` call. Provide this to retrieve the subsequent page.
   * @param {string} [request.filter]
   *   Optional. Applies a strict filter to the search results. The expression
   *   supports a subset of the syntax described at https://google.aip.dev/160.
   *
   *   While `SearchDocumentChunks` returns
   *   {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}s, the filter
   *   is applied to `DocumentChunk.document` fields.
   *
   *   Supported fields for filtering:
   *
   *   * `data_source` (STRING): The source of the document, e.g.
   *     `docs.cloud.google.com`. See
   *     https://developers.google.com/knowledge/reference/corpus-reference for
   *     the complete list of data sources in the corpus.
   *   * `update_time` (TIMESTAMP): The timestamp of when the document was last
   *     meaningfully updated. A meaningful update is one that changes document's
   *     markdown content or metadata.
   *   * `uri` (STRING): The document URI, e.g.
   *     `https://docs.cloud.google.com/bigquery/docs/tables`.
   *
   *   STRING fields support `=` (equals) and `!=` (not equals) operators for
   *   **exact match** on the whole string. Partial match, prefix match, and
   *   regexp match are not supported.
   *
   *   TIMESTAMP fields support `=`, `<`, `<=`, `>`, and `>=` operators.
   *   Timestamps must be in RFC-3339 format, e.g., `"2025-01-01T00:00:00Z"`.
   *
   *   Note: Field names must be in `snake_case` (e.g., `data_source`). Values on
   *   the right-hand side of filtering expressions must be string literals
   *   enclosed in double quotes (e.g., `"docs.cloud.google.com"`).
   *
   *   You can combine expressions using `AND`, `OR`, and `NOT` (or `-`) logical
   *   operators. `OR` has higher precedence than `AND`. Use parentheses for
   *   explicit precedence grouping.
   *
   *   Examples:
   *
   *   * `data_source = "docs.cloud.google.com" OR data_source =
   *     "firebase.google.com"`
   *   * `data_source != "firebase.google.com"`
   *   * `update_time < "2024-01-01T00:00:00Z"`
   *   * `update_time >= "2025-01-22T00:00:00Z" AND (data_source =
   *     "developer.chrome.com" OR data_source = "web.dev")`
   *   * `uri = "https://docs.cloud.google.com/release-notes"`
   *
   *   The `filter` string must not exceed 500 characters; values longer than 500
   *   characters will result in an `INVALID_ARGUMENT` error.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `searchDocumentChunksAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchDocumentChunksStream(
    request?: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['searchDocumentChunks'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchDocumentChunks stream %j', request);
    return this.descriptors.page.searchDocumentChunks.createStream(
      this.innerApiCalls.searchDocumentChunks as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `searchDocumentChunks`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.query
   *   Required. Provides the raw query string provided by the user, such as "How
   *   to create a Cloud Storage bucket?".
   * @param {number} [request.pageSize]
   *   Optional. Specifies the maximum number of results to return. The service
   *   may return fewer than this value.
   *
   *   If unspecified, at most 5 results will be returned.
   *
   *   The maximum value is 100; values above 100 will be coerced to 100.
   * @param {string} [request.pageToken]
   *   Optional. Contains a page token, received from a previous
   *   `SearchDocumentChunks` call. Provide this to retrieve the subsequent page.
   * @param {string} [request.filter]
   *   Optional. Applies a strict filter to the search results. The expression
   *   supports a subset of the syntax described at https://google.aip.dev/160.
   *
   *   While `SearchDocumentChunks` returns
   *   {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}s, the filter
   *   is applied to `DocumentChunk.document` fields.
   *
   *   Supported fields for filtering:
   *
   *   * `data_source` (STRING): The source of the document, e.g.
   *     `docs.cloud.google.com`. See
   *     https://developers.google.com/knowledge/reference/corpus-reference for
   *     the complete list of data sources in the corpus.
   *   * `update_time` (TIMESTAMP): The timestamp of when the document was last
   *     meaningfully updated. A meaningful update is one that changes document's
   *     markdown content or metadata.
   *   * `uri` (STRING): The document URI, e.g.
   *     `https://docs.cloud.google.com/bigquery/docs/tables`.
   *
   *   STRING fields support `=` (equals) and `!=` (not equals) operators for
   *   **exact match** on the whole string. Partial match, prefix match, and
   *   regexp match are not supported.
   *
   *   TIMESTAMP fields support `=`, `<`, `<=`, `>`, and `>=` operators.
   *   Timestamps must be in RFC-3339 format, e.g., `"2025-01-01T00:00:00Z"`.
   *
   *   Note: Field names must be in `snake_case` (e.g., `data_source`). Values on
   *   the right-hand side of filtering expressions must be string literals
   *   enclosed in double quotes (e.g., `"docs.cloud.google.com"`).
   *
   *   You can combine expressions using `AND`, `OR`, and `NOT` (or `-`) logical
   *   operators. `OR` has higher precedence than `AND`. Use parentheses for
   *   explicit precedence grouping.
   *
   *   Examples:
   *
   *   * `data_source = "docs.cloud.google.com" OR data_source =
   *     "firebase.google.com"`
   *   * `data_source != "firebase.google.com"`
   *   * `update_time < "2024-01-01T00:00:00Z"`
   *   * `update_time >= "2025-01-22T00:00:00Z" AND (data_source =
   *     "developer.chrome.com" OR data_source = "web.dev")`
   *   * `uri = "https://docs.cloud.google.com/release-notes"`
   *
   *   The `filter` string must not exceed 500 characters; values longer than 500
   *   characters will result in an `INVALID_ARGUMENT` error.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.developers.knowledge.v1.DocumentChunk|DocumentChunk}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/developer_knowledge.search_document_chunks.js</caption>
   * region_tag:developerknowledge_v1_generated_DeveloperKnowledge_SearchDocumentChunks_async
   */
  searchDocumentChunksAsync(
    request?: protos.google.developers.knowledge.v1.ISearchDocumentChunksRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.developers.knowledge.v1.IDocumentChunk> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    const defaultCallSettings = this._defaults['searchDocumentChunks'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchDocumentChunks iterate %j', request);
    return this.descriptors.page.searchDocumentChunks.asyncIterate(
      this.innerApiCalls['searchDocumentChunks'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.developers.knowledge.v1.IDocumentChunk>;
  }
  // --------------------
  // -- Path templates --
  // --------------------

  /**
   * Return a fully-qualified document resource name string.
   *
   * @param {string} document
   * @returns {string} Resource name string.
   */
  documentPath(document: string) {
    return this.pathTemplates.documentPathTemplate.render({
      document: document,
    });
  }

  /**
   * Parse the document from Document resource.
   *
   * @param {string} documentName
   *   A fully-qualified path representing Document resource.
   * @returns {string} A string representing the document.
   */
  matchDocumentFromDocumentName(documentName: string) {
    return this.pathTemplates.documentPathTemplate.match(documentName).document;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    if (this.developerKnowledgeStub && !this._terminated) {
      return this.developerKnowledgeStub.then((stub) => {
        this._log.info('ending gRPC channel');
        this._terminated = true;
        stub.close();
      });
    }
    return Promise.resolve();
  }
}
