// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

/* global window */
import type * as gax from 'google-gax';
import type {
  Callback,
  CallOptions,
  Descriptors,
  ClientOptions,
  GrpcClientOptions,
  LROperation,
  PaginationCallback,
  GaxCall,
  LocationsClient,
  LocationProtos,
} from 'google-gax';
import { Transform } from 'stream';
import * as protos from '../../protos/protos';
import jsonProtos = require('../../protos/protos.json');
import { loggingUtils as logging, decodeAnyProtosInArray } from 'google-gax';

/**
 * Client JSON configuration object, loaded from
 * `src/v1/agent_registry_client_config.json`.
 * This file defines retry strategy and timeouts for all API methods in this library.
 */
import * as gapicConfig from './agent_registry_client_config.json';
const version = require('../../../package.json').version;

/**
 *  Service for managing Agents, Endpoints, McpServers, Services, and Bindings.
 * @class
 * @memberof v1
 */
export class AgentRegistryClient {
  private _terminated = false;
  private _opts: ClientOptions;
  private _providedCustomServicePath: boolean;
  private _gaxModule: typeof gax | typeof gax.fallback;
  private _gaxGrpc: gax.GrpcClient | gax.fallback.GrpcClient;
  private _protos: {};
  private _defaults: { [method: string]: gax.CallSettings };
  private _universeDomain: string;
  private _servicePath: string;
  private _log = logging.log('agentregistry');

  auth: gax.GoogleAuth;
  descriptors: Descriptors = {
    page: {},
    stream: {},
    longrunning: {},
    batching: {},
  };
  warn: (code: string, message: string, warnType?: string) => void;
  innerApiCalls: { [name: string]: Function };
  locationsClient: LocationsClient;
  pathTemplates: { [name: string]: gax.PathTemplate };
  operationsClient: gax.OperationsClient;
  agentRegistryStub?: Promise<{ [name: string]: Function }>;

  /**
   * Construct an instance of AgentRegistryClient.
   *
   * @param {object} [options] - The configuration object.
   * The options accepted by the constructor are described in detail
   * in [this document](https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#creating-the-client-instance).
   * The common options are:
   * @param {object} [options.credentials] - Credentials object.
   * @param {string} [options.credentials.client_email]
   * @param {string} [options.credentials.private_key]
   * @param {string} [options.email] - Account email address. Required when
   *     using a .pem or .p12 keyFilename.
   * @param {string} [options.keyFilename] - Full path to the a .json, .pem, or
   *     .p12 key downloaded from the Google Developers Console. If you provide
   *     a path to a JSON file, the projectId option below is not necessary.
   *     NOTE: .pem and .p12 require you to specify options.email as well.
   * @param {number} [options.port] - The port on which to connect to
   *     the remote host.
   * @param {string} [options.projectId] - The project ID from the Google
   *     Developer's Console, e.g. 'grape-spaceship-123'. We will also check
   *     the environment variable GCLOUD_PROJECT for your project ID. If your
   *     app is running in an environment which supports
   *     {@link https://cloud.google.com/docs/authentication/application-default-credentials Application Default Credentials},
   *     your project ID will be detected automatically.
   * @param {string} [options.apiEndpoint] - The domain name of the
   *     API remote host.
   * @param {gax.ClientConfig} [options.clientConfig] - Client configuration override.
   *     Follows the structure of {@link gapicConfig}.
   * @param {boolean} [options.fallback] - Use HTTP/1.1 REST mode.
   *     For more information, please check the
   *     {@link https://github.com/googleapis/gax-nodejs/blob/main/client-libraries.md#http11-rest-api-mode documentation}.
   * @param {gax} [gaxInstance]: loaded instance of `google-gax`. Useful if you
   *     need to avoid loading the default gRPC version and want to use the fallback
   *     HTTP implementation. Load only fallback version and pass it to the constructor:
   *     ```
   *     const gax = require('google-gax/build/src/fallback'); // avoids loading google-gax with gRPC
   *     const client = new AgentRegistryClient({fallback: true}, gax);
   *     ```
   */
  constructor(
    opts?: ClientOptions,
    gaxInstance?: typeof gax | typeof gax.fallback,
  ) {
    // Ensure that options include all the required fields.
    const staticMembers = this.constructor as typeof AgentRegistryClient;
    if (
      opts?.universe_domain &&
      opts?.universeDomain &&
      opts?.universe_domain !== opts?.universeDomain
    ) {
      throw new Error(
        'Please set either universe_domain or universeDomain, but not both.',
      );
    }
    const universeDomainEnvVar =
      typeof process === 'object' && typeof process.env === 'object'
        ? process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN']
        : undefined;
    this._universeDomain =
      opts?.universeDomain ??
      opts?.universe_domain ??
      universeDomainEnvVar ??
      'googleapis.com';
    this._servicePath = 'agentregistry.' + this._universeDomain;
    const servicePath =
      opts?.servicePath || opts?.apiEndpoint || this._servicePath;
    this._providedCustomServicePath = !!(
      opts?.servicePath || opts?.apiEndpoint
    );
    const port = opts?.port || staticMembers.port;
    const clientConfig = opts?.clientConfig ?? {};
    const fallback =
      opts?.fallback ??
      (typeof window !== 'undefined' && typeof window?.fetch === 'function');
    opts = Object.assign({ servicePath, port, clientConfig, fallback }, opts);

    // Request numeric enum values if REST transport is used.
    opts.numericEnums = true;

    // If scopes are unset in options and we're connecting to a non-default endpoint, set scopes just in case.
    if (servicePath !== this._servicePath && !('scopes' in opts)) {
      opts['scopes'] = staticMembers.scopes;
    }

    // Load google-gax module synchronously if needed
    if (!gaxInstance) {
      gaxInstance = require('google-gax') as typeof gax;
    }

    // Choose either gRPC or proto-over-HTTP implementation of google-gax.
    this._gaxModule = opts.fallback ? gaxInstance.fallback : gaxInstance;

    // Create a `gaxGrpc` object, with any grpc-specific options sent to the client.
    this._gaxGrpc = new this._gaxModule.GrpcClient(opts);

    // Save options to use in initialize() method.
    this._opts = opts;

    // Save the auth object to the client, for use by other methods.
    this.auth = this._gaxGrpc.auth as gax.GoogleAuth;

    // Set useJWTAccessWithScope on the auth object.
    this.auth.useJWTAccessWithScope = true;

    // Set defaultServicePath on the auth object.
    this.auth.defaultServicePath = this._servicePath;

    // Set the default scopes in auth client if needed.
    if (servicePath === this._servicePath) {
      this.auth.defaultScopes = staticMembers.scopes;
    }
    this.locationsClient = new this._gaxModule.LocationsClient(
      this._gaxGrpc,
      opts,
    );

    // Determine the client header string.
    const clientHeader = [`gax/${this._gaxModule.version}`, `gapic/${version}`];
    if (typeof process === 'object' && 'versions' in process) {
      clientHeader.push(`gl-node/${process.versions.node}`);
    } else {
      clientHeader.push(`gl-web/${this._gaxModule.version}`);
    }
    if (!opts.fallback) {
      clientHeader.push(`grpc/${this._gaxGrpc.grpcVersion}`);
    } else {
      clientHeader.push(`rest/${this._gaxGrpc.grpcVersion}`);
    }
    if (opts.libName && opts.libVersion) {
      clientHeader.push(`${opts.libName}/${opts.libVersion}`);
    }
    // Load the applicable protos.
    this._protos = this._gaxGrpc.loadProtoJSON(jsonProtos);

    // This API contains "path templates"; forward-slash-separated
    // identifiers to uniquely identify resources within the API.
    // Create useful helper objects for these.
    this.pathTemplates = {
      agentPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/agents/{agent}',
      ),
      bindingPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/bindings/{binding}',
      ),
      endpointPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/endpoints/{endpoint}',
      ),
      locationPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}',
      ),
      mcpServerPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/mcpServers/{mcp_server}',
      ),
      projectPathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}',
      ),
      servicePathTemplate: new this._gaxModule.PathTemplate(
        'projects/{project}/locations/{location}/services/{service}',
      ),
    };

    // Some of the methods on this service return "paged" results,
    // (e.g. 50 results at a time, with tokens to get subsequent
    // pages). Denote the keys used for pagination and results.
    this.descriptors.page = {
      listAgents: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'agents',
      ),
      searchAgents: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'agents',
      ),
      listEndpoints: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'endpoints',
      ),
      listMcpServers: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'mcpServers',
      ),
      searchMcpServers: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'mcpServers',
      ),
      listServices: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'services',
      ),
      listBindings: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'bindings',
      ),
      fetchAvailableBindings: new this._gaxModule.PageDescriptor(
        'pageToken',
        'nextPageToken',
        'bindings',
      ),
    };

    const protoFilesRoot = this._gaxModule.protobufFromJSON(jsonProtos);
    // This API contains "long-running operations", which return a
    // an Operation object that allows for tracking of the operation,
    // rather than holding a request open.
    const lroOptions: GrpcClientOptions = {
      auth: this.auth,
      grpc: 'grpc' in this._gaxGrpc ? this._gaxGrpc.grpc : undefined,
    };
    if (opts.fallback) {
      lroOptions.protoJson = protoFilesRoot;
      lroOptions.httpRules = [
        {
          selector: 'google.cloud.location.Locations.GetLocation',
          get: '/v1/{name=projects/*/locations/*}',
        },
        {
          selector: 'google.cloud.location.Locations.ListLocations',
          get: '/v1/{name=projects/*}/locations',
        },
        {
          selector: 'google.longrunning.Operations.CancelOperation',
          post: '/v1/{name=projects/*/locations/*/operations/*}:cancel',
          body: '*',
        },
        {
          selector: 'google.longrunning.Operations.DeleteOperation',
          delete: '/v1/{name=projects/*/locations/*/operations/*}',
        },
        {
          selector: 'google.longrunning.Operations.GetOperation',
          get: '/v1/{name=projects/*/locations/*/operations/*}',
        },
        {
          selector: 'google.longrunning.Operations.ListOperations',
          get: '/v1/{name=projects/*/locations/*}/operations',
        },
      ];
    }
    this.operationsClient = this._gaxModule
      .lro(lroOptions)
      .operationsClient(opts);
    const createServiceResponse = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.Service',
    ) as gax.protobuf.Type;
    const createServiceMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;
    const updateServiceResponse = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.Service',
    ) as gax.protobuf.Type;
    const updateServiceMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;
    const deleteServiceResponse = protoFilesRoot.lookup(
      '.google.protobuf.Empty',
    ) as gax.protobuf.Type;
    const deleteServiceMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;
    const createBindingResponse = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.Binding',
    ) as gax.protobuf.Type;
    const createBindingMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;
    const updateBindingResponse = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.Binding',
    ) as gax.protobuf.Type;
    const updateBindingMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;
    const deleteBindingResponse = protoFilesRoot.lookup(
      '.google.protobuf.Empty',
    ) as gax.protobuf.Type;
    const deleteBindingMetadata = protoFilesRoot.lookup(
      '.google.cloud.agentregistry.v1.OperationMetadata',
    ) as gax.protobuf.Type;

    this.descriptors.longrunning = {
      createService: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        createServiceResponse.decode.bind(createServiceResponse),
        createServiceMetadata.decode.bind(createServiceMetadata),
      ),
      updateService: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        updateServiceResponse.decode.bind(updateServiceResponse),
        updateServiceMetadata.decode.bind(updateServiceMetadata),
      ),
      deleteService: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        deleteServiceResponse.decode.bind(deleteServiceResponse),
        deleteServiceMetadata.decode.bind(deleteServiceMetadata),
      ),
      createBinding: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        createBindingResponse.decode.bind(createBindingResponse),
        createBindingMetadata.decode.bind(createBindingMetadata),
      ),
      updateBinding: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        updateBindingResponse.decode.bind(updateBindingResponse),
        updateBindingMetadata.decode.bind(updateBindingMetadata),
      ),
      deleteBinding: new this._gaxModule.LongrunningDescriptor(
        this.operationsClient,
        deleteBindingResponse.decode.bind(deleteBindingResponse),
        deleteBindingMetadata.decode.bind(deleteBindingMetadata),
      ),
    };

    // Put together the default options sent with requests.
    this._defaults = this._gaxGrpc.constructSettings(
      'google.cloud.agentregistry.v1.AgentRegistry',
      gapicConfig as gax.ClientConfig,
      opts.clientConfig || {},
      { 'x-goog-api-client': clientHeader.join(' ') },
    );

    // Set up a dictionary of "inner API calls"; the core implementation
    // of calling the API is handled in `google-gax`, with this code
    // merely providing the destination and request information.
    this.innerApiCalls = {};

    // Add a warn function to the client constructor so it can be easily tested.
    this.warn = this._gaxModule.warn;
  }

  /**
   * Initialize the client.
   * Performs asynchronous operations (such as authentication) and prepares the client.
   * This function will be called automatically when any class method is called for the
   * first time, but if you need to initialize it before calling an actual method,
   * feel free to call initialize() directly.
   *
   * You can await on this method if you want to make sure the client is initialized.
   *
   * @returns {Promise} A promise that resolves to an authenticated service stub.
   */
  initialize() {
    // If the client stub promise is already initialized, return immediately.
    if (this.agentRegistryStub) {
      return this.agentRegistryStub;
    }

    // Put together the "service stub" for
    // google.cloud.agentregistry.v1.AgentRegistry.
    this.agentRegistryStub = this._gaxGrpc.createStub(
      this._opts.fallback
        ? (this._protos as protobuf.Root).lookupService(
            'google.cloud.agentregistry.v1.AgentRegistry',
          )
        : // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (this._protos as any).google.cloud.agentregistry.v1.AgentRegistry,
      this._opts,
      this._providedCustomServicePath,
    ) as Promise<{ [method: string]: Function }>;

    // Iterate over each of the methods that the service provides
    // and create an API call method for each.
    const agentRegistryStubMethods = [
      'listAgents',
      'searchAgents',
      'getAgent',
      'listEndpoints',
      'getEndpoint',
      'listMcpServers',
      'searchMcpServers',
      'getMcpServer',
      'listServices',
      'getService',
      'createService',
      'updateService',
      'deleteService',
      'listBindings',
      'getBinding',
      'createBinding',
      'updateBinding',
      'deleteBinding',
      'fetchAvailableBindings',
    ];
    for (const methodName of agentRegistryStubMethods) {
      const callPromise = this.agentRegistryStub.then(
        (stub) =>
          (...args: Array<{}>) => {
            if (this._terminated) {
              return Promise.reject('The client has already been closed.');
            }
            const func = stub[methodName];
            return func.apply(stub, args);
          },
        (err: Error | null | undefined) => () => {
          throw err;
        },
      );

      const descriptor =
        this.descriptors.page[methodName] ||
        this.descriptors.longrunning[methodName] ||
        undefined;
      const apiCall = this._gaxModule.createApiCall(
        callPromise,
        this._defaults[methodName],
        descriptor,
        this._opts.fallback,
      );

      this.innerApiCalls[methodName] = apiCall;
    }

    return this.agentRegistryStub;
  }

  /**
   * The DNS address for this API service.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get servicePath() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static servicePath is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'agentregistry.googleapis.com';
  }

  /**
   * The DNS address for this API service - same as servicePath.
   * @deprecated Use the apiEndpoint method of the client instance.
   * @returns {string} The DNS address for this service.
   */
  static get apiEndpoint() {
    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      process.emitWarning(
        'Static apiEndpoint is deprecated, please use the instance method instead.',
        'DeprecationWarning',
      );
    }
    return 'agentregistry.googleapis.com';
  }

  /**
   * The DNS address for this API service.
   * @returns {string} The DNS address for this service.
   */
  get apiEndpoint() {
    return this._servicePath;
  }

  get universeDomain() {
    return this._universeDomain;
  }

  /**
   * The port for this API service.
   * @returns {number} The default port for this service.
   */
  static get port() {
    return 443;
  }

  /**
   * The scopes needed to make gRPC calls for every method defined
   * in this service.
   * @returns {string[]} List of default scopes.
   */
  static get scopes() {
    return [
      'https://www.googleapis.com/auth/agentregistry.read-only',
      'https://www.googleapis.com/auth/agentregistry.read-write',
      'https://www.googleapis.com/auth/cloud-platform',
      'https://www.googleapis.com/auth/cloud-platform.read-only',
    ];
  }

  getProjectId(): Promise<string>;
  getProjectId(callback: Callback<string, undefined, undefined>): void;
  /**
   * Return the project ID used by this class.
   * @returns {Promise} A promise that resolves to string containing the project ID.
   */
  getProjectId(
    callback?: Callback<string, undefined, undefined>,
  ): Promise<string> | void {
    if (callback) {
      this.auth.getProjectId(callback);
      return;
    }
    return this.auth.getProjectId();
  }

  // -------------------
  // -- Service calls --
  // -------------------
  /**
   * Gets details of a single Agent.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. Name of the resource
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.agentregistry.v1.Agent|Agent}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.get_agent.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_GetAgent_async
   */
  getAgent(
    request?: protos.google.cloud.agentregistry.v1.IGetAgentRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent,
      protos.google.cloud.agentregistry.v1.IGetAgentRequest | undefined,
      {} | undefined,
    ]
  >;
  getAgent(
    request: protos.google.cloud.agentregistry.v1.IGetAgentRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IAgent,
      protos.google.cloud.agentregistry.v1.IGetAgentRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getAgent(
    request: protos.google.cloud.agentregistry.v1.IGetAgentRequest,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IAgent,
      protos.google.cloud.agentregistry.v1.IGetAgentRequest | null | undefined,
      {} | null | undefined
    >,
  ): void;
  getAgent(
    request?: protos.google.cloud.agentregistry.v1.IGetAgentRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.agentregistry.v1.IAgent,
          | protos.google.cloud.agentregistry.v1.IGetAgentRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.agentregistry.v1.IAgent,
      protos.google.cloud.agentregistry.v1.IGetAgentRequest | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent,
      protos.google.cloud.agentregistry.v1.IGetAgentRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getAgent request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.agentregistry.v1.IAgent,
          | protos.google.cloud.agentregistry.v1.IGetAgentRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getAgent response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getAgent(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.agentregistry.v1.IAgent,
          protos.google.cloud.agentregistry.v1.IGetAgentRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getAgent response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Gets details of a single Endpoint.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the endpoint to retrieve.
   *   Format: `projects/{project}/locations/{location}/endpoints/{endpoint}`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.agentregistry.v1.Endpoint|Endpoint}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.get_endpoint.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_GetEndpoint_async
   */
  getEndpoint(
    request?: protos.google.cloud.agentregistry.v1.IGetEndpointRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IEndpoint,
      protos.google.cloud.agentregistry.v1.IGetEndpointRequest | undefined,
      {} | undefined,
    ]
  >;
  getEndpoint(
    request: protos.google.cloud.agentregistry.v1.IGetEndpointRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IEndpoint,
      | protos.google.cloud.agentregistry.v1.IGetEndpointRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getEndpoint(
    request: protos.google.cloud.agentregistry.v1.IGetEndpointRequest,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IEndpoint,
      | protos.google.cloud.agentregistry.v1.IGetEndpointRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getEndpoint(
    request?: protos.google.cloud.agentregistry.v1.IGetEndpointRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.agentregistry.v1.IEndpoint,
          | protos.google.cloud.agentregistry.v1.IGetEndpointRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.agentregistry.v1.IEndpoint,
      | protos.google.cloud.agentregistry.v1.IGetEndpointRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IEndpoint,
      protos.google.cloud.agentregistry.v1.IGetEndpointRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getEndpoint request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.agentregistry.v1.IEndpoint,
          | protos.google.cloud.agentregistry.v1.IGetEndpointRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getEndpoint response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getEndpoint(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.agentregistry.v1.IEndpoint,
          protos.google.cloud.agentregistry.v1.IGetEndpointRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getEndpoint response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Gets details of a single McpServer.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. Name of the resource
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.get_mcp_server.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_GetMcpServer_async
   */
  getMcpServer(
    request?: protos.google.cloud.agentregistry.v1.IGetMcpServerRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer,
      protos.google.cloud.agentregistry.v1.IGetMcpServerRequest | undefined,
      {} | undefined,
    ]
  >;
  getMcpServer(
    request: protos.google.cloud.agentregistry.v1.IGetMcpServerRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IMcpServer,
      | protos.google.cloud.agentregistry.v1.IGetMcpServerRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getMcpServer(
    request: protos.google.cloud.agentregistry.v1.IGetMcpServerRequest,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IMcpServer,
      | protos.google.cloud.agentregistry.v1.IGetMcpServerRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getMcpServer(
    request?: protos.google.cloud.agentregistry.v1.IGetMcpServerRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.agentregistry.v1.IMcpServer,
          | protos.google.cloud.agentregistry.v1.IGetMcpServerRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.agentregistry.v1.IMcpServer,
      | protos.google.cloud.agentregistry.v1.IGetMcpServerRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer,
      protos.google.cloud.agentregistry.v1.IGetMcpServerRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getMcpServer request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.agentregistry.v1.IMcpServer,
          | protos.google.cloud.agentregistry.v1.IGetMcpServerRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getMcpServer response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getMcpServer(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.agentregistry.v1.IMcpServer,
          protos.google.cloud.agentregistry.v1.IGetMcpServerRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getMcpServer response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Gets details of a single Service.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the Service.
   *   Format: `projects/{project}/locations/{location}/services/{service}`.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.agentregistry.v1.Service|Service}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.get_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_GetService_async
   */
  getService(
    request?: protos.google.cloud.agentregistry.v1.IGetServiceRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IService,
      protos.google.cloud.agentregistry.v1.IGetServiceRequest | undefined,
      {} | undefined,
    ]
  >;
  getService(
    request: protos.google.cloud.agentregistry.v1.IGetServiceRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IService,
      | protos.google.cloud.agentregistry.v1.IGetServiceRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getService(
    request: protos.google.cloud.agentregistry.v1.IGetServiceRequest,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IService,
      | protos.google.cloud.agentregistry.v1.IGetServiceRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getService(
    request?: protos.google.cloud.agentregistry.v1.IGetServiceRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.agentregistry.v1.IService,
          | protos.google.cloud.agentregistry.v1.IGetServiceRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.agentregistry.v1.IService,
      | protos.google.cloud.agentregistry.v1.IGetServiceRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IService,
      protos.google.cloud.agentregistry.v1.IGetServiceRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getService request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.agentregistry.v1.IService,
          | protos.google.cloud.agentregistry.v1.IGetServiceRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getService response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getService(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.agentregistry.v1.IService,
          protos.google.cloud.agentregistry.v1.IGetServiceRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getService response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }
  /**
   * Gets details of a single Binding.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the Binding.
   *   Format: `projects/{project}/locations/{location}/bindings/{binding}`.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link protos.google.cloud.agentregistry.v1.Binding|Binding}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.get_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_GetBinding_async
   */
  getBinding(
    request?: protos.google.cloud.agentregistry.v1.IGetBindingRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding,
      protos.google.cloud.agentregistry.v1.IGetBindingRequest | undefined,
      {} | undefined,
    ]
  >;
  getBinding(
    request: protos.google.cloud.agentregistry.v1.IGetBindingRequest,
    options: CallOptions,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IBinding,
      | protos.google.cloud.agentregistry.v1.IGetBindingRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getBinding(
    request: protos.google.cloud.agentregistry.v1.IGetBindingRequest,
    callback: Callback<
      protos.google.cloud.agentregistry.v1.IBinding,
      | protos.google.cloud.agentregistry.v1.IGetBindingRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): void;
  getBinding(
    request?: protos.google.cloud.agentregistry.v1.IGetBindingRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          protos.google.cloud.agentregistry.v1.IBinding,
          | protos.google.cloud.agentregistry.v1.IGetBindingRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.cloud.agentregistry.v1.IBinding,
      | protos.google.cloud.agentregistry.v1.IGetBindingRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding,
      protos.google.cloud.agentregistry.v1.IGetBindingRequest | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('getBinding request %j', request);
    const wrappedCallback:
      | Callback<
          protos.google.cloud.agentregistry.v1.IBinding,
          | protos.google.cloud.agentregistry.v1.IGetBindingRequest
          | null
          | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, options, rawResponse) => {
          this._log.info('getBinding response %j', response);
          callback!(error, response, options, rawResponse); // We verified callback above.
        }
      : undefined;
    return this.innerApiCalls
      .getBinding(request, options, wrappedCallback)
      ?.then(
        ([response, options, rawResponse]: [
          protos.google.cloud.agentregistry.v1.IBinding,
          protos.google.cloud.agentregistry.v1.IGetBindingRequest | undefined,
          {} | undefined,
        ]) => {
          this._log.info('getBinding response %j', response);
          return [response, options, rawResponse];
        },
      )
      .catch((error: any) => {
        if (
          error &&
          'statusDetails' in error &&
          error.statusDetails instanceof Array
        ) {
          const protos = this._gaxModule.protobuf.Root.fromJSON(
            jsonProtos,
          ) as unknown as gax.protobuf.Type;
          error.statusDetails = decodeAnyProtosInArray(
            error.statusDetails,
            protos,
          );
        }
        throw error;
      });
  }

  /**
   * Creates a new Service in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to create the Service in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {string} request.serviceId
   *   Required. The ID to use for the service, which will become the final
   *   component of the service's resource name.
   *
   *   This value should be 4-63 characters, and valid characters
   *   are `/{@link protos.0-9|a-z}-/`.
   * @param {google.cloud.agentregistry.v1.Service} request.service
   *   Required. The Service resource that is being created.
   *   Format: `projects/{project}/locations/{location}/services/{service}`.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes since the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.create_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_CreateService_async
   */
  createService(
    request?: protos.google.cloud.agentregistry.v1.ICreateServiceRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  createService(
    request: protos.google.cloud.agentregistry.v1.ICreateServiceRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  createService(
    request: protos.google.cloud.agentregistry.v1.ICreateServiceRequest,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  createService(
    request?: protos.google.cloud.agentregistry.v1.ICreateServiceRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('createService response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('createService request %j', request);
    return this.innerApiCalls
      .createService(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('createService response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `createService()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.create_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_CreateService_async
   */
  async checkCreateServiceProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.cloud.agentregistry.v1.Service,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('createService long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.createService,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.cloud.agentregistry.v1.Service,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Updates the parameters of a single Service.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.protobuf.FieldMask} [request.updateMask]
   *   Optional. Field mask is used to specify the fields to be overwritten in the
   *   Service resource by the update.
   *   The fields specified in the update_mask are relative to the resource, not
   *   the full request. A field will be overwritten if it is in the mask. If the
   *   user does not provide a mask then all fields present in the request will be
   *   overwritten.
   * @param {google.cloud.agentregistry.v1.Service} request.service
   *   Required. The Service resource that is being updated.
   *   Format: `projects/{project}/locations/{location}/services/{service}`.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes since the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.update_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_UpdateService_async
   */
  updateService(
    request?: protos.google.cloud.agentregistry.v1.IUpdateServiceRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  updateService(
    request: protos.google.cloud.agentregistry.v1.IUpdateServiceRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  updateService(
    request: protos.google.cloud.agentregistry.v1.IUpdateServiceRequest,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  updateService(
    request?: protos.google.cloud.agentregistry.v1.IUpdateServiceRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        'service.name': request.service!.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('updateService response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('updateService request %j', request);
    return this.innerApiCalls
      .updateService(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.cloud.agentregistry.v1.IService,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('updateService response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `updateService()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.update_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_UpdateService_async
   */
  async checkUpdateServiceProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.cloud.agentregistry.v1.Service,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('updateService long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.updateService,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.cloud.agentregistry.v1.Service,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Deletes a single Service.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the Service.
   *   Format: `projects/{project}/locations/{location}/services/{service}`.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes after the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.delete_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_DeleteService_async
   */
  deleteService(
    request?: protos.google.cloud.agentregistry.v1.IDeleteServiceRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  deleteService(
    request: protos.google.cloud.agentregistry.v1.IDeleteServiceRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  deleteService(
    request: protos.google.cloud.agentregistry.v1.IDeleteServiceRequest,
    callback: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  deleteService(
    request?: protos.google.cloud.agentregistry.v1.IDeleteServiceRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('deleteService response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('deleteService request %j', request);
    return this.innerApiCalls
      .deleteService(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('deleteService response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `deleteService()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.delete_service.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_DeleteService_async
   */
  async checkDeleteServiceProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.protobuf.Empty,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('deleteService long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.deleteService,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.protobuf.Empty,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Creates a new Binding in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to create the Binding in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {string} request.bindingId
   *   Required. The ID to use for the binding, which will become the final
   *   component of the binding's resource name.
   *
   *   This value should be 4-63 characters, and must conform to RFC-1034.
   *   Specifically, it must match the regular expression
   *   `^[a-z]([a-z0-9-]{0,61}[a-z0-9])?$`.
   * @param {google.cloud.agentregistry.v1.Binding} request.binding
   *   Required. The Binding resource that is being created.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes since the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.create_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_CreateBinding_async
   */
  createBinding(
    request?: protos.google.cloud.agentregistry.v1.ICreateBindingRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  createBinding(
    request: protos.google.cloud.agentregistry.v1.ICreateBindingRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  createBinding(
    request: protos.google.cloud.agentregistry.v1.ICreateBindingRequest,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  createBinding(
    request?: protos.google.cloud.agentregistry.v1.ICreateBindingRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('createBinding response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('createBinding request %j', request);
    return this.innerApiCalls
      .createBinding(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('createBinding response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `createBinding()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.create_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_CreateBinding_async
   */
  async checkCreateBindingProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.cloud.agentregistry.v1.Binding,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('createBinding long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.createBinding,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.cloud.agentregistry.v1.Binding,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Updates the parameters of a single Binding.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {google.cloud.agentregistry.v1.Binding} request.binding
   *   Required. The Binding resource that is being updated.
   * @param {google.protobuf.FieldMask} [request.updateMask]
   *   Optional. Field mask is used to specify the fields to be overwritten in the
   *   Binding resource by the update.
   *   The fields specified in the update_mask are relative to the resource, not
   *   the full request. A field will be overwritten if it is in the mask. If the
   *   user does not provide a mask then all fields present in the request will be
   *   overwritten.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes since the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.update_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_UpdateBinding_async
   */
  updateBinding(
    request?: protos.google.cloud.agentregistry.v1.IUpdateBindingRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  updateBinding(
    request: protos.google.cloud.agentregistry.v1.IUpdateBindingRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  updateBinding(
    request: protos.google.cloud.agentregistry.v1.IUpdateBindingRequest,
    callback: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  updateBinding(
    request?: protos.google.cloud.agentregistry.v1.IUpdateBindingRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        'binding.name': request.binding!.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('updateBinding response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('updateBinding request %j', request);
    return this.innerApiCalls
      .updateBinding(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.cloud.agentregistry.v1.IBinding,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('updateBinding response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `updateBinding()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.update_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_UpdateBinding_async
   */
  async checkUpdateBindingProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.cloud.agentregistry.v1.Binding,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('updateBinding long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.updateBinding,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.cloud.agentregistry.v1.Binding,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Deletes a single Binding.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Required. The name of the Binding.
   *   Format: `projects/{project}/locations/{location}/bindings/{binding}`.
   * @param {string} [request.requestId]
   *   Optional. An optional request ID to identify requests. Specify a unique
   *   request ID so that if you must retry your request, the server will know to
   *   ignore the request if it has already been completed. The server will
   *   guarantee that for at least 60 minutes after the first request.
   *
   *   For example, consider a situation where you make an initial request and the
   *   request times out. If you make the request again with the same request
   *   ID, the server can check if original operation with the same request ID
   *   was received, and if so, will ignore the second request. This prevents
   *   clients from accidentally creating duplicate commitments.
   *
   *   The request ID must be a valid UUID with the exception that zero UUID is
   *   not supported (00000000-0000-0000-0000-000000000000).
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   *   a long running operation. Its `promise()` method returns a promise
   *   you can `await` for.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.delete_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_DeleteBinding_async
   */
  deleteBinding(
    request?: protos.google.cloud.agentregistry.v1.IDeleteBindingRequest,
    options?: CallOptions,
  ): Promise<
    [
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  >;
  deleteBinding(
    request: protos.google.cloud.agentregistry.v1.IDeleteBindingRequest,
    options: CallOptions,
    callback: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  deleteBinding(
    request: protos.google.cloud.agentregistry.v1.IDeleteBindingRequest,
    callback: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): void;
  deleteBinding(
    request?: protos.google.cloud.agentregistry.v1.IDeleteBindingRequest,
    optionsOrCallback?:
      | CallOptions
      | Callback<
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | null | undefined,
      {} | null | undefined
    >,
  ): Promise<
    [
      LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >,
      protos.google.longrunning.IOperation | undefined,
      {} | undefined,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | Callback<
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | null | undefined,
          {} | null | undefined
        >
      | undefined = callback
      ? (error, response, rawResponse, _) => {
          this._log.info('deleteBinding response %j', rawResponse);
          callback!(error, response, rawResponse, _); // We verified callback above.
        }
      : undefined;
    this._log.info('deleteBinding request %j', request);
    return this.innerApiCalls
      .deleteBinding(request, options, wrappedCallback)
      ?.then(
        ([response, rawResponse, _]: [
          LROperation<
            protos.google.protobuf.IEmpty,
            protos.google.cloud.agentregistry.v1.IOperationMetadata
          >,
          protos.google.longrunning.IOperation | undefined,
          {} | undefined,
        ]) => {
          this._log.info('deleteBinding response %j', rawResponse);
          return [response, rawResponse, _];
        },
      );
  }
  /**
   * Check the status of the long running operation returned by `deleteBinding()`.
   * @param {String} name
   *   The operation name that will be passed.
   * @returns {Promise} - The promise which resolves to an object.
   *   The decoded operation object has result and metadata field to get information from.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#long-running-operations | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.delete_binding.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_DeleteBinding_async
   */
  async checkDeleteBindingProgress(
    name: string,
  ): Promise<
    LROperation<
      protos.google.protobuf.Empty,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >
  > {
    this._log.info('deleteBinding long-running');
    const request =
      new this._gaxModule.operationsProtos.google.longrunning.GetOperationRequest(
        { name },
      );
    const [operation] = await this.operationsClient.getOperation(request);
    const decodeOperation = new this._gaxModule.Operation(
      operation,
      this.descriptors.longrunning.deleteBinding,
      this._gaxModule.createDefaultBackoffSettings(),
    );
    return decodeOperation as LROperation<
      protos.google.protobuf.Empty,
      protos.google.cloud.agentregistry.v1.OperationMetadata
    >;
  }
  /**
   * Lists Agents in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListAgentsRequest
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Agent|Agent}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listAgentsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listAgents(
    request?: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent[],
      protos.google.cloud.agentregistry.v1.IListAgentsRequest | null,
      protos.google.cloud.agentregistry.v1.IListAgentsResponse,
    ]
  >;
  listAgents(
    request: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListAgentsRequest,
      | protos.google.cloud.agentregistry.v1.IListAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): void;
  listAgents(
    request: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListAgentsRequest,
      | protos.google.cloud.agentregistry.v1.IListAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): void;
  listAgents(
    request?: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListAgentsRequest,
          | protos.google.cloud.agentregistry.v1.IListAgentsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IAgent
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListAgentsRequest,
      | protos.google.cloud.agentregistry.v1.IListAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent[],
      protos.google.cloud.agentregistry.v1.IListAgentsRequest | null,
      protos.google.cloud.agentregistry.v1.IListAgentsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListAgentsRequest,
          | protos.google.cloud.agentregistry.v1.IListAgentsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IAgent
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listAgents values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listAgents request %j', request);
    return this.innerApiCalls
      .listAgents(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IAgent[],
          protos.google.cloud.agentregistry.v1.IListAgentsRequest | null,
          protos.google.cloud.agentregistry.v1.IListAgentsResponse,
        ]) => {
          this._log.info('listAgents values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listAgents`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListAgentsRequest
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Agent|Agent} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listAgentsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listAgentsStream(
    request?: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listAgents'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listAgents stream %j', request);
    return this.descriptors.page.listAgents.createStream(
      this.innerApiCalls.listAgents as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listAgents`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListAgentsRequest
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Agent|Agent}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.list_agents.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_ListAgents_async
   */
  listAgentsAsync(
    request?: protos.google.cloud.agentregistry.v1.IListAgentsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IAgent> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listAgents'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listAgents iterate %j', request);
    return this.descriptors.page.listAgents.asyncIterate(
      this.innerApiCalls['listAgents'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IAgent>;
  }
  /**
   * Searches Agents in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchAgentsRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the Agents to return. If no search
   *   criteria is specified then all accessible Agents will be returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | agentId            | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *   | description        | No  | Yes | No  | Included       |
   *   | skills             | No  | Yes | No  | Included       |
   *   | skills.id          | No  | Yes | No  | Included       |
   *   | skills.name        | No  | Yes | No  | Included       |
   *   | skills.description | No  | Yes | No  | Included       |
   *   | skills.tags        | No  | Yes | No  | Included       |
   *   | skills.examples    | No  | Yes | No  | Included       |
   *
   *   Examples:
   *
   *   * `agentId="urn:agent:projects-123:projects:123:locations:us-central1:reasoningEngines:1234"`
   *     to find the agent with the specified agent ID.
   *   * `name:important` to find agents whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find agents whose display name contains words
   *     that start with `works`.
   *   * `skills.tags:test` to find agents whose skills tags contain `test`.
   *   * `planner OR booking` to find agents whose metadata contains the words
   *     `planner` or `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Agent|Agent}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `searchAgentsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchAgents(
    request?: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent[],
      protos.google.cloud.agentregistry.v1.ISearchAgentsRequest | null,
      protos.google.cloud.agentregistry.v1.ISearchAgentsResponse,
    ]
  >;
  searchAgents(
    request: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
      | protos.google.cloud.agentregistry.v1.ISearchAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): void;
  searchAgents(
    request: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
      | protos.google.cloud.agentregistry.v1.ISearchAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): void;
  searchAgents(
    request?: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
          | protos.google.cloud.agentregistry.v1.ISearchAgentsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IAgent
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
      | protos.google.cloud.agentregistry.v1.ISearchAgentsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IAgent
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IAgent[],
      protos.google.cloud.agentregistry.v1.ISearchAgentsRequest | null,
      protos.google.cloud.agentregistry.v1.ISearchAgentsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
          | protos.google.cloud.agentregistry.v1.ISearchAgentsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IAgent
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('searchAgents values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('searchAgents request %j', request);
    return this.innerApiCalls
      .searchAgents(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IAgent[],
          protos.google.cloud.agentregistry.v1.ISearchAgentsRequest | null,
          protos.google.cloud.agentregistry.v1.ISearchAgentsResponse,
        ]) => {
          this._log.info('searchAgents values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `searchAgents`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchAgentsRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the Agents to return. If no search
   *   criteria is specified then all accessible Agents will be returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | agentId            | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *   | description        | No  | Yes | No  | Included       |
   *   | skills             | No  | Yes | No  | Included       |
   *   | skills.id          | No  | Yes | No  | Included       |
   *   | skills.name        | No  | Yes | No  | Included       |
   *   | skills.description | No  | Yes | No  | Included       |
   *   | skills.tags        | No  | Yes | No  | Included       |
   *   | skills.examples    | No  | Yes | No  | Included       |
   *
   *   Examples:
   *
   *   * `agentId="urn:agent:projects-123:projects:123:locations:us-central1:reasoningEngines:1234"`
   *     to find the agent with the specified agent ID.
   *   * `name:important` to find agents whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find agents whose display name contains words
   *     that start with `works`.
   *   * `skills.tags:test` to find agents whose skills tags contain `test`.
   *   * `planner OR booking` to find agents whose metadata contains the words
   *     `planner` or `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Agent|Agent} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `searchAgentsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchAgentsStream(
    request?: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['searchAgents'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchAgents stream %j', request);
    return this.descriptors.page.searchAgents.createStream(
      this.innerApiCalls.searchAgents as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `searchAgents`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchAgentsRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the Agents to return. If no search
   *   criteria is specified then all accessible Agents will be returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | agentId            | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *   | description        | No  | Yes | No  | Included       |
   *   | skills             | No  | Yes | No  | Included       |
   *   | skills.id          | No  | Yes | No  | Included       |
   *   | skills.name        | No  | Yes | No  | Included       |
   *   | skills.description | No  | Yes | No  | Included       |
   *   | skills.tags        | No  | Yes | No  | Included       |
   *   | skills.examples    | No  | Yes | No  | Included       |
   *
   *   Examples:
   *
   *   * `agentId="urn:agent:projects-123:projects:123:locations:us-central1:reasoningEngines:1234"`
   *     to find the agent with the specified agent ID.
   *   * `name:important` to find agents whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find agents whose display name contains words
   *     that start with `works`.
   *   * `skills.tags:test` to find agents whose skills tags contain `test`.
   *   * `planner OR booking` to find agents whose metadata contains the words
   *     `planner` or `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Agent|Agent}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.search_agents.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_SearchAgents_async
   */
  searchAgentsAsync(
    request?: protos.google.cloud.agentregistry.v1.ISearchAgentsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IAgent> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['searchAgents'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchAgents iterate %j', request);
    return this.descriptors.page.searchAgents.asyncIterate(
      this.innerApiCalls['searchAgents'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IAgent>;
  }
  /**
   * Lists Endpoints in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list endpoints in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of endpoints returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   `version`, and `interfaces` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/endpoints/e1"`
   *     * `display_name = "my-endpoint"`
   *     * `description = "my-endpoint-description"`
   *     * `version = "v1"`
   *     * `interfaces.transport = "HTTP_JSON"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Endpoint|Endpoint}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listEndpointsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listEndpoints(
    request?: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IEndpoint[],
      protos.google.cloud.agentregistry.v1.IListEndpointsRequest | null,
      protos.google.cloud.agentregistry.v1.IListEndpointsResponse,
    ]
  >;
  listEndpoints(
    request: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
      | protos.google.cloud.agentregistry.v1.IListEndpointsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IEndpoint
    >,
  ): void;
  listEndpoints(
    request: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
      | protos.google.cloud.agentregistry.v1.IListEndpointsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IEndpoint
    >,
  ): void;
  listEndpoints(
    request?: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
          | protos.google.cloud.agentregistry.v1.IListEndpointsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IEndpoint
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
      | protos.google.cloud.agentregistry.v1.IListEndpointsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IEndpoint
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IEndpoint[],
      protos.google.cloud.agentregistry.v1.IListEndpointsRequest | null,
      protos.google.cloud.agentregistry.v1.IListEndpointsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
          | protos.google.cloud.agentregistry.v1.IListEndpointsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IEndpoint
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listEndpoints values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listEndpoints request %j', request);
    return this.innerApiCalls
      .listEndpoints(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IEndpoint[],
          protos.google.cloud.agentregistry.v1.IListEndpointsRequest | null,
          protos.google.cloud.agentregistry.v1.IListEndpointsResponse,
        ]) => {
          this._log.info('listEndpoints values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listEndpoints`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list endpoints in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of endpoints returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   `version`, and `interfaces` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/endpoints/e1"`
   *     * `display_name = "my-endpoint"`
   *     * `description = "my-endpoint-description"`
   *     * `version = "v1"`
   *     * `interfaces.transport = "HTTP_JSON"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Endpoint|Endpoint} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listEndpointsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listEndpointsStream(
    request?: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listEndpoints'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listEndpoints stream %j', request);
    return this.descriptors.page.listEndpoints.createStream(
      this.innerApiCalls.listEndpoints as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listEndpoints`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list endpoints in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of endpoints returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   `version`, and `interfaces` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/endpoints/e1"`
   *     * `display_name = "my-endpoint"`
   *     * `description = "my-endpoint-description"`
   *     * `version = "v1"`
   *     * `interfaces.transport = "HTTP_JSON"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Endpoint|Endpoint}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.list_endpoints.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_ListEndpoints_async
   */
  listEndpointsAsync(
    request?: protos.google.cloud.agentregistry.v1.IListEndpointsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IEndpoint> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listEndpoints'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listEndpoints iterate %j', request);
    return this.descriptors.page.listEndpoints.asyncIterate(
      this.innerApiCalls['listEndpoints'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IEndpoint>;
  }
  /**
   * Lists McpServers in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listMcpServersAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listMcpServers(
    request?: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer[],
      protos.google.cloud.agentregistry.v1.IListMcpServersRequest | null,
      protos.google.cloud.agentregistry.v1.IListMcpServersResponse,
    ]
  >;
  listMcpServers(
    request: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.IListMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): void;
  listMcpServers(
    request: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.IListMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): void;
  listMcpServers(
    request?: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
          | protos.google.cloud.agentregistry.v1.IListMcpServersResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IMcpServer
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.IListMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer[],
      protos.google.cloud.agentregistry.v1.IListMcpServersRequest | null,
      protos.google.cloud.agentregistry.v1.IListMcpServersResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
          | protos.google.cloud.agentregistry.v1.IListMcpServersResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IMcpServer
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listMcpServers values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listMcpServers request %j', request);
    return this.innerApiCalls
      .listMcpServers(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IMcpServer[],
          protos.google.cloud.agentregistry.v1.IListMcpServersRequest | null,
          protos.google.cloud.agentregistry.v1.IListMcpServersResponse,
        ]) => {
          this._log.info('listMcpServers values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listMcpServers`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listMcpServersAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listMcpServersStream(
    request?: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listMcpServers'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listMcpServers stream %j', request);
    return this.descriptors.page.listMcpServers.createStream(
      this.innerApiCalls.listMcpServers as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listMcpServers`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for ListMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. Filtering results
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.list_mcp_servers.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_ListMcpServers_async
   */
  listMcpServersAsync(
    request?: protos.google.cloud.agentregistry.v1.IListMcpServersRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IMcpServer> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listMcpServers'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listMcpServers iterate %j', request);
    return this.descriptors.page.listMcpServers.asyncIterate(
      this.innerApiCalls['listMcpServers'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IMcpServer>;
  }
  /**
   * Searches McpServers in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the MCP Servers to return. If no
   *   search criteria is specified then all accessible MCP Servers will be
   *   returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | mcpServerId        | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *
   *   Examples:
   *
   *   * `mcpServerId="urn:mcp:projects-123:projects:123:locations:us-central1:agentregistry:services:service-id"`
   *     to find the MCP Server with the specified MCP Server ID.
   *   * `name:important` to find MCP Servers whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find MCP Servers whose display name contains
   *     words that start with `works`.
   *   * `planner OR booking` to find MCP Servers whose metadata contains the
   *     words `planner` or `booking`.
   *   * `mcpServerId:service-id AND (displayName:planner OR
   *     displayName:booking)` to find MCP Servers whose MCP Server ID contains
   *     `service-id` and whose display name contains `planner` or
   *     `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `searchMcpServersAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchMcpServers(
    request?: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer[],
      protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest | null,
      protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse,
    ]
  >;
  searchMcpServers(
    request: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): void;
  searchMcpServers(
    request: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): void;
  searchMcpServers(
    request?: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
          | protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IMcpServer
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
      | protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IMcpServer
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IMcpServer[],
      protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest | null,
      protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
          | protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IMcpServer
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('searchMcpServers values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('searchMcpServers request %j', request);
    return this.innerApiCalls
      .searchMcpServers(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IMcpServer[],
          protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest | null,
          protos.google.cloud.agentregistry.v1.ISearchMcpServersResponse,
        ]) => {
          this._log.info('searchMcpServers values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `searchMcpServers`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the MCP Servers to return. If no
   *   search criteria is specified then all accessible MCP Servers will be
   *   returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | mcpServerId        | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *
   *   Examples:
   *
   *   * `mcpServerId="urn:mcp:projects-123:projects:123:locations:us-central1:agentregistry:services:service-id"`
   *     to find the MCP Server with the specified MCP Server ID.
   *   * `name:important` to find MCP Servers whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find MCP Servers whose display name contains
   *     words that start with `works`.
   *   * `planner OR booking` to find MCP Servers whose metadata contains the
   *     words `planner` or `booking`.
   *   * `mcpServerId:service-id AND (displayName:planner OR
   *     displayName:booking)` to find MCP Servers whose MCP Server ID contains
   *     `service-id` and whose display name contains `planner` or
   *     `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `searchMcpServersAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  searchMcpServersStream(
    request?: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['searchMcpServers'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchMcpServers stream %j', request);
    return this.descriptors.page.searchMcpServers.createStream(
      this.innerApiCalls.searchMcpServers as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `searchMcpServers`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. Parent value for SearchMcpServersRequest. Format:
   *   `projects/{project}/locations/{location}`.
   * @param {string} [request.searchString]
   *   Optional. Search criteria used to select the MCP Servers to return. If no
   *   search criteria is specified then all accessible MCP Servers will be
   *   returned.
   *
   *   Search expressions can be used to restrict results based upon searchable
   *   fields, where the operators can be used along with the suffix wildcard
   *   symbol `*`. See
   *   [instructions](https://docs.cloud.google.com/agent-registry/search-agents-and-tools)
   *   for more details.
   *
   *   Allowed operators: `=`, `:`, `NOT`, `AND`, `OR`, and `()`.
   *
   *   Searchable fields:
   *
   *   | Field              | `=` | `:` | `*` | Keyword Search |
   *   |--------------------|-----|-----|-----|----------------|
   *   | mcpServerId        | Yes | Yes | Yes | Included       |
   *   | name               | No  | Yes | Yes | Included       |
   *   | displayName        | No  | Yes | Yes | Included       |
   *
   *   Examples:
   *
   *   * `mcpServerId="urn:mcp:projects-123:projects:123:locations:us-central1:agentregistry:services:service-id"`
   *     to find the MCP Server with the specified MCP Server ID.
   *   * `name:important` to find MCP Servers whose name contains `important` as a
   *     word.
   *   * `displayName:works*` to find MCP Servers whose display name contains
   *     words that start with `works`.
   *   * `planner OR booking` to find MCP Servers whose metadata contains the
   *     words `planner` or `booking`.
   *   * `mcpServerId:service-id AND (displayName:planner OR
   *     displayName:booking)` to find MCP Servers whose MCP Server ID contains
   *     `service-id` and whose display name contains `planner` or
   *     `booking`.
   * @param {number} [request.pageSize]
   *   Optional. The maximum number of search results to return per page. The page
   *   size is capped at `100`, even if a larger value is specified. A negative
   *   value will result in an `INVALID_ARGUMENT` error. If unspecified or set to
   *   `0`, a default value of `20` will be used. The server may return fewer
   *   results than requested.
   * @param {string} [request.pageToken]
   *   Optional. If present, retrieve the next batch of results from the preceding
   *   call to this method. `page_token` must be the value of `next_page_token`
   *   from the previous response. The values of all other method parameters, must
   *   be identical to those in the previous call.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.McpServer|McpServer}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.search_mcp_servers.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_SearchMcpServers_async
   */
  searchMcpServersAsync(
    request?: protos.google.cloud.agentregistry.v1.ISearchMcpServersRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IMcpServer> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['searchMcpServers'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('searchMcpServers iterate %j', request);
    return this.descriptors.page.searchMcpServers.asyncIterate(
      this.innerApiCalls['searchMcpServers'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IMcpServer>;
  }
  /**
   * Lists Services in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list services in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of services returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   and `labels` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/services/s1"`
   *     * `display_name = "my-service"`
   *     * `description : "myservice description"`
   *     * `labels.env = "prod"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Service|Service}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listServicesAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listServices(
    request?: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IService[],
      protos.google.cloud.agentregistry.v1.IListServicesRequest | null,
      protos.google.cloud.agentregistry.v1.IListServicesResponse,
    ]
  >;
  listServices(
    request: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListServicesRequest,
      | protos.google.cloud.agentregistry.v1.IListServicesResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IService
    >,
  ): void;
  listServices(
    request: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListServicesRequest,
      | protos.google.cloud.agentregistry.v1.IListServicesResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IService
    >,
  ): void;
  listServices(
    request?: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListServicesRequest,
          | protos.google.cloud.agentregistry.v1.IListServicesResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IService
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListServicesRequest,
      | protos.google.cloud.agentregistry.v1.IListServicesResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IService
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IService[],
      protos.google.cloud.agentregistry.v1.IListServicesRequest | null,
      protos.google.cloud.agentregistry.v1.IListServicesResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListServicesRequest,
          | protos.google.cloud.agentregistry.v1.IListServicesResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IService
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listServices values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listServices request %j', request);
    return this.innerApiCalls
      .listServices(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IService[],
          protos.google.cloud.agentregistry.v1.IListServicesRequest | null,
          protos.google.cloud.agentregistry.v1.IListServicesResponse,
        ]) => {
          this._log.info('listServices values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listServices`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list services in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of services returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   and `labels` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/services/s1"`
   *     * `display_name = "my-service"`
   *     * `description : "myservice description"`
   *     * `labels.env = "prod"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Service|Service} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listServicesAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listServicesStream(
    request?: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listServices'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listServices stream %j', request);
    return this.descriptors.page.listServices.createStream(
      this.innerApiCalls.listServices as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listServices`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list services in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. If unspecified, server will pick an appropriate default.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of services returned.
   *   The filter expression must follow AIP-160 syntax.
   *
   *   Filtering is supported on the `name`, `display_name`, `description`,
   *   and `labels` fields.
   *
   *   Some examples:
   *
   *     * `name = "projects/p1/locations/l1/services/s1"`
   *     * `display_name = "my-service"`
   *     * `description : "myservice description"`
   *     * `labels.env = "prod"`
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Service|Service}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.list_services.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_ListServices_async
   */
  listServicesAsync(
    request?: protos.google.cloud.agentregistry.v1.IListServicesRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IService> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listServices'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listServices iterate %j', request);
    return this.descriptors.page.listServices.asyncIterate(
      this.innerApiCalls['listServices'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IService>;
  }
  /**
   * Lists Bindings in a given project and location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list bindings in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of bindings returned.
   *   The filter expression must follow AIP-160 syntax.
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Binding|Binding}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `listBindingsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listBindings(
    request?: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding[],
      protos.google.cloud.agentregistry.v1.IListBindingsRequest | null,
      protos.google.cloud.agentregistry.v1.IListBindingsResponse,
    ]
  >;
  listBindings(
    request: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IListBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): void;
  listBindings(
    request: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IListBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): void;
  listBindings(
    request?: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListBindingsRequest,
          | protos.google.cloud.agentregistry.v1.IListBindingsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IBinding
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IListBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IListBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding[],
      protos.google.cloud.agentregistry.v1.IListBindingsRequest | null,
      protos.google.cloud.agentregistry.v1.IListBindingsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IListBindingsRequest,
          | protos.google.cloud.agentregistry.v1.IListBindingsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IBinding
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('listBindings values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('listBindings request %j', request);
    return this.innerApiCalls
      .listBindings(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IBinding[],
          protos.google.cloud.agentregistry.v1.IListBindingsRequest | null,
          protos.google.cloud.agentregistry.v1.IListBindingsResponse,
        ]) => {
          this._log.info('listBindings values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `listBindings`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list bindings in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of bindings returned.
   *   The filter expression must follow AIP-160 syntax.
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Binding|Binding} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `listBindingsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  listBindingsStream(
    request?: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listBindings'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listBindings stream %j', request);
    return this.descriptors.page.listBindings.createStream(
      this.innerApiCalls.listBindings as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `listBindings`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.parent
   *   Required. The project and location to list bindings in.
   *   Expected format: `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {string} [request.filter]
   *   Optional. A query string used to filter the list of bindings returned.
   *   The filter expression must follow AIP-160 syntax.
   * @param {string} [request.orderBy]
   *   Optional. Hint for how to order the results
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Binding|Binding}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.list_bindings.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_ListBindings_async
   */
  listBindingsAsync(
    request?: protos.google.cloud.agentregistry.v1.IListBindingsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IBinding> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['listBindings'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('listBindings iterate %j', request);
    return this.descriptors.page.listBindings.asyncIterate(
      this.innerApiCalls['listBindings'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IBinding>;
  }
  /**
   * Fetches available Bindings.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.sourceIdentifier
   *   The identifier of the source Agent.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   * @param {string} [request.targetIdentifier]
   *   Optional. The identifier of the target Agent, MCP Server, or Endpoint.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   *   * `urn:mcp:{publisher}:{namespace}:{name}`
   *   * `urn:endpoint:{publisher}:{namespace}:{name}`
   * @param {string} request.parent
   *   Required. The parent, in the format
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is Array of {@link protos.google.cloud.agentregistry.v1.Binding|Binding}.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed and will merge results from all the pages into this array.
   *   Note that it can affect your quota.
   *   We recommend using `fetchAvailableBindingsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  fetchAvailableBindings(
    request?: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    options?: CallOptions,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding[],
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest | null,
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse,
    ]
  >;
  fetchAvailableBindings(
    request: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    options: CallOptions,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): void;
  fetchAvailableBindings(
    request: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    callback: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): void;
  fetchAvailableBindings(
    request?: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    optionsOrCallback?:
      | CallOptions
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
          | protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IBinding
        >,
    callback?: PaginationCallback<
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
      | protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse
      | null
      | undefined,
      protos.google.cloud.agentregistry.v1.IBinding
    >,
  ): Promise<
    [
      protos.google.cloud.agentregistry.v1.IBinding[],
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest | null,
      protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse,
    ]
  > | void {
    request = request || {};
    let options: CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    this.initialize().catch((err) => {
      throw err;
    });
    const wrappedCallback:
      | PaginationCallback<
          protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
          | protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse
          | null
          | undefined,
          protos.google.cloud.agentregistry.v1.IBinding
        >
      | undefined = callback
      ? (error, values, nextPageRequest, rawResponse) => {
          this._log.info('fetchAvailableBindings values %j', values);
          callback!(error, values, nextPageRequest, rawResponse); // We verified callback above.
        }
      : undefined;
    this._log.info('fetchAvailableBindings request %j', request);
    return this.innerApiCalls
      .fetchAvailableBindings(request, options, wrappedCallback)
      ?.then(
        ([response, input, output]: [
          protos.google.cloud.agentregistry.v1.IBinding[],
          protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest | null,
          protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsResponse,
        ]) => {
          this._log.info('fetchAvailableBindings values %j', response);
          return [response, input, output];
        },
      );
  }

  /**
   * Equivalent to `fetchAvailableBindings`, but returns a NodeJS Stream object.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.sourceIdentifier
   *   The identifier of the source Agent.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   * @param {string} [request.targetIdentifier]
   *   Optional. The identifier of the target Agent, MCP Server, or Endpoint.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   *   * `urn:mcp:{publisher}:{namespace}:{name}`
   *   * `urn:endpoint:{publisher}:{namespace}:{name}`
   * @param {string} request.parent
   *   Required. The parent, in the format
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Stream}
   *   An object stream which emits an object representing {@link protos.google.cloud.agentregistry.v1.Binding|Binding} on 'data' event.
   *   The client library will perform auto-pagination by default: it will call the API as many
   *   times as needed. Note that it can affect your quota.
   *   We recommend using `fetchAvailableBindingsAsync()`
   *   method described below for async iteration which you can stop as needed.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   */
  fetchAvailableBindingsStream(
    request?: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    options?: CallOptions,
  ): Transform {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['fetchAvailableBindings'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('fetchAvailableBindings stream %j', request);
    return this.descriptors.page.fetchAvailableBindings.createStream(
      this.innerApiCalls.fetchAvailableBindings as GaxCall,
      request,
      callSettings,
    );
  }

  /**
   * Equivalent to `fetchAvailableBindings`, but returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.sourceIdentifier
   *   The identifier of the source Agent.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   * @param {string} [request.targetIdentifier]
   *   Optional. The identifier of the target Agent, MCP Server, or Endpoint.
   *   Format:
   *
   *   * `urn:agent:{publisher}:{namespace}:{name}`
   *   * `urn:mcp:{publisher}:{namespace}:{name}`
   *   * `urn:endpoint:{publisher}:{namespace}:{name}`
   * @param {string} request.parent
   *   Required. The parent, in the format
   *   `projects/{project}/locations/{location}`.
   * @param {number} [request.pageSize]
   *   Optional. Requested page size. Server may return fewer items than
   *   requested. Page size is 500 if unspecified and is capped at `500` even if a
   *   larger value is given.
   * @param {string} [request.pageToken]
   *   Optional. A token identifying a page of results the server should return.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link protos.google.cloud.agentregistry.v1.Binding|Binding}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example <caption>include:samples/generated/v1/agent_registry.fetch_available_bindings.js</caption>
   * region_tag:agentregistry_v1_generated_AgentRegistry_FetchAvailableBindings_async
   */
  fetchAvailableBindingsAsync(
    request?: protos.google.cloud.agentregistry.v1.IFetchAvailableBindingsRequest,
    options?: CallOptions,
  ): AsyncIterable<protos.google.cloud.agentregistry.v1.IBinding> {
    request = request || {};
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        parent: request.parent ?? '',
      });
    const defaultCallSettings = this._defaults['fetchAvailableBindings'];
    const callSettings = defaultCallSettings.merge(options);
    this.initialize().catch((err) => {
      throw err;
    });
    this._log.info('fetchAvailableBindings iterate %j', request);
    return this.descriptors.page.fetchAvailableBindings.asyncIterate(
      this.innerApiCalls['fetchAvailableBindings'] as GaxCall,
      request as {},
      callSettings,
    ) as AsyncIterable<protos.google.cloud.agentregistry.v1.IBinding>;
  }

  /**
   * Gets information about a location.
   *
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   Resource name for the location.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html | CallOptions} for more details.
   * @returns {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing {@link google.cloud.location.Location | Location}.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#regular-methods | documentation }
   *   for more details and examples.
   * @example
   * ```
   * const [response] = await client.getLocation(request);
   * ```
   */
  getLocation(
    request: LocationProtos.google.cloud.location.IGetLocationRequest,
    options?:
      | gax.CallOptions
      | Callback<
          LocationProtos.google.cloud.location.ILocation,
          | LocationProtos.google.cloud.location.IGetLocationRequest
          | null
          | undefined,
          {} | null | undefined
        >,
    callback?: Callback<
      LocationProtos.google.cloud.location.ILocation,
      | LocationProtos.google.cloud.location.IGetLocationRequest
      | null
      | undefined,
      {} | null | undefined
    >,
  ): Promise<LocationProtos.google.cloud.location.ILocation> {
    return this.locationsClient.getLocation(request, options, callback);
  }
  /**
   * Lists information about the supported locations for this service. Returns an iterable object.
   *
   * `for`-`await`-`of` syntax is used with the iterable to get response elements on-demand.
   * @param {Object} request
   *   The request object that will be sent.
   * @param {string} request.name
   *   The resource that owns the locations collection, if applicable.
   * @param {string} request.filter
   *   The standard list filter.
   * @param {number} request.pageSize
   *   The standard list page size.
   * @param {string} request.pageToken
   *   The standard list page token.
   * @param {object} [options]
   *   Call options. See {@link https://googleapis.dev/nodejs/google-gax/latest/interfaces/CallOptions.html|CallOptions} for more details.
   * @returns {Object}
   *   An iterable Object that allows {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | async iteration }.
   *   When you iterate the returned iterable, each element will be an object representing
   *   {@link google.cloud.location.Location | Location}. The API will be called under the hood as needed, once per the page,
   *   so you can stop the iteration when you don't need more results.
   *   Please see the {@link https://github.com/googleapis/gax-nodejs/blob/master/client-libraries.md#auto-pagination | documentation }
   *   for more details and examples.
   * @example
   * ```
   * const iterable = client.listLocationsAsync(request);
   * for await (const response of iterable) {
   *   // process response
   * }
   * ```
   */
  listLocationsAsync(
    request: LocationProtos.google.cloud.location.IListLocationsRequest,
    options?: CallOptions,
  ): AsyncIterable<LocationProtos.google.cloud.location.ILocation> {
    return this.locationsClient.listLocationsAsync(request, options);
  }

  /**
   * Gets the latest state of a long-running operation.  Clients can use this
   * method to poll the operation result at intervals as recommended by the API
   * service.
   *
   * @param {Object} request - The request object that will be sent.
   * @param {string} request.name - The name of the operation resource.
   * @param {Object=} options
   *   Optional parameters. You can override the default settings for this call,
   *   e.g, timeout, retries, paginations, etc. See {@link
   *   https://googleapis.github.io/gax-nodejs/global.html#CallOptions | gax.CallOptions}
   *   for the details.
   * @param {function(?Error, ?Object)=} callback
   *   The function which will be called with the result of the API call.
   *
   *   The second parameter to the callback is an object representing
   *   {@link google.longrunning.Operation | google.longrunning.Operation}.
   * @return {Promise} - The promise which resolves to an array.
   *   The first element of the array is an object representing
   * {@link google.longrunning.Operation | google.longrunning.Operation}.
   * The promise has a method named "cancel" which cancels the ongoing API call.
   *
   * @example
   * ```
   * const client = longrunning.operationsClient();
   * const name = '';
   * const [response] = await client.getOperation({name});
   * // doThingsWith(response)
   * ```
   */
  getOperation(
    request: protos.google.longrunning.GetOperationRequest,
    optionsOrCallback?:
      | gax.CallOptions
      | Callback<
          protos.google.longrunning.Operation,
          protos.google.longrunning.GetOperationRequest,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.longrunning.Operation,
      protos.google.longrunning.GetOperationRequest,
      {} | null | undefined
    >,
  ): Promise<[protos.google.longrunning.Operation]> {
    let options: gax.CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as gax.CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    return this.operationsClient.getOperation(request, options, callback);
  }
  /**
   * Lists operations that match the specified filter in the request. If the
   * server doesn't support this method, it returns `UNIMPLEMENTED`. Returns an iterable object.
   *
   * For-await-of syntax is used with the iterable to recursively get response element on-demand.
   *
   * @param {Object} request - The request object that will be sent.
   * @param {string} request.name - The name of the operation collection.
   * @param {string} request.filter - The standard list filter.
   * @param {number=} request.pageSize -
   *   The maximum number of resources contained in the underlying API
   *   response. If page streaming is performed per-resource, this
   *   parameter does not affect the return value. If page streaming is
   *   performed per-page, this determines the maximum number of
   *   resources in a page.
   * @param {Object=} options
   *   Optional parameters. You can override the default settings for this call,
   *   e.g, timeout, retries, paginations, etc. See {@link
   *   https://googleapis.github.io/gax-nodejs/global.html#CallOptions | gax.CallOptions} for the
   *   details.
   * @returns {Object}
   *   An iterable Object that conforms to {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols | iteration protocols}.
   *
   * @example
   * ```
   * const client = longrunning.operationsClient();
   * for await (const response of client.listOperationsAsync(request));
   * // doThingsWith(response)
   * ```
   */
  listOperationsAsync(
    request: protos.google.longrunning.ListOperationsRequest,
    options?: gax.CallOptions,
  ): AsyncIterable<protos.google.longrunning.IOperation> {
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    return this.operationsClient.listOperationsAsync(request, options);
  }
  /**
   * Starts asynchronous cancellation on a long-running operation.  The server
   * makes a best effort to cancel the operation, but success is not
   * guaranteed.  If the server doesn't support this method, it returns
   * `google.rpc.Code.UNIMPLEMENTED`.  Clients can use
   * {@link Operations.GetOperation} or
   * other methods to check whether the cancellation succeeded or whether the
   * operation completed despite cancellation. On successful cancellation,
   * the operation is not deleted; instead, it becomes an operation with
   * an {@link Operation.error} value with a {@link google.rpc.Status.code} of
   * 1, corresponding to `Code.CANCELLED`.
   *
   * @param {Object} request - The request object that will be sent.
   * @param {string} request.name - The name of the operation resource to be cancelled.
   * @param {Object=} options
   *   Optional parameters. You can override the default settings for this call,
   * e.g, timeout, retries, paginations, etc. See {@link
   * https://googleapis.github.io/gax-nodejs/global.html#CallOptions | gax.CallOptions} for the
   * details.
   * @param {function(?Error)=} callback
   *   The function which will be called with the result of the API call.
   * @return {Promise} - The promise which resolves when API call finishes.
   *   The promise has a method named "cancel" which cancels the ongoing API
   * call.
   *
   * @example
   * ```
   * const client = longrunning.operationsClient();
   * await client.cancelOperation({name: ''});
   * ```
   */
  cancelOperation(
    request: protos.google.longrunning.CancelOperationRequest,
    optionsOrCallback?:
      | gax.CallOptions
      | Callback<
          protos.google.longrunning.CancelOperationRequest,
          protos.google.protobuf.Empty,
          {} | undefined | null
        >,
    callback?: Callback<
      protos.google.longrunning.CancelOperationRequest,
      protos.google.protobuf.Empty,
      {} | undefined | null
    >,
  ): Promise<protos.google.protobuf.Empty> {
    let options: gax.CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as gax.CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    return this.operationsClient.cancelOperation(request, options, callback);
  }
  /**
   * Deletes a long-running operation. This method indicates that the client is
   * no longer interested in the operation result. It does not cancel the
   * operation. If the server doesn't support this method, it returns
   * `google.rpc.Code.UNIMPLEMENTED`.
   *
   * @param {Object} request - The request object that will be sent.
   * @param {string} request.name - The name of the operation resource to be deleted.
   * @param {Object=} options
   *   Optional parameters. You can override the default settings for this call,
   * e.g, timeout, retries, paginations, etc. See {@link
   * https://googleapis.github.io/gax-nodejs/global.html#CallOptions | gax.CallOptions}
   * for the details.
   * @param {function(?Error)=} callback
   *   The function which will be called with the result of the API call.
   * @return {Promise} - The promise which resolves when API call finishes.
   *   The promise has a method named "cancel" which cancels the ongoing API
   * call.
   *
   * @example
   * ```
   * const client = longrunning.operationsClient();
   * await client.deleteOperation({name: ''});
   * ```
   */
  deleteOperation(
    request: protos.google.longrunning.DeleteOperationRequest,
    optionsOrCallback?:
      | gax.CallOptions
      | Callback<
          protos.google.protobuf.Empty,
          protos.google.longrunning.DeleteOperationRequest,
          {} | null | undefined
        >,
    callback?: Callback<
      protos.google.protobuf.Empty,
      protos.google.longrunning.DeleteOperationRequest,
      {} | null | undefined
    >,
  ): Promise<protos.google.protobuf.Empty> {
    let options: gax.CallOptions;
    if (typeof optionsOrCallback === 'function' && callback === undefined) {
      callback = optionsOrCallback;
      options = {};
    } else {
      options = optionsOrCallback as gax.CallOptions;
    }
    options = options || {};
    options.otherArgs = options.otherArgs || {};
    options.otherArgs.headers = options.otherArgs.headers || {};
    options.otherArgs.headers['x-goog-request-params'] =
      this._gaxModule.routingHeader.fromParams({
        name: request.name ?? '',
      });
    return this.operationsClient.deleteOperation(request, options, callback);
  }

  // --------------------
  // -- Path templates --
  // --------------------

  /**
   * Return a fully-qualified agent resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} agent
   * @returns {string} Resource name string.
   */
  agentPath(project: string, location: string, agent: string) {
    return this.pathTemplates.agentPathTemplate.render({
      project: project,
      location: location,
      agent: agent,
    });
  }

  /**
   * Parse the project from Agent resource.
   *
   * @param {string} agentName
   *   A fully-qualified path representing Agent resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromAgentName(agentName: string) {
    return this.pathTemplates.agentPathTemplate.match(agentName).project;
  }

  /**
   * Parse the location from Agent resource.
   *
   * @param {string} agentName
   *   A fully-qualified path representing Agent resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromAgentName(agentName: string) {
    return this.pathTemplates.agentPathTemplate.match(agentName).location;
  }

  /**
   * Parse the agent from Agent resource.
   *
   * @param {string} agentName
   *   A fully-qualified path representing Agent resource.
   * @returns {string} A string representing the agent.
   */
  matchAgentFromAgentName(agentName: string) {
    return this.pathTemplates.agentPathTemplate.match(agentName).agent;
  }

  /**
   * Return a fully-qualified binding resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} binding
   * @returns {string} Resource name string.
   */
  bindingPath(project: string, location: string, binding: string) {
    return this.pathTemplates.bindingPathTemplate.render({
      project: project,
      location: location,
      binding: binding,
    });
  }

  /**
   * Parse the project from Binding resource.
   *
   * @param {string} bindingName
   *   A fully-qualified path representing Binding resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromBindingName(bindingName: string) {
    return this.pathTemplates.bindingPathTemplate.match(bindingName).project;
  }

  /**
   * Parse the location from Binding resource.
   *
   * @param {string} bindingName
   *   A fully-qualified path representing Binding resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromBindingName(bindingName: string) {
    return this.pathTemplates.bindingPathTemplate.match(bindingName).location;
  }

  /**
   * Parse the binding from Binding resource.
   *
   * @param {string} bindingName
   *   A fully-qualified path representing Binding resource.
   * @returns {string} A string representing the binding.
   */
  matchBindingFromBindingName(bindingName: string) {
    return this.pathTemplates.bindingPathTemplate.match(bindingName).binding;
  }

  /**
   * Return a fully-qualified endpoint resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} endpoint
   * @returns {string} Resource name string.
   */
  endpointPath(project: string, location: string, endpoint: string) {
    return this.pathTemplates.endpointPathTemplate.render({
      project: project,
      location: location,
      endpoint: endpoint,
    });
  }

  /**
   * Parse the project from Endpoint resource.
   *
   * @param {string} endpointName
   *   A fully-qualified path representing Endpoint resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromEndpointName(endpointName: string) {
    return this.pathTemplates.endpointPathTemplate.match(endpointName).project;
  }

  /**
   * Parse the location from Endpoint resource.
   *
   * @param {string} endpointName
   *   A fully-qualified path representing Endpoint resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromEndpointName(endpointName: string) {
    return this.pathTemplates.endpointPathTemplate.match(endpointName).location;
  }

  /**
   * Parse the endpoint from Endpoint resource.
   *
   * @param {string} endpointName
   *   A fully-qualified path representing Endpoint resource.
   * @returns {string} A string representing the endpoint.
   */
  matchEndpointFromEndpointName(endpointName: string) {
    return this.pathTemplates.endpointPathTemplate.match(endpointName).endpoint;
  }

  /**
   * Return a fully-qualified location resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @returns {string} Resource name string.
   */
  locationPath(project: string, location: string) {
    return this.pathTemplates.locationPathTemplate.render({
      project: project,
      location: location,
    });
  }

  /**
   * Parse the project from Location resource.
   *
   * @param {string} locationName
   *   A fully-qualified path representing Location resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromLocationName(locationName: string) {
    return this.pathTemplates.locationPathTemplate.match(locationName).project;
  }

  /**
   * Parse the location from Location resource.
   *
   * @param {string} locationName
   *   A fully-qualified path representing Location resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromLocationName(locationName: string) {
    return this.pathTemplates.locationPathTemplate.match(locationName).location;
  }

  /**
   * Return a fully-qualified mcpServer resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} mcp_server
   * @returns {string} Resource name string.
   */
  mcpServerPath(project: string, location: string, mcpServer: string) {
    return this.pathTemplates.mcpServerPathTemplate.render({
      project: project,
      location: location,
      mcp_server: mcpServer,
    });
  }

  /**
   * Parse the project from McpServer resource.
   *
   * @param {string} mcpServerName
   *   A fully-qualified path representing McpServer resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromMcpServerName(mcpServerName: string) {
    return this.pathTemplates.mcpServerPathTemplate.match(mcpServerName)
      .project;
  }

  /**
   * Parse the location from McpServer resource.
   *
   * @param {string} mcpServerName
   *   A fully-qualified path representing McpServer resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromMcpServerName(mcpServerName: string) {
    return this.pathTemplates.mcpServerPathTemplate.match(mcpServerName)
      .location;
  }

  /**
   * Parse the mcp_server from McpServer resource.
   *
   * @param {string} mcpServerName
   *   A fully-qualified path representing McpServer resource.
   * @returns {string} A string representing the mcp_server.
   */
  matchMcpServerFromMcpServerName(mcpServerName: string) {
    return this.pathTemplates.mcpServerPathTemplate.match(mcpServerName)
      .mcp_server;
  }

  /**
   * Return a fully-qualified project resource name string.
   *
   * @param {string} project
   * @returns {string} Resource name string.
   */
  projectPath(project: string) {
    return this.pathTemplates.projectPathTemplate.render({
      project: project,
    });
  }

  /**
   * Parse the project from Project resource.
   *
   * @param {string} projectName
   *   A fully-qualified path representing Project resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromProjectName(projectName: string) {
    return this.pathTemplates.projectPathTemplate.match(projectName).project;
  }

  /**
   * Return a fully-qualified service resource name string.
   *
   * @param {string} project
   * @param {string} location
   * @param {string} service
   * @returns {string} Resource name string.
   */
  servicePath(project: string, location: string, service: string) {
    return this.pathTemplates.servicePathTemplate.render({
      project: project,
      location: location,
      service: service,
    });
  }

  /**
   * Parse the project from Service resource.
   *
   * @param {string} serviceName
   *   A fully-qualified path representing Service resource.
   * @returns {string} A string representing the project.
   */
  matchProjectFromServiceName(serviceName: string) {
    return this.pathTemplates.servicePathTemplate.match(serviceName).project;
  }

  /**
   * Parse the location from Service resource.
   *
   * @param {string} serviceName
   *   A fully-qualified path representing Service resource.
   * @returns {string} A string representing the location.
   */
  matchLocationFromServiceName(serviceName: string) {
    return this.pathTemplates.servicePathTemplate.match(serviceName).location;
  }

  /**
   * Parse the service from Service resource.
   *
   * @param {string} serviceName
   *   A fully-qualified path representing Service resource.
   * @returns {string} A string representing the service.
   */
  matchServiceFromServiceName(serviceName: string) {
    return this.pathTemplates.servicePathTemplate.match(serviceName).service;
  }

  /**
   * Terminate the gRPC channel and close the client.
   *
   * The client will no longer be usable and all future behavior is undefined.
   * @returns {Promise} A promise that resolves when the client is closed.
   */
  close(): Promise<void> {
    if (this.agentRegistryStub && !this._terminated) {
      return this.agentRegistryStub.then((stub) => {
        this._log.info('ending gRPC channel');
        this._terminated = true;
        stub.close();
        this.locationsClient.close().catch((err) => {
          throw err;
        });
        void this.operationsClient.close();
      });
    }
    return Promise.resolve();
  }
}
