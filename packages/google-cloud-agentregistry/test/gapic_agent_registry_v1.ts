// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// ** This file is automatically generated by gapic-generator-typescript. **
// ** https://github.com/googleapis/gapic-generator-typescript **
// ** All changes to this file may be overwritten. **

import * as protos from '../protos/protos';
import * as assert from 'assert';
import * as sinon from 'sinon';
import { SinonStub } from 'sinon';
import { describe, it } from 'mocha';
import * as agentregistryModule from '../src';

import { PassThrough } from 'stream';

import {
  protobuf,
  LROperation,
  operationsProtos,
  LocationProtos,
} from 'google-gax';

// Dynamically loaded proto JSON is needed to get the type information
// to fill in default values for request objects
const root = protobuf.Root.fromJSON(
  require('../protos/protos.json'),
).resolveAll();

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getTypeDefaultValue(typeName: string, fields: string[]) {
  let type = root.lookupType(typeName) as protobuf.Type;
  for (const field of fields.slice(0, -1)) {
    type = type.fields[field]?.resolvedType as protobuf.Type;
  }
  return type.fields[fields[fields.length - 1]]?.defaultValue;
}

function generateSampleMessage<T extends object>(instance: T) {
  const filledObject = (
    instance.constructor as typeof protobuf.Message
  ).toObject(instance as protobuf.Message<T>, { defaults: true });
  return (instance.constructor as typeof protobuf.Message).fromObject(
    filledObject,
  ) as T;
}

function stubSimpleCall<ResponseType>(response?: ResponseType, error?: Error) {
  return error
    ? sinon.stub().rejects(error)
    : sinon.stub().resolves([response]);
}

function stubSimpleCallWithCallback<ResponseType>(
  response?: ResponseType,
  error?: Error,
) {
  return error
    ? sinon.stub().callsArgWith(2, error)
    : sinon.stub().callsArgWith(2, null, response);
}

function stubLongRunningCall<ResponseType>(
  response?: ResponseType,
  callError?: Error,
  lroError?: Error,
) {
  const innerStub = lroError
    ? sinon.stub().rejects(lroError)
    : sinon.stub().resolves([response]);
  const mockOperation = {
    promise: innerStub,
  };
  return callError
    ? sinon.stub().rejects(callError)
    : sinon.stub().resolves([mockOperation]);
}

function stubLongRunningCallWithCallback<ResponseType>(
  response?: ResponseType,
  callError?: Error,
  lroError?: Error,
) {
  const innerStub = lroError
    ? sinon.stub().rejects(lroError)
    : sinon.stub().resolves([response]);
  const mockOperation = {
    promise: innerStub,
  };
  return callError
    ? sinon.stub().callsArgWith(2, callError)
    : sinon.stub().callsArgWith(2, null, mockOperation);
}

function stubPageStreamingCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error,
) {
  const pagingStub = sinon.stub();
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      pagingStub.onCall(i).callsArgWith(2, null, responses[i]);
    }
  }
  const transformStub = error
    ? sinon.stub().callsArgWith(2, error)
    : pagingStub;
  const mockStream = new PassThrough({
    objectMode: true,
    transform: transformStub,
  });
  // trigger as many responses as needed
  if (responses) {
    for (let i = 0; i < responses.length; ++i) {
      setImmediate(() => {
        mockStream.write({});
      });
    }
    setImmediate(() => {
      mockStream.end();
    });
  } else {
    setImmediate(() => {
      mockStream.write({});
    });
    setImmediate(() => {
      mockStream.end();
    });
  }
  return sinon.stub().returns(mockStream);
}

function stubAsyncIterationCall<ResponseType>(
  responses?: ResponseType[],
  error?: Error,
) {
  let counter = 0;
  const asyncIterable = {
    [Symbol.asyncIterator]() {
      return {
        async next() {
          if (error) {
            return Promise.reject(error);
          }
          if (counter >= responses!.length) {
            return Promise.resolve({ done: true, value: undefined });
          }
          return Promise.resolve({ done: false, value: responses![counter++] });
        },
      };
    },
  };
  return sinon.stub().returns(asyncIterable);
}

describe('v1.AgentRegistryClient', () => {
  describe('Common methods', () => {
    it('has apiEndpoint', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient();
      const apiEndpoint = client.apiEndpoint;
      assert.strictEqual(apiEndpoint, 'agentregistry.googleapis.com');
    });

    it('has universeDomain', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient();
      const universeDomain = client.universeDomain;
      assert.strictEqual(universeDomain, 'googleapis.com');
    });

    if (
      typeof process === 'object' &&
      typeof process.emitWarning === 'function'
    ) {
      it('throws DeprecationWarning if static servicePath is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const servicePath =
          agentregistryModule.v1.AgentRegistryClient.servicePath;
        assert.strictEqual(servicePath, 'agentregistry.googleapis.com');
        assert(stub.called);
        stub.restore();
      });

      it('throws DeprecationWarning if static apiEndpoint is used', () => {
        const stub = sinon.stub(process, 'emitWarning');
        const apiEndpoint =
          agentregistryModule.v1.AgentRegistryClient.apiEndpoint;
        assert.strictEqual(apiEndpoint, 'agentregistry.googleapis.com');
        assert(stub.called);
        stub.restore();
      });
    }
    it('sets apiEndpoint according to universe domain camelCase', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        universeDomain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'agentregistry.example.com');
    });

    it('sets apiEndpoint according to universe domain snakeCase', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        universe_domain: 'example.com',
      });
      const servicePath = client.apiEndpoint;
      assert.strictEqual(servicePath, 'agentregistry.example.com');
    });

    if (typeof process === 'object' && 'env' in process) {
      describe('GOOGLE_CLOUD_UNIVERSE_DOMAIN environment variable', () => {
        it('sets apiEndpoint from environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new agentregistryModule.v1.AgentRegistryClient();
          const servicePath = client.apiEndpoint;
          assert.strictEqual(servicePath, 'agentregistry.example.com');
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });

        it('value configured in code has priority over environment variable', () => {
          const saved = process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = 'example.com';
          const client = new agentregistryModule.v1.AgentRegistryClient({
            universeDomain: 'configured.example.com',
          });
          const servicePath = client.apiEndpoint;
          assert.strictEqual(
            servicePath,
            'agentregistry.configured.example.com',
          );
          if (saved) {
            process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'] = saved;
          } else {
            delete process.env['GOOGLE_CLOUD_UNIVERSE_DOMAIN'];
          }
        });
      });
    }
    it('does not allow setting both universeDomain and universe_domain', () => {
      assert.throws(() => {
        new agentregistryModule.v1.AgentRegistryClient({
          universe_domain: 'example.com',
          universeDomain: 'example.net',
        });
      });
    });

    it('has port', () => {
      const port = agentregistryModule.v1.AgentRegistryClient.port;
      assert(port);
      assert(typeof port === 'number');
    });

    it('should create a client with no option', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient();
      assert(client);
    });

    it('should create a client with gRPC fallback', () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        fallback: true,
      });
      assert(client);
    });

    it('has initialize method and supports deferred initialization', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      assert.strictEqual(client.agentRegistryStub, undefined);
      await client.initialize();
      assert(client.agentRegistryStub);
    });

    it('has close method for the initialized client', (done) => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      client.initialize().catch((err) => {
        throw err;
      });
      assert(client.agentRegistryStub);
      client
        .close()
        .then(() => {
          done();
        })
        .catch((err) => {
          throw err;
        });
    });

    it('has close method for the non-initialized client', (done) => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      assert.strictEqual(client.agentRegistryStub, undefined);
      client
        .close()
        .then(() => {
          done();
        })
        .catch((err) => {
          throw err;
        });
    });

    it('has getProjectId method', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon.stub().resolves(fakeProjectId);
      const result = await client.getProjectId();
      assert.strictEqual(result, fakeProjectId);
      assert((client.auth.getProjectId as SinonStub).calledWithExactly());
    });

    it('has getProjectId method with callback', async () => {
      const fakeProjectId = 'fake-project-id';
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      client.auth.getProjectId = sinon
        .stub()
        .callsArgWith(0, null, fakeProjectId);
      const promise = new Promise((resolve, reject) => {
        client.getProjectId((err?: Error | null, projectId?: string | null) => {
          if (err) {
            reject(err);
          } else {
            resolve(projectId);
          }
        });
      });
      const result = await promise;
      assert.strictEqual(result, fakeProjectId);
    });
  });

  describe('getAgent', () => {
    it('invokes getAgent without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetAgentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetAgentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Agent(),
      );
      client.innerApiCalls.getAgent = stubSimpleCall(expectedResponse);
      const [response] = await client.getAgent(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getAgent without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetAgentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetAgentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Agent(),
      );
      client.innerApiCalls.getAgent =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getAgent(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IAgent | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getAgent with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetAgentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetAgentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getAgent = stubSimpleCall(undefined, expectedError);
      await assert.rejects(client.getAgent(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getAgent as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getAgent with closed client', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetAgentRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetAgentRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch((err) => {
        throw err;
      });
      await assert.rejects(client.getAgent(request), expectedError);
    });
  });

  describe('getEndpoint', () => {
    it('invokes getEndpoint without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetEndpointRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetEndpointRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Endpoint(),
      );
      client.innerApiCalls.getEndpoint = stubSimpleCall(expectedResponse);
      const [response] = await client.getEndpoint(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getEndpoint without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetEndpointRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetEndpointRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Endpoint(),
      );
      client.innerApiCalls.getEndpoint =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getEndpoint(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IEndpoint | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getEndpoint with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetEndpointRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetEndpointRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getEndpoint = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.getEndpoint(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getEndpoint as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getEndpoint with closed client', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetEndpointRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetEndpointRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch((err) => {
        throw err;
      });
      await assert.rejects(client.getEndpoint(request), expectedError);
    });
  });

  describe('getMcpServer', () => {
    it('invokes getMcpServer without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetMcpServerRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetMcpServerRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.McpServer(),
      );
      client.innerApiCalls.getMcpServer = stubSimpleCall(expectedResponse);
      const [response] = await client.getMcpServer(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getMcpServer without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetMcpServerRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetMcpServerRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.McpServer(),
      );
      client.innerApiCalls.getMcpServer =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getMcpServer(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IMcpServer | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getMcpServer with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetMcpServerRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetMcpServerRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getMcpServer = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.getMcpServer(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getMcpServer as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getMcpServer with closed client', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetMcpServerRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetMcpServerRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch((err) => {
        throw err;
      });
      await assert.rejects(client.getMcpServer(request), expectedError);
    });
  });

  describe('getService', () => {
    it('invokes getService without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Service(),
      );
      client.innerApiCalls.getService = stubSimpleCall(expectedResponse);
      const [response] = await client.getService(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getService without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Service(),
      );
      client.innerApiCalls.getService =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getService(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IService | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getService with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getService = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.getService(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getService with closed client', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch((err) => {
        throw err;
      });
      await assert.rejects(client.getService(request), expectedError);
    });
  });

  describe('getBinding', () => {
    it('invokes getBinding without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Binding(),
      );
      client.innerApiCalls.getBinding = stubSimpleCall(expectedResponse);
      const [response] = await client.getBinding(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getBinding without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.Binding(),
      );
      client.innerApiCalls.getBinding =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getBinding(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IBinding | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getBinding with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.getBinding = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.getBinding(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.getBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes getBinding with closed client', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.GetBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.GetBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedError = new Error('The client has already been closed.');
      client.close().catch((err) => {
        throw err;
      });
      await assert.rejects(client.getBinding(request), expectedError);
    });
  });

  describe('createService', () => {
    it('invokes createService without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateServiceRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.createService =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.createService(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createService without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateServiceRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.createService =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.createService(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.cloud.agentregistry.v1.IService,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createService with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateServiceRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.createService = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.createService(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createService with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateServiceRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.createService = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.createService(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkCreateServiceProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkCreateServiceProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkCreateServiceProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkCreateServiceProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('updateService', () => {
    it('invokes updateService without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateServiceRequest(),
      );
      request.service ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateServiceRequest',
        ['service', 'name'],
      );
      request.service.name = defaultValue1;
      const expectedHeaderRequestParams = `service.name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.updateService =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.updateService(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateService without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateServiceRequest(),
      );
      request.service ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateServiceRequest',
        ['service', 'name'],
      );
      request.service.name = defaultValue1;
      const expectedHeaderRequestParams = `service.name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.updateService =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.updateService(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.cloud.agentregistry.v1.IService,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.cloud.agentregistry.v1.IService,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateService with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateServiceRequest(),
      );
      request.service ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateServiceRequest',
        ['service', 'name'],
      );
      request.service.name = defaultValue1;
      const expectedHeaderRequestParams = `service.name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.updateService = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.updateService(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateService with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateServiceRequest(),
      );
      request.service ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateServiceRequest',
        ['service', 'name'],
      );
      request.service.name = defaultValue1;
      const expectedHeaderRequestParams = `service.name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.updateService = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.updateService(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkUpdateServiceProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkUpdateServiceProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkUpdateServiceProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkUpdateServiceProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('deleteService', () => {
    it('invokes deleteService without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.deleteService =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.deleteService(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteService without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.deleteService =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.deleteService(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.protobuf.IEmpty,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteService with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.deleteService = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.deleteService(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteService with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteServiceRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteServiceRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.deleteService = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.deleteService(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteService as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkDeleteServiceProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkDeleteServiceProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkDeleteServiceProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkDeleteServiceProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('createBinding', () => {
    it('invokes createBinding without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateBindingRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.createBinding =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.createBinding(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createBinding without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateBindingRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.createBinding =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.createBinding(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.cloud.agentregistry.v1.IBinding,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createBinding with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateBindingRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.createBinding = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.createBinding(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes createBinding with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.CreateBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.CreateBindingRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.createBinding = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.createBinding(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.createBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkCreateBindingProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkCreateBindingProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkCreateBindingProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkCreateBindingProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('updateBinding', () => {
    it('invokes updateBinding without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateBindingRequest(),
      );
      request.binding ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateBindingRequest',
        ['binding', 'name'],
      );
      request.binding.name = defaultValue1;
      const expectedHeaderRequestParams = `binding.name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.updateBinding =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.updateBinding(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateBinding without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateBindingRequest(),
      );
      request.binding ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateBindingRequest',
        ['binding', 'name'],
      );
      request.binding.name = defaultValue1;
      const expectedHeaderRequestParams = `binding.name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.updateBinding =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.updateBinding(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.cloud.agentregistry.v1.IBinding,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.cloud.agentregistry.v1.IBinding,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateBinding with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateBindingRequest(),
      );
      request.binding ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateBindingRequest',
        ['binding', 'name'],
      );
      request.binding.name = defaultValue1;
      const expectedHeaderRequestParams = `binding.name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.updateBinding = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.updateBinding(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes updateBinding with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.UpdateBindingRequest(),
      );
      request.binding ??= {};
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.UpdateBindingRequest',
        ['binding', 'name'],
      );
      request.binding.name = defaultValue1;
      const expectedHeaderRequestParams = `binding.name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.updateBinding = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.updateBinding(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.updateBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkUpdateBindingProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkUpdateBindingProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkUpdateBindingProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkUpdateBindingProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('deleteBinding', () => {
    it('invokes deleteBinding without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.deleteBinding =
        stubLongRunningCall(expectedResponse);
      const [operation] = await client.deleteBinding(request);
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteBinding without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedResponse = generateSampleMessage(
        new protos.google.longrunning.Operation(),
      );
      client.innerApiCalls.deleteBinding =
        stubLongRunningCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.deleteBinding(
          request,
          (
            err?: Error | null,
            result?: LROperation<
              protos.google.protobuf.IEmpty,
              protos.google.cloud.agentregistry.v1.IOperationMetadata
            > | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const operation = (await promise) as LROperation<
        protos.google.protobuf.IEmpty,
        protos.google.cloud.agentregistry.v1.IOperationMetadata
      >;
      const [response] = await operation.promise();
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteBinding with call error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.deleteBinding = stubLongRunningCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.deleteBinding(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes deleteBinding with LRO error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.DeleteBindingRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.DeleteBindingRequest',
        ['name'],
      );
      request.name = defaultValue1;
      const expectedHeaderRequestParams = `name=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.deleteBinding = stubLongRunningCall(
        undefined,
        undefined,
        expectedError,
      );
      const [operation] = await client.deleteBinding(request);
      await assert.rejects(operation.promise(), expectedError);
      const actualRequest = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.deleteBinding as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes checkDeleteBindingProgress without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      expectedResponse.name = 'test';
      expectedResponse.response = { type_url: 'url', value: Buffer.from('') };
      expectedResponse.metadata = { type_url: 'url', value: Buffer.from('') };

      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const decodedOperation = await client.checkDeleteBindingProgress(
        expectedResponse.name,
      );
      assert.deepStrictEqual(decodedOperation.name, expectedResponse.name);
      assert(decodedOperation.metadata);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });

    it('invokes checkDeleteBindingProgress with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const expectedError = new Error('expected');

      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.checkDeleteBindingProgress(''),
        expectedError,
      );
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
  });

  describe('listAgents', () => {
    it('invokes listAgents without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.innerApiCalls.listAgents = stubSimpleCall(expectedResponse);
      const [response] = await client.listAgents(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listAgents without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.innerApiCalls.listAgents =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listAgents(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IAgent[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listAgents with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listAgents = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.listAgents(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listAgentsStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.descriptors.page.listAgents.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listAgentsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Agent[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Agent) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listAgents.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listAgents, request),
      );
      assert(
        (client.descriptors.page.listAgents.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes listAgentsStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listAgents.createStream = stubPageStreamingCall(
        undefined,
        expectedError,
      );
      const stream = client.listAgentsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Agent[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Agent) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listAgents.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listAgents, request),
      );
      assert(
        (client.descriptors.page.listAgents.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listAgents without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.descriptors.page.listAgents.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IAgent[] = [];
      const iterable = client.listAgentsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (client.descriptors.page.listAgents.asyncIterate as SinonStub).getCall(
          0,
        ).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listAgents.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listAgents with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listAgents.asyncIterate = stubAsyncIterationCall(
        undefined,
        expectedError,
      );
      const iterable = client.listAgentsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IAgent[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (client.descriptors.page.listAgents.asyncIterate as SinonStub).getCall(
          0,
        ).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listAgents.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('searchAgents', () => {
    it('invokes searchAgents without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.innerApiCalls.searchAgents = stubSimpleCall(expectedResponse);
      const [response] = await client.searchAgents(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchAgents without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.innerApiCalls.searchAgents =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.searchAgents(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IAgent[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchAgents with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.searchAgents = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.searchAgents(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchAgents as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchAgentsStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.descriptors.page.searchAgents.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.searchAgentsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Agent[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Agent) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.searchAgents.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchAgents, request),
      );
      assert(
        (client.descriptors.page.searchAgents.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes searchAgentsStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.searchAgents.createStream = stubPageStreamingCall(
        undefined,
        expectedError,
      );
      const stream = client.searchAgentsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Agent[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Agent) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.searchAgents.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchAgents, request),
      );
      assert(
        (client.descriptors.page.searchAgents.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with searchAgents without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
        generateSampleMessage(new protos.google.cloud.agentregistry.v1.Agent()),
      ];
      client.descriptors.page.searchAgents.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IAgent[] = [];
      const iterable = client.searchAgentsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchAgents.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.searchAgents.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with searchAgents with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchAgentsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchAgentsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.searchAgents.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.searchAgentsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IAgent[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchAgents.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.searchAgents.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('listEndpoints', () => {
    it('invokes listEndpoints without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
      ];
      client.innerApiCalls.listEndpoints = stubSimpleCall(expectedResponse);
      const [response] = await client.listEndpoints(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listEndpoints without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
      ];
      client.innerApiCalls.listEndpoints =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listEndpoints(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IEndpoint[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listEndpoints with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listEndpoints = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.listEndpoints(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listEndpoints as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listEndpointsStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
      ];
      client.descriptors.page.listEndpoints.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listEndpointsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Endpoint[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Endpoint) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listEndpoints.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listEndpoints, request),
      );
      assert(
        (client.descriptors.page.listEndpoints.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes listEndpointsStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listEndpoints.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.listEndpointsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Endpoint[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Endpoint) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listEndpoints.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listEndpoints, request),
      );
      assert(
        (client.descriptors.page.listEndpoints.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listEndpoints without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Endpoint(),
        ),
      ];
      client.descriptors.page.listEndpoints.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IEndpoint[] = [];
      const iterable = client.listEndpointsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.listEndpoints.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listEndpoints.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listEndpoints with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListEndpointsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListEndpointsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listEndpoints.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listEndpointsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IEndpoint[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.listEndpoints.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listEndpoints.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('listMcpServers', () => {
    it('invokes listMcpServers without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.innerApiCalls.listMcpServers = stubSimpleCall(expectedResponse);
      const [response] = await client.listMcpServers(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listMcpServers without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.innerApiCalls.listMcpServers =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listMcpServers(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IMcpServer[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listMcpServers with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listMcpServers = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.listMcpServers(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listMcpServersStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.descriptors.page.listMcpServers.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listMcpServersStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.McpServer[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.McpServer) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listMcpServers.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listMcpServers, request),
      );
      assert(
        (client.descriptors.page.listMcpServers.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes listMcpServersStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listMcpServers.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.listMcpServersStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.McpServer[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.McpServer) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listMcpServers.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listMcpServers, request),
      );
      assert(
        (client.descriptors.page.listMcpServers.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listMcpServers without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.descriptors.page.listMcpServers.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IMcpServer[] = [];
      const iterable = client.listMcpServersAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.listMcpServers.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listMcpServers.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listMcpServers with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listMcpServers.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listMcpServersAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IMcpServer[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.listMcpServers.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listMcpServers.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('searchMcpServers', () => {
    it('invokes searchMcpServers without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.innerApiCalls.searchMcpServers = stubSimpleCall(expectedResponse);
      const [response] = await client.searchMcpServers(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchMcpServers without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.innerApiCalls.searchMcpServers =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.searchMcpServers(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IMcpServer[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchMcpServers with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.searchMcpServers = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.searchMcpServers(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.searchMcpServers as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes searchMcpServersStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.descriptors.page.searchMcpServers.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.searchMcpServersStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.McpServer[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.McpServer) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.searchMcpServers.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchMcpServers, request),
      );
      assert(
        (client.descriptors.page.searchMcpServers.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes searchMcpServersStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.searchMcpServers.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.searchMcpServersStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.McpServer[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.McpServer) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.searchMcpServers.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.searchMcpServers, request),
      );
      assert(
        (client.descriptors.page.searchMcpServers.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with searchMcpServers without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.McpServer(),
        ),
      ];
      client.descriptors.page.searchMcpServers.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IMcpServer[] = [];
      const iterable = client.searchMcpServersAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchMcpServers.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.searchMcpServers.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with searchMcpServers with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.SearchMcpServersRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.SearchMcpServersRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.searchMcpServers.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.searchMcpServersAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IMcpServer[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.searchMcpServers.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.searchMcpServers.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('listServices', () => {
    it('invokes listServices without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
      ];
      client.innerApiCalls.listServices = stubSimpleCall(expectedResponse);
      const [response] = await client.listServices(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listServices without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
      ];
      client.innerApiCalls.listServices =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listServices(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IService[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listServices with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listServices = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.listServices(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listServices as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listServicesStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
      ];
      client.descriptors.page.listServices.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listServicesStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Service[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Service) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listServices.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listServices, request),
      );
      assert(
        (client.descriptors.page.listServices.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes listServicesStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listServices.createStream = stubPageStreamingCall(
        undefined,
        expectedError,
      );
      const stream = client.listServicesStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Service[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Service) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listServices.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listServices, request),
      );
      assert(
        (client.descriptors.page.listServices.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listServices without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Service(),
        ),
      ];
      client.descriptors.page.listServices.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IService[] = [];
      const iterable = client.listServicesAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.listServices.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listServices.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listServices with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListServicesRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListServicesRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listServices.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listServicesAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IService[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.listServices.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listServices.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('listBindings', () => {
    it('invokes listBindings without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.innerApiCalls.listBindings = stubSimpleCall(expectedResponse);
      const [response] = await client.listBindings(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listBindings without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.innerApiCalls.listBindings =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.listBindings(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IBinding[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listBindings with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.listBindings = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(client.listBindings(request), expectedError);
      const actualRequest = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.listBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes listBindingsStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.descriptors.page.listBindings.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.listBindingsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Binding[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Binding) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (client.descriptors.page.listBindings.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listBindings, request),
      );
      assert(
        (client.descriptors.page.listBindings.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('invokes listBindingsStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listBindings.createStream = stubPageStreamingCall(
        undefined,
        expectedError,
      );
      const stream = client.listBindingsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Binding[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Binding) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (client.descriptors.page.listBindings.createStream as SinonStub)
          .getCall(0)
          .calledWith(client.innerApiCalls.listBindings, request),
      );
      assert(
        (client.descriptors.page.listBindings.createStream as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listBindings without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.descriptors.page.listBindings.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IBinding[] = [];
      const iterable = client.listBindingsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.listBindings.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listBindings.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });

    it('uses async iteration with listBindings with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.ListBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.ListBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.listBindings.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listBindingsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IBinding[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.listBindings.asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (client.descriptors.page.listBindings.asyncIterate as SinonStub)
          .getCall(0)
          .args[2].otherArgs.headers[
            'x-goog-request-params'
          ].includes(expectedHeaderRequestParams),
      );
    });
  });

  describe('fetchAvailableBindings', () => {
    it('invokes fetchAvailableBindings without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.innerApiCalls.fetchAvailableBindings =
        stubSimpleCall(expectedResponse);
      const [response] = await client.fetchAvailableBindings(request);
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes fetchAvailableBindings without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.innerApiCalls.fetchAvailableBindings =
        stubSimpleCallWithCallback(expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.fetchAvailableBindings(
          request,
          (
            err?: Error | null,
            result?: protos.google.cloud.agentregistry.v1.IBinding[] | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      const actualRequest = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes fetchAvailableBindings with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.innerApiCalls.fetchAvailableBindings = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.fetchAvailableBindings(request),
        expectedError,
      );
      const actualRequest = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[0];
      assert.deepStrictEqual(actualRequest, request);
      const actualHeaderRequestParams = (
        client.innerApiCalls.fetchAvailableBindings as SinonStub
      ).getCall(0).args[1].otherArgs.headers['x-goog-request-params'];
      assert(actualHeaderRequestParams.includes(expectedHeaderRequestParams));
    });

    it('invokes fetchAvailableBindingsStream without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.descriptors.page.fetchAvailableBindings.createStream =
        stubPageStreamingCall(expectedResponse);
      const stream = client.fetchAvailableBindingsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Binding[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Binding) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      const responses = await promise;
      assert.deepStrictEqual(responses, expectedResponse);
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .createStream as SinonStub
        )
          .getCall(0)
          .calledWith(client.innerApiCalls.fetchAvailableBindings, request),
      );
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .createStream as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });

    it('invokes fetchAvailableBindingsStream with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.fetchAvailableBindings.createStream =
        stubPageStreamingCall(undefined, expectedError);
      const stream = client.fetchAvailableBindingsStream(request);
      const promise = new Promise((resolve, reject) => {
        const responses: protos.google.cloud.agentregistry.v1.Binding[] = [];
        stream.on(
          'data',
          (response: protos.google.cloud.agentregistry.v1.Binding) => {
            responses.push(response);
          },
        );
        stream.on('end', () => {
          resolve(responses);
        });
        stream.on('error', (err: Error) => {
          reject(err);
        });
      });
      await assert.rejects(promise, expectedError);
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .createStream as SinonStub
        )
          .getCall(0)
          .calledWith(client.innerApiCalls.fetchAvailableBindings, request),
      );
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .createStream as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });

    it('uses async iteration with fetchAvailableBindings without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedResponse = [
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
        generateSampleMessage(
          new protos.google.cloud.agentregistry.v1.Binding(),
        ),
      ];
      client.descriptors.page.fetchAvailableBindings.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: protos.google.cloud.agentregistry.v1.IBinding[] = [];
      const iterable = client.fetchAvailableBindingsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.descriptors.page.fetchAvailableBindings
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .asyncIterate as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });

    it('uses async iteration with fetchAvailableBindings with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new protos.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest(),
      );
      const defaultValue1 = getTypeDefaultValue(
        '.google.cloud.agentregistry.v1.FetchAvailableBindingsRequest',
        ['parent'],
      );
      request.parent = defaultValue1;
      const expectedHeaderRequestParams = `parent=${defaultValue1 ?? ''}`;
      const expectedError = new Error('expected');
      client.descriptors.page.fetchAvailableBindings.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.fetchAvailableBindingsAsync(request);
      await assert.rejects(async () => {
        const responses: protos.google.cloud.agentregistry.v1.IBinding[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.descriptors.page.fetchAvailableBindings
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (
          client.descriptors.page.fetchAvailableBindings
            .asyncIterate as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });
  });
  describe('getLocation', () => {
    it('invokes getLocation without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new LocationProtos.google.cloud.location.GetLocationRequest(),
      );
      request.name = '';
      const expectedHeaderRequestParams = 'name=';
      const expectedOptions = {
        otherArgs: {
          headers: {
            'x-goog-request-params': expectedHeaderRequestParams,
          },
        },
      };
      const expectedResponse = generateSampleMessage(
        new LocationProtos.google.cloud.location.Location(),
      );
      client.locationsClient.getLocation = stubSimpleCall(expectedResponse);
      const response = await client.getLocation(request, expectedOptions);
      assert.deepStrictEqual(response, [expectedResponse]);
      assert(
        (client.locationsClient.getLocation as SinonStub)
          .getCall(0)
          .calledWith(request, expectedOptions, undefined),
      );
    });
    it('invokes getLocation without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new LocationProtos.google.cloud.location.GetLocationRequest(),
      );
      request.name = '';
      const expectedHeaderRequestParams = 'name=';
      const expectedOptions = {
        otherArgs: {
          headers: {
            'x-goog-request-params': expectedHeaderRequestParams,
          },
        },
      };
      const expectedResponse = generateSampleMessage(
        new LocationProtos.google.cloud.location.Location(),
      );
      client.locationsClient.getLocation = sinon
        .stub()
        .callsArgWith(2, null, expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.getLocation(
          request,
          expectedOptions,
          (
            err?: Error | null,
            result?: LocationProtos.google.cloud.location.ILocation | null,
          ) => {
            if (err) {
              reject(err);
            } else {
              resolve(result);
            }
          },
        );
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      assert((client.locationsClient.getLocation as SinonStub).getCall(0));
    });
    it('invokes getLocation with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new LocationProtos.google.cloud.location.GetLocationRequest(),
      );
      request.name = '';
      const expectedHeaderRequestParams = 'name=';
      const expectedOptions = {
        otherArgs: {
          headers: {
            'x-goog-request-params': expectedHeaderRequestParams,
          },
        },
      };
      const expectedError = new Error('expected');
      client.locationsClient.getLocation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(
        client.getLocation(request, expectedOptions),
        expectedError,
      );
      assert(
        (client.locationsClient.getLocation as SinonStub)
          .getCall(0)
          .calledWith(request, expectedOptions, undefined),
      );
    });
  });
  describe('listLocationsAsync', () => {
    it('uses async iteration with listLocations without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new LocationProtos.google.cloud.location.ListLocationsRequest(),
      );
      request.name = '';
      const expectedHeaderRequestParams = 'name=';
      const expectedResponse = [
        generateSampleMessage(
          new LocationProtos.google.cloud.location.Location(),
        ),
        generateSampleMessage(
          new LocationProtos.google.cloud.location.Location(),
        ),
        generateSampleMessage(
          new LocationProtos.google.cloud.location.Location(),
        ),
      ];
      client.locationsClient.descriptors.page.listLocations.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: LocationProtos.google.cloud.location.ILocation[] = [];
      const iterable = client.listLocationsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.locationsClient.descriptors.page.listLocations
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (
          client.locationsClient.descriptors.page.listLocations
            .asyncIterate as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });
    it('uses async iteration with listLocations with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new LocationProtos.google.cloud.location.ListLocationsRequest(),
      );
      request.name = '';
      const expectedHeaderRequestParams = 'name=';
      const expectedError = new Error('expected');
      client.locationsClient.descriptors.page.listLocations.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.listLocationsAsync(request);
      await assert.rejects(async () => {
        const responses: LocationProtos.google.cloud.location.ILocation[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.locationsClient.descriptors.page.listLocations
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
      assert(
        (
          client.locationsClient.descriptors.page.listLocations
            .asyncIterate as SinonStub
        )
          .getCall(0)
          .args[2].otherArgs.headers['x-goog-request-params'].includes(
            expectedHeaderRequestParams,
          ),
      );
    });
  });
  describe('getOperation', () => {
    it('invokes getOperation without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.GetOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      client.operationsClient.getOperation = stubSimpleCall(expectedResponse);
      const response = await client.getOperation(request);
      assert.deepStrictEqual(response, [expectedResponse]);
      assert(
        (client.operationsClient.getOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
    it('invokes getOperation without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.GetOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new operationsProtos.google.longrunning.Operation(),
      );
      client.operationsClient.getOperation = sinon
        .stub()
        .callsArgWith(2, null, expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.operationsClient
          .getOperation(
            request,
            undefined,
            (
              err?: Error | null,
              result?: operationsProtos.google.longrunning.Operation | null,
            ) => {
              if (err) {
                reject(err);
              } else {
                resolve(result);
              }
            },
          )
          .catch((err) => {
            throw err;
          });
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      assert((client.operationsClient.getOperation as SinonStub).getCall(0));
    });
    it('invokes getOperation with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.GetOperationRequest(),
      );
      const expectedError = new Error('expected');
      client.operationsClient.getOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(async () => {
        await client.getOperation(request);
      }, expectedError);
      assert(
        (client.operationsClient.getOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
  });
  describe('cancelOperation', () => {
    it('invokes cancelOperation without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.CancelOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty(),
      );
      client.operationsClient.cancelOperation =
        stubSimpleCall(expectedResponse);
      const response = await client.cancelOperation(request);
      assert.deepStrictEqual(response, [expectedResponse]);
      assert(
        (client.operationsClient.cancelOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
    it('invokes cancelOperation without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.CancelOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty(),
      );
      client.operationsClient.cancelOperation = sinon
        .stub()
        .callsArgWith(2, null, expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.operationsClient
          .cancelOperation(
            request,
            undefined,
            (
              err?: Error | null,
              result?: protos.google.protobuf.Empty | null,
            ) => {
              if (err) {
                reject(err);
              } else {
                resolve(result);
              }
            },
          )
          .catch((err) => {
            throw err;
          });
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      assert((client.operationsClient.cancelOperation as SinonStub).getCall(0));
    });
    it('invokes cancelOperation with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.CancelOperationRequest(),
      );
      const expectedError = new Error('expected');
      client.operationsClient.cancelOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(async () => {
        await client.cancelOperation(request);
      }, expectedError);
      assert(
        (client.operationsClient.cancelOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
  });
  describe('deleteOperation', () => {
    it('invokes deleteOperation without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.DeleteOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty(),
      );
      client.operationsClient.deleteOperation =
        stubSimpleCall(expectedResponse);
      const response = await client.deleteOperation(request);
      assert.deepStrictEqual(response, [expectedResponse]);
      assert(
        (client.operationsClient.deleteOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
    it('invokes deleteOperation without error using callback', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.DeleteOperationRequest(),
      );
      const expectedResponse = generateSampleMessage(
        new protos.google.protobuf.Empty(),
      );
      client.operationsClient.deleteOperation = sinon
        .stub()
        .callsArgWith(2, null, expectedResponse);
      const promise = new Promise((resolve, reject) => {
        client.operationsClient
          .deleteOperation(
            request,
            undefined,
            (
              err?: Error | null,
              result?: protos.google.protobuf.Empty | null,
            ) => {
              if (err) {
                reject(err);
              } else {
                resolve(result);
              }
            },
          )
          .catch((err) => {
            throw err;
          });
      });
      const response = await promise;
      assert.deepStrictEqual(response, expectedResponse);
      assert((client.operationsClient.deleteOperation as SinonStub).getCall(0));
    });
    it('invokes deleteOperation with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.DeleteOperationRequest(),
      );
      const expectedError = new Error('expected');
      client.operationsClient.deleteOperation = stubSimpleCall(
        undefined,
        expectedError,
      );
      await assert.rejects(async () => {
        await client.deleteOperation(request);
      }, expectedError);
      assert(
        (client.operationsClient.deleteOperation as SinonStub)
          .getCall(0)
          .calledWith(request),
      );
    });
  });
  describe('listOperationsAsync', () => {
    it('uses async iteration with listOperations without error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.ListOperationsRequest(),
      );
      const expectedResponse = [
        generateSampleMessage(
          new operationsProtos.google.longrunning.ListOperationsResponse(),
        ),
        generateSampleMessage(
          new operationsProtos.google.longrunning.ListOperationsResponse(),
        ),
        generateSampleMessage(
          new operationsProtos.google.longrunning.ListOperationsResponse(),
        ),
      ];
      client.operationsClient.descriptor.listOperations.asyncIterate =
        stubAsyncIterationCall(expectedResponse);
      const responses: operationsProtos.google.longrunning.IOperation[] = [];
      const iterable = client.operationsClient.listOperationsAsync(request);
      for await (const resource of iterable) {
        responses.push(resource!);
      }
      assert.deepStrictEqual(responses, expectedResponse);
      assert.deepStrictEqual(
        (
          client.operationsClient.descriptor.listOperations
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
    });
    it('uses async iteration with listOperations with error', async () => {
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      const request = generateSampleMessage(
        new operationsProtos.google.longrunning.ListOperationsRequest(),
      );
      const expectedError = new Error('expected');
      client.operationsClient.descriptor.listOperations.asyncIterate =
        stubAsyncIterationCall(undefined, expectedError);
      const iterable = client.operationsClient.listOperationsAsync(request);
      await assert.rejects(async () => {
        const responses: operationsProtos.google.longrunning.IOperation[] = [];
        for await (const resource of iterable) {
          responses.push(resource!);
        }
      });
      assert.deepStrictEqual(
        (
          client.operationsClient.descriptor.listOperations
            .asyncIterate as SinonStub
        ).getCall(0).args[1],
        request,
      );
    });
  });

  describe('Path templates', () => {
    describe('agent', async () => {
      const fakePath = '/rendered/path/agent';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
        agent: 'agentValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.agentPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.agentPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('agentPath', () => {
        const result = client.agentPath(
          'projectValue',
          'locationValue',
          'agentValue',
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.agentPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromAgentName', () => {
        const result = client.matchProjectFromAgentName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.agentPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromAgentName', () => {
        const result = client.matchLocationFromAgentName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.agentPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchAgentFromAgentName', () => {
        const result = client.matchAgentFromAgentName(fakePath);
        assert.strictEqual(result, 'agentValue');
        assert(
          (client.pathTemplates.agentPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('binding', async () => {
      const fakePath = '/rendered/path/binding';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
        binding: 'bindingValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.bindingPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.bindingPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('bindingPath', () => {
        const result = client.bindingPath(
          'projectValue',
          'locationValue',
          'bindingValue',
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.bindingPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromBindingName', () => {
        const result = client.matchProjectFromBindingName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.bindingPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromBindingName', () => {
        const result = client.matchLocationFromBindingName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.bindingPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchBindingFromBindingName', () => {
        const result = client.matchBindingFromBindingName(fakePath);
        assert.strictEqual(result, 'bindingValue');
        assert(
          (client.pathTemplates.bindingPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('endpoint', async () => {
      const fakePath = '/rendered/path/endpoint';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
        endpoint: 'endpointValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.endpointPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.endpointPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('endpointPath', () => {
        const result = client.endpointPath(
          'projectValue',
          'locationValue',
          'endpointValue',
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.endpointPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromEndpointName', () => {
        const result = client.matchProjectFromEndpointName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.endpointPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromEndpointName', () => {
        const result = client.matchLocationFromEndpointName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.endpointPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchEndpointFromEndpointName', () => {
        const result = client.matchEndpointFromEndpointName(fakePath);
        assert.strictEqual(result, 'endpointValue');
        assert(
          (client.pathTemplates.endpointPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('location', async () => {
      const fakePath = '/rendered/path/location';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.locationPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.locationPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('locationPath', () => {
        const result = client.locationPath('projectValue', 'locationValue');
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.locationPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromLocationName', () => {
        const result = client.matchProjectFromLocationName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.locationPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromLocationName', () => {
        const result = client.matchLocationFromLocationName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.locationPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('mcpServer', async () => {
      const fakePath = '/rendered/path/mcpServer';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
        mcp_server: 'mcpServerValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.mcpServerPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.mcpServerPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('mcpServerPath', () => {
        const result = client.mcpServerPath(
          'projectValue',
          'locationValue',
          'mcpServerValue',
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.mcpServerPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromMcpServerName', () => {
        const result = client.matchProjectFromMcpServerName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.mcpServerPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromMcpServerName', () => {
        const result = client.matchLocationFromMcpServerName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.mcpServerPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchMcpServerFromMcpServerName', () => {
        const result = client.matchMcpServerFromMcpServerName(fakePath);
        assert.strictEqual(result, 'mcpServerValue');
        assert(
          (client.pathTemplates.mcpServerPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('project', async () => {
      const fakePath = '/rendered/path/project';
      const expectedParameters = {
        project: 'projectValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.projectPathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.projectPathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('projectPath', () => {
        const result = client.projectPath('projectValue');
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.projectPathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromProjectName', () => {
        const result = client.matchProjectFromProjectName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.projectPathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });

    describe('service', async () => {
      const fakePath = '/rendered/path/service';
      const expectedParameters = {
        project: 'projectValue',
        location: 'locationValue',
        service: 'serviceValue',
      };
      const client = new agentregistryModule.v1.AgentRegistryClient({
        credentials: { client_email: 'bogus', private_key: 'bogus' },
        projectId: 'bogus',
      });
      await client.initialize();
      client.pathTemplates.servicePathTemplate.render = sinon
        .stub()
        .returns(fakePath);
      client.pathTemplates.servicePathTemplate.match = sinon
        .stub()
        .returns(expectedParameters);

      it('servicePath', () => {
        const result = client.servicePath(
          'projectValue',
          'locationValue',
          'serviceValue',
        );
        assert.strictEqual(result, fakePath);
        assert(
          (client.pathTemplates.servicePathTemplate.render as SinonStub)
            .getCall(-1)
            .calledWith(expectedParameters),
        );
      });

      it('matchProjectFromServiceName', () => {
        const result = client.matchProjectFromServiceName(fakePath);
        assert.strictEqual(result, 'projectValue');
        assert(
          (client.pathTemplates.servicePathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchLocationFromServiceName', () => {
        const result = client.matchLocationFromServiceName(fakePath);
        assert.strictEqual(result, 'locationValue');
        assert(
          (client.pathTemplates.servicePathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });

      it('matchServiceFromServiceName', () => {
        const result = client.matchServiceFromServiceName(fakePath);
        assert.strictEqual(result, 'serviceValue');
        assert(
          (client.pathTemplates.servicePathTemplate.match as SinonStub)
            .getCall(-1)
            .calledWith(fakePath),
        );
      });
    });
  });
});
