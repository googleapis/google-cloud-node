{
  "name": "spanner-sample-fixture",
  "description": "An app we're using to test the library.",
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "node build/src/index.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@google-cloud/spanner": "file:./spanner.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^4.0.0",
    "gts": "^3.0.0"
  },
  "overrides": {
    "gcp-metadata": "^7.0.1",
    "@sinonjs/fake-timers": "15.2.1",
    "whatwg-url": "^14.0.0",
    "uri-js": "npm:fast-uri@^3.0.0",
    "punycode": "^2.3.1"
  }
}
