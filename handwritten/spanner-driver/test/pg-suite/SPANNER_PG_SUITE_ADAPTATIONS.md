# Cloud Spanner PG Suite Adaptations & Invariants

This document outlines the source, architectural adaptations, SQL dialect modifications, and directory exclusions applied when porting the official [`node-postgres` (`pg`) test suite](https://github.com/brianc/node-postgres/tree/c5e8c9a57bff6d9160ec5dbd5c4f4c1e4c460711/packages/pg/test) to execute against **Google Cloud Spanner** via `@google-cloud/spanner-driver`.

---

## 1. Upstream Source & Porting Scope

* **Upstream Reference**: [`brianc/node-postgres/packages/pg/test` (Commit: `c5e8c9a`)](https://github.com/brianc/node-postgres/tree/c5e8c9a57bff6d9160ec5dbd5c4f4c1e4c460711/packages/pg/test)
* **Ported Scope**: All integration tests for client queries, parameter binding, transactions, type parsers, and connection pooling in `integration/client/` and `integration/connection-pool/`.

### Omitted Upstream Test Suites & Directories:

| Upstream Directory / Suite | Reason for Exclusion |
| :--- | :--- |
| **`cloudflare/`** | Tests Cloudflare Workers runtime compatibility (`workerd` environment and WebSocket-based socket polyfills). Inapplicable to Node.js native gRPC / CGO drivers. |
| **`native/`** | Tests `pg-native` (Node bindings to C `libpq.so`). `@google-cloud/spanner-driver` communicates with Cloud Spanner via `@google-cloud/spannerlib-node` (Google Cloud gRPC / CGO `spannerlib`), not `libpq`. |
| **`tls/`** | Tests local TCP socket TLS handshakes, custom server certificates (`server.crt`), and mutual TLS client keys (`client.key`) with standalone PostgreSQL daemons. Cloud Spanner connections use Google Cloud IAM authentication and gRPC TLS/ALTS transport. |
| **`unit/`** | Tests internal implementation details of `node-postgres`'s pure-JS wire-protocol packet serializer/deserializer (`connection.js`, `packet-reader.js`, etc.). `@google-cloud/spanner-driver` maintains its own comprehensive TypeScript unit test suite in `test/unit/`. |

---

## 2. Architectural & Schema Adaptation Principles

### Primary Keys & Permanent `CREATE TABLE`
* **Upstream `node-postgres`**: Used `CREATE TEMP TABLE tableName(...)` or `CREATE TEMPORARY TABLE`.
* **Cloud Spanner Requirement**: Cloud Spanner does not support temporary or unlogged tables.
* **Adaptation**: Converted to standard `CREATE TABLE IF NOT EXISTS` with a Spanner-compatible Bit-Reversed identity primary key:
  ```sql
  CREATE TABLE IF NOT EXISTS tableName (
    spanner_pk_id BIGINT GENERATED BY DEFAULT AS IDENTITY (BIT_REVERSED_POSITIVE) PRIMARY KEY,
    -- additional columns
  )
  ```

### Asynchronous DDL Callback Chaining (`CREATE TABLE` -> `DML`)
* **Upstream `node-postgres`**: Issued unchained queries sequentially (`client.query('CREATE TABLE...')`, `client.query('INSERT...')`) over a single FIFO TCP socket connection.
* **Cloud Spanner Requirement**: Cloud Spanner processes DDL (`CREATE TABLE`) via the **Database Admin API** (`UpdateDatabaseDdl`) and DML/DQL (`INSERT`/`SELECT`) via the **Data API** (`ExecuteSql`). Schema changes take ~5-10s to propagate across Cloud Spanner database nodes on GCP.
* **Adaptation**: Chained callbacks so `CREATE TABLE` finishes on GCP before `INSERT`/`SELECT` queries are issued (`client.query('CREATE TABLE...', (err) => { client.query('INSERT...', ...); })`).

### Self-Contained Test Data Cleanup (`DELETE FROM`)
* **Upstream `node-postgres`**: Relied on temporary tables dropping automatically when TCP socket connections closed.
* **Adaptation**: Added explicit data cleanup (`DELETE FROM tableName`) before inserting test rows to prevent row accumulation across test runs.

---

## 3. Deleted Test Files (Raw TCP Socket Protocol)

The following test files tested low-level PostgreSQL TCP socket wire protocols that **can never apply** to Cloud Spanner's gRPC channel architecture, and were removed:

| Deleted Test File | Reason for Deletion |
| :--- | :--- |
| **`connection-timeout-tests.cjs`** | Created a local raw TCP server to test TCP socket read timeouts (`query_timeout: 1000`). Cloud Spanner communicates via gRPC / CGO channels. |
| **`ssl-tests.cjs`** | Tested raw PostgreSQL `SSLRequest` packet negotiation (`80877103`) over TCP sockets. Cloud Spanner uses Google Cloud IAM / gRPC TLS authentication. |
| **`network-partition-tests.cjs`** | Simulated TCP socket drops via local Node `net.createServer()`. Spanner uses HTTP/2 gRPC channel pools. |
| **`connection-pool/tls-tests.cjs`** | Tested PostgreSQL SSL/TLS client certificate files (`PGSSLROOTCERT`, `PGSSLCERT`, `PGSSLKEY`) over raw TCP sockets. Cloud Spanner uses Google Cloud IAM / gRPC TLS authentication. |
| **`connection-pool/native-instance-tests.cjs`** | Tested `libpq` C-binding toggles (`pg.native`). `@google-cloud/spanner-driver` uses the native `spannerlib-node` Go CGO bridge. |
| **`sasl-scram-tests.cjs`** | Tested PostgreSQL SCRAM-SHA-256 password authentication negotiation and `pg_hba.conf` role authentication over raw TCP sockets. `@google-cloud/spanner-driver` uses Google Cloud IAM authentication over gRPC TLS. |

*Note: Accompanying TCP packet mocking helpers (`test-buffers.cjs` and `buffer-list.cjs`) were also removed in favor of a single unified `test/pg-suite/test-helper.cjs`.*

---

## 4. Skipped Test Files & Future Roadmap

| Test File | Skipped Test Cases | Reason for Skipping / Cloud Spanner Invariant |
| :--- | :--- | :--- |
| **`appname-tests.cjs`** | `application_name`, `fallback_application_name`, `PGAPPNAME` | Cloud Spanner does not support libpq GUC session configuration parameters (`SET application_name = ...`). Application identity and telemetry are tracked via Google Cloud IAM credentials and SDK User-Agent headers. |
| **`api-tests.cjs`** | `query timeout`, `query recover from timeout`, `query with timeout on query basis` | Tests TCP socket read timeouts (`query_timeout: 1000`) and uses unsupported `pg_sleep()`. Query execution deadlines in Spanner are managed via gRPC contexts or `statement_timeout`. |
| **`error-handling-tests.cjs`** | `query receives error on client shutdown`, `connected, idle client error` | Uses unsupported `pg_sleep()` and mocks internal TCP socket stream error events (`(client.connection || client.native).emit('error')`). |
| **`statement_timeout-tests.cjs`** | `statement_timeout actually cancels long running queries` | Relies on PostgreSQL `pg_sleep(5)` to simulate long-running queries (unsupported in Spanner). |
| **`multiple-results-tests.cjs`** | `queryMode: 'extended'`, `generate_series()`, `mixed queries and statements` | PostgreSQL wire-specific query mode, unsupported set-returning function `generate_series()`, and per-statement command tag derivation. |
| **`array-tests.cjs`** | `nested array`, `integer nested array` | Multi-dimensional 2D arrays (`int[][]`) are unsupported in Cloud Spanner's 1D flat array type system. |
| **`timezone-tests.cjs`** | `timestamp without time zone` | `TIMESTAMP WITHOUT TIME ZONE` is unsupported in Cloud Spanner PostgreSQL dialect (only `TIMESTAMPTZ` is supported). |
| **`txstatus-tests.cjs`** | `txStatus error state` | Follow-up roadmap item for transitioning `txStatus` to `'E'` upon query execution error inside transaction blocks. |
| **`query-as-promise-tests.cjs`** | `promise API with configurable promise type` | Custom 3rd-party `bluebird` promise constructor configuration (unneeded in native ES6). |
| **`prepared-statement-tests.cjs`** | All named statement test cases | Client-side named statement caching without query text (`name: 'find_user'`) is not supported. |
| **`query-error-handling-prepared-statement-tests.cjs`** | Prepared statement cancellation & error tests | Uses unsupported `pg_sleep()`, `pg_stat_activity`, and named statements. |
| **`notice-tests.cjs`** | `LISTEN` / `NOTIFY` & `RAISE NOTICE` | PostgreSQL server-specific asynchronous pub/sub wire messages and PL/pgSQL triggers. |
| **`idle_in_transaction_session_timeout-tests.cjs`** | `idle_in_transaction_session_timeout` | PostgreSQL server-side connection auto-kill timers. |
| **`configuration-tests.cjs`** | `pg.defaults` global env mutations | `libpq` global process environment variable mutations (`process.env.USER`). |

---

## 5. Modified Test Files & Applied Changes

| Test File | Changes Applied | Reason for Modification |
| :--- | :--- | :--- |
| **`connection-pool/error-tests.cjs`** | 1. Replaced TCP port 13801 test with non-existent database connection rejection.<br>2. Replaced `pg_terminate_backend` kill query with client error release (`done(new Error(...))`).<br>3. Tested queued and future query rejection on closed client.<br>4. Replaced `pg_sleep` + `net.Socket` stream error mocking with invalid query handling and pool health retention. | Replaces PostgreSQL-specific functions (`pg_stat_activity`, `pg_terminate_backend`, `pg_sleep`) and raw `net.Socket` stream mocking with native Cloud Spanner pool error handling tests. |
| **`connection-pool/idle-timeout-tests.cjs`** | 1. Converted test to `async`/`await`.<br>2. Replaced `SELECT NOW()` with `SELECT CURRENT_TIMESTAMP`.<br>3. Awaited query execution and client eviction via `pool.once('remove')` on 50ms timer.<br>4. Verified `pool.totalCount === 0` and closed pool via `await pool.end()`. | Fixes false-pass synchronous execution bug and verifies asynchronous client idle eviction and pool teardown. |
| **`results-as-array-tests.cjs`** | 1. Converted test to `async`/`await`.<br>2. Replaced `SELECT NOW()` with `SELECT CURRENT_TIMESTAMP`.<br>3. Adapted assertions for `INT8` returning as string `'1'` and closed client with `await client.end()`. | Adapts SQL dialect to Spanner and matches INT8 string representation. |
| **`row-description-on-results-tests.cjs`** | 1. Converted both test cases to `async`/`await`.<br>2. Replaced `SELECT NOW()` with `SELECT CURRENT_TIMESTAMP`.<br>3. Asserted Spanner PostgreSQL OIDs: `1184` (`TIMESTAMPTZ`), `20` (`INT8`), `25` (`TEXT`), and closed client with `await client.end()`. | Adapts SQL dialect and validates Spanner PostgreSQL type OID descriptors. |
| **`parse-int-8-tests.cjs`** | 1. Converted test to `async`/`await`.<br>2. Replaced legacy 0.x `pg.defaults.parseInt8` flag with `pg.types.setTypeParser(20, ...)` dynamic toggling for `INT8` parsing between string and number. | Modernizes type parser test to use `pg.types` type overrides. |
| **`empty-query-tests.cjs`** | Adapted test to assert client-side validation error (`Query text must be a non-empty string`) via callback and Promise rejection. | Cloud Spanner requires non-empty query strings; `spanner-driver` guards against empty queries client-side before sending network requests. |
| **`type-coercion-tests.cjs`** | 1. Used `SELECT $1::type AS col` in `testForTypeCoercion` instead of 13 repeated `CREATE`/`DROP TABLE` DDL cycles.<br>2. Filtered unsupported types (`timestamp` without timezone, `timetz`, `time`).<br>3. Mapped cast type names (`smallint` -> `integer`, `oid` -> `bigint`, `varchar(5)` -> `varchar`, `real`/`double precision` -> `numeric::type`).<br>4. Removed precision modifier `(3)` from `timestamptz`.<br>5. Cast untyped `$1::bigint` in null comparison (`7 <> $1::bigint`).<br>6. Used supported date range and cast string parameter via `SELECT $1::text::TIMESTAMPTZ`. | Avoids 13 expensive GCP DDL operations (~4 mins), adapts to Spanner PG supported data types (`TIMESTAMPTZ`), and handles Spanner protobuf parameter typing requirements for integers, floats, and strings. |
| **`timezone-tests.cjs`** | Used `TIMESTAMPTZ` and `SELECT CURRENT_TIMESTAMP as val` for timezone tests. | Validates supported `TIMESTAMPTZ` behavior and UTC timezone offset handling. |
| **`promise-api-tests.cjs`** | 1. Verified native ES6 `Promise` resolution on `client.connect()`.<br>2. Verified invalid database query execution (`await client.query('SELECT 1')`) rejects with `Database not found` when `resolveDsn` preserves `cfg.database`. | Validates promise API behavior and verifies `client.query()` rejection on non-existent databases. |
| **`query-as-promise-tests.cjs`** | Adapted promise test cases to native ES6 promises (`Promise`). | Modernizes test cases to native ES6 `Promise`. |
| **`huge-numeric-tests.cjs`** | 1. Wrapped in `new helper.Suite()`.<br>2. Removed binary wire format parser call (`types.setTypeParser(1700, 'binary', ...)`).<br>3. Adjusted NUMERIC string length in test assertion to valid Cloud Spanner NUMERIC precision (30 digits). | Validates `pg.types.setTypeParser` custom NUMERIC parsers via text wire format within Cloud Spanner NUMERIC precision limits. |
| **`multiple-results-tests.cjs`** | 1. Converted generator-based `co.wrap` to native ES6 `async`/`await` (removing legacy `co` dependency).<br>2. Renamed test table to `weather_multi_test` to prevent `CREATE TABLE IF NOT EXISTS` skipping. | Modernizes test runner async syntax and eliminates missing `co` module errors. |
| **`field-name-escape-tests.cjs`** | 1. Wrapped in `new helper.Suite()`.<br>2. Updated result column key assertions using `Object.keys(res.rows[0])`. | Handles special character column alias escaping in Spanner result sets. |
| **`custom-types-tests.cjs`** | Replaced PostgreSQL `SELECT NOW()` with standard Spanner syntax `SELECT CURRENT_TIMESTAMP as val`. | `NOW()` function calls replaced with `CURRENT_TIMESTAMP` in Spanner PostgreSQL dialect. |
| **`statement_timeout-tests.cjs`** | 1. Updated `SET statement_timeout` queries to use duration strings (e.g. `'3000ms'`, `'1s'`).<br>2. Accepted empty string `''`, `'0'`, or `'0s'` in `No default statement_timeout` assertion. | Aligns with Go duration property handling and accommodates Spanner's default return value for unconfigured session variables. |
| **`simple-query-tests.cjs`** | 1. Renamed table `boom` to `boom_select_test` to prevent column collisions.<br>2. Chained `CREATE TABLE` -> `INSERT` -> `SELECT` callbacks.<br>3. Passed `currentResult` as 2nd parameter in `query.emit('row', rowData, currentResult)`. | Guarantees schema creation completes on GCP before DML runs, prevents inter-file schema collisions, and provides `result` metadata to `row` listeners. |
| **`array-tests.cjs`** | Adapted `JS array parameter` to verify 1D array parameter bindings (`[[1, 2, 3]]`). | Cloud Spanner type system supports 1D flat arrays (`text[]`, `integer[]`). |
| **`json-type-parsing-tests.cjs`** | Wrapped in `new helper.Suite()` and verified `JSONB` column encoding and decoding with plain JS objects. | Spanner PostgreSQL dialect automatically coerces string JSON parameters into `JSONB` columns. |
| **`quick-disconnect-tests.cjs`** | 1. Wrapped in `new helper.Suite()`.<br>2. Removed hardcoded TCP `helper.config` so client connects directly to resolved Spanner DSN. | Connects and disconnects client immediately without hanging gRPC handles or attempting TCP loopback connections. |
| **`type-parser-override-tests.cjs`** | 1. Wrapped in `new helper.Suite()` and removed overriding local PostgreSQL `host: 'localhost'` config.<br>2. Removed binary format parser override calls (`client.setTypeParser(oid, 'binary', fn)`). | Routes client connections to Cloud Spanner DSN and validates per-client scoped `ITypeOverrides` delegation. |
| **`txstatus-tests.cjs`** | Added test for `client.getTransactionStatus()`. | Verifies active transaction state tracking (`'I'` -> `'T'` -> `'I'`). |
| **`transaction-tests.cjs`** | Modernized test cases to `async`/`await` and used explicit single-statement transaction control (`BEGIN`, `INSERT`, `COMMIT`, `ROLLBACK`). | Adapts transaction execution to Spanner's single-statement query execution model. |
| **`api-tests.cjs`** | 1. Added Spanner primary key (`spanner_pk_id BIGINT GENERATED BY DEFAULT AS IDENTITY (BIT_REVERSED_POSITIVE) PRIMARY KEY`) to table `my_nulls`.<br>2. Modernized callback chaining for DDL -> DELETE -> INSERT -> SELECT. | Satisfies Spanner primary key requirement and ensures sequential query execution. |
| **`query-column-names-tests.cjs`**, **`result-metadata-tests.cjs`**, **`query-error-handling-tests.cjs`**, **`no-data-tests.cjs`** | Added Cloud Spanner required primary key schema (`spanner_pk_id BIGINT GENERATED BY DEFAULT AS IDENTITY (BIT_REVERSED_POSITIVE) PRIMARY KEY`). | Cloud Spanner requires primary keys on all tables; bit-reversed positive sequence prevents hot-spotting. |
| **`async-stack-trace-tests.cjs`** | Updated error assertions to verify V8 stack traces contain the database error message (`nonexistent`) and `outerFunction`. | Validates async stack trace preservation across the CGO / FFI boundary. |
| **`connection-pool-size-tests.cjs`** | Replaced `SELECT * FROM NOW()` and `generate_series()` with standard queries (`SELECT CURRENT_TIMESTAMP AS now`, `SELECT 1 AS num`). | Replaces PostgreSQL-specific table function `FROM NOW()` and unsupported set-returning function `generate_series()` with standard queries to test high concurrency pool throughput. |
| **`yield-support-tests.cjs`** | Converted legacy generator coroutines (`co.wrap(function* () {})`) to native ES6 `async`/`await`. | Eliminates missing `co` module dependency and modernizes asynchronous test syntax. |

---

## 6. Dynamic Database Lifecycle & Single Helper Architecture

* **Single Unified Helper (`test/pg-suite/test-helper.cjs`):**
  * Merged test runner logic (`Suite`, `Test`) directly into `test-helper.cjs`.
  * Exports `Suite`, `Client`, `pg`, `connect()`, assertions (`assert.calls`, `assert.raises`), and table fixtures (`createPersonTable`).
* **Dynamic Database Management (`test/pg-suite/run_pg_suite.cjs`):**
  * Aligned with `system-test/driver.ts` environment variable resolution (`SPANNER_INSTANCE`, `DATABASE_URL`, `SPANNER_CONNECTION_STRING`, `SPANNER_PROJECT`, `SPANNER_EMULATOR_HOST`).
  * Automatically creates a fresh temporary Spanner PostgreSQL-dialect database (`test_pg_${Date.now()}`) using the Cloud Spanner Database Admin Client.
  * Pauses 3 seconds to warm up Spanner session nodes before test execution.
  * Drops the temporary database in `finally` upon test completion.
  * Compiles and outputs master summary metrics to `test/test_results.json`.
