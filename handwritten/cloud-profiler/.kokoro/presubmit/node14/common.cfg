# Format: //devtools/kokoro/config/proto/build.proto

# Download trampoline resources.
gfile_resources: "/bigstore/cloud-devrel-kokoro-resources/trampoline"

# Action needed to build the trampoline binary used by trampoline_v1.py
before_action {
  invoke_blaze {
    name: "trampoline"
    command: BUILD
    targets: "//cloud/kokoro/tools/trampoline:trampoline.par"
  }
}

# Configure the default docker image for kokoro-trampoline.
env_vars: {
    key: "TRAMPOLINE_IMAGE"
    value: "gcr.io/cloud-devrel-kokoro-resources/node:14-user"
}

# The directory to use as the working directory *inside the container*,
# relative to the SCM root. This is common for all tests in this package.
# env_vars: {
#   key: "TRAMPOLINE_WORKDIR"
#   value: "handwritten/cloud-profiler"
# }

# Build logs will be here
action {
  define_artifacts {
    regex: "**/*sponge_log.xml"
  }
}
