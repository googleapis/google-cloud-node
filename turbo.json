{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["PATH", "SHELL", "SYSTEMROOT"],
  "tasks": {
    "compile": {
      "outputs": ["build/**"]
    },
    "test": {
      "dependsOn": ["compile"],
      "outputs": []
    }
  }
}