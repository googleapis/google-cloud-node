{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["PATH", "SystemRoot", "APPDATA", "LOCALAPPDATA"],
  "tasks": {
    "compile": {
      "outputs": ["build/**"]
    },
    "test": {
      "dependsOn": ["compile"],
      "outputs": []
    }
  }
}